---
Register: design
Standing: current-development-state
---

# UI function landing — Version 2 shell × Version 1 functionality

The integration commission of 2026-09-18: the parallel UI (Version 2) is the
presentation destination; the accumulated functionality (Version 1 and the
native owners) is the donor. One real application. This is the landing table
the commission (§4) requires; it is maintained here and reported on #375.

## Revisions this landing stands on (verified 2026-09-18 evening)

| Role | Revision |
|---|---|
| UI destination (working tree, authoritative incl. uncommitted refinements) | `agent/expression-world-convergence-20260917` @ `7747d5e6` + live dirty state (the currently open app: `tauri dev`, vite :1421) |
| Functionality donor (pre-convergence cut) | O-I `main` @ `fe92e1d0` |
| Factory donor — real workbench | PR #292 `origin/cradle-factory-arrangement` |
| Factory donor — desk reading lane | `agent/factory-desk-reading-20260918` @ `6a450b74` (worktree `/private/tmp/oi-factory-ui-review`) |
| Companion lane (continue, never restart) | `agent/agent-chat-ui-2026-09-18` @ `a808da20` (worktree `.worktrees/agent-chat-ui`) |
| Settings/System lane | `agent/settings-redesign-2026-09-18` @ `979b6b8e` |
| QL instruments (`ql.techne/v1`, TechneAdapter) | QL-MEF `main` @ `dbff5cc` |
| Kernel camelCase `providerContract` | already in the live tree (`kernel/src/factory.rs` accepts both spellings; the 6a450b74 fix) |

The owner lanes above are active and respected; none is rebased, reset or
landed here. The integration commits ride the convergence branch locally;
PR #370 is the owner's to land.

## Landing table

| Functional obligation | Donor implementation | Native contract | Destination in the new UI | State/lifecycle binding | Test in the new app | Standing |
|---|---|---|---|---|---|---|
| F1 · Full SSSF Run view of a selected Run (multi-lane monitor + semantic reading + live world + run map) | F0 trace composition already captured (`components/ExecutionTraceExplorer/PhaseDetail`, Factory `factory-ui` port); Run-map reading + validators from PR #292 (`run-reading.ts`, `RunMapReading.tsx`, `factory.css`) | `factory_build_snapshot` (kernel accepts `providerContract` camelCase); `factory_development_read` `run` → `factory.run-reading/v1` | `desk/DeskRunView.tsx` inside `DeskRunDetail` (Desk centre) — depths Trajectory/Reading/Live/Map | one read per entry, owner validators, honest refused states; no poll | `walk/desk-run-view-probe.mjs` (all four depths, fixture + refused map), `walk/desk-probe.mjs`, `tsc --noEmit` | **landed 2026-09-18** |
| F2 · Demo BuildSurface retired from the operating face | — | — | `BuildSurface` now mounted only in the dev-only development console (`FactoryDevelopmentSurface`, `import.meta.env.DEV` `<details>`) | dev disclosure only | desk probes assert the centre no longer routes through it implicitly | **landed with F1** (file retained for the dev console; retirement by name recorded here) |
| F3 · Factory live updates (arrival/hold/resume over real streams) | PR #292 `FactoryLive.tsx` — ported as `FactoryLiveProvider` (observe-on-read + acknowledge; the donor's revision-index poll and `central-project-link-read` linkage check name kernel ops this cut does not carry, so there is no poll loop at all — observations come only from reads the Desk actually performs, with the basis disclosed per observation) | `factory_build_snapshot` (revision signature from the owner's disclosed `factory.build-view/v1` revisions; bounded content signature when the owner does not disclose them — never presented as an owner revision) | `FactoryCentre` wraps the Desk in one live field; `DeskBoard` cards carry the arrival marker + revision acknowledge; `DeskRunDetail` carries the live-follow Refresh + acknowledge | one provider per visible centre surface, no timer, unseen material accumulates until acknowledged | `walk/desk-live-material-probe.mjs` (arrival → acknowledge, fixture-labelled scenario) + desk probes green, `tsc --noEmit` clean for the lane | **landed 2026-09-18** (native watch/cursor absent by design — owner ops `factory_attempt_task_list_read` / `central-project-link-read` remain the donor's future wiring) |
| F4 · Handoff / material / attempt-task surfaces (persisted results, Git basis) | PR #292 files ported: `FactoryHandoffSurface`, `FactoryMaterialSurface`, `FactoryAttemptHandoffDocument`, `attempt-task.ts`, `factory-material-reading.ts`, `factory-review-snapshot.ts`, `CandidateReading` (subject browser) | material reads `factory_build_snapshot` + the owner validators (the donor's `build` development-read spelling is not carried; attempt-task ops `factory_attempt_task_list_read`/`factory_attempt_task_read` are NOT on this kernel cut — the handoff surface renders the named owner-read-unavailable state and invents no list) | Desk Run detail: Produced material section (inline material reading, fixture-labelled in the dev scenario) + Attempt handoff section | retained review snapshots (byte-bounded, exact identity+revision restore) bind the reviewed revision; a moved owner revision is offered, never auto-replaced | `walk/desk-live-material-probe.mjs` (fixture-labelled material, honest empty state, handoff unavailable state) + desk probes green, `tsc --noEmit` clean for the lane | **landed 2026-09-18** (material complete; attempt-handoff documents wait on the two owner kernel ops) |
| F5 · Cross-project desk aggregation over explicitly added Factory sources | desk lane (§10 of the handoff) | `development read` project → journey → runs at bounded concurrency | `DeskBoard` (already present in the live tree) | preferences, never a run database | `fixtures/desk-board.ts` + probes | already in tree (fixture-proven; live read proven by desk lane) |
| F6 · Tasks = the real chat in the centre | shared `AgentChat` (one conversation Surface) | encounter session; Run binding by exact agent-session ref | `FactoryCentre` Tasks view | draft survives view switches | `walk/desk-probe.mjs` 4/5 | already in tree |
| E1 · Expressions stage owns the full surface; the Studio dock is a summoned overlay, never a flex column that shrinks the field (superseded as the centre face 2026-09-19 by E3 — the hosted application owns the centre; the overlay law stands for the retained surface and its parts) | Expressions mode (convergence line) | stage ownership law (#375 §9); shell canvas panels already float over the fixed full-window stage | `expressions.css` + `ExpressionsSurface.tsx` — `.xp-studio` absolute overlay (right/foot) over the full-bleed `.xp-field`, shared resize handle on the overlay's leading edge | dock geometry remembered per viewer (unchanged); field width invariant under open/close | `walk/expressions-overlay-probe.mjs` (field width invariant under open/close, overlay edge alignment, handle position, arrow-key resize) + `expression-resize-integration` green | **landed 2026-09-18** |
| E2 · Epi-Logos sixth entrance binds the world; context retains across arrangements; leaving is explicit | #375 amendment (the footer entrance was a dead boolean; the store's world-binding path was unreachable — `TREE_MODES` excludes epi-logos) | workspace context world (`context.world`) — structural, mode-independent | footer "Enter the Epi-Logos world" → binds world + opens the curated Epi surface; "Within Epi-Logos" chip with explicit Leave in every arrangement | the context (with return trail) lives in the workspace, not the mode — switching arrangements cannot lose it | `walk/epi-retention-probe.mjs` (enter → retains through factory/expressions/techne/base → explicit leave clears; no errors) | **landed 2026-09-18** |
| T1 · Technè top-bar instrument tabs M0′–M5′ bound to `TechneReading.disclosure` | QL-MEF `main` `dbff5cc` TechneAdapter + six instruments | `ql.techne/v1` | `techne/` arrangement (`TechneSurface` rail in the shared `TabPresentation` grammar; `techneReading.ts` provider seam) | subject's disclosed constellation, never a static six; tab switching is presentation state, arrangement restores | `walk/techne-instrument-tabs-probe.mjs` (six tabs, M0′ first view, stated reasons not simulated content, state preservation across tabs and modes, pin/orient/reveal grammar, wire-contract checks) + `tsc --noEmit` | **landed 2026-09-18** — the frame→subject seam landed the same evening (CradleFrame/Workbench pass `workspace.current.context.subject` into `TechneSurface`; the disclosure is requested for the person's selected subject). Open join: no QL reading provider registered into the cradle yet (`registerTechneReadingProvider` in `src/techne/techneReading.ts` is the QL-side seam) — until it registers, the rail truthfully names the unavailable reading source |
| W1 · O:I Web through the one Library (subjects → presentations → return) | existing Library/Search, Beings/Things, WorldPresentation, SharedField routes | native carriers (#18/#352/#366); providers read `expression list`, the hosted-field read, search; worlds refuse honestly (no list-all op — the reason IS the state) | `library/` scope model (here/local/shared) | identity/revision/reading position preserved | `walk/library-join-probe.mjs` (scopes disclose; shared-unavailable honest; no errors) | **standing confirmed 2026-09-18** — the connective field works with honest degradation; the full page → Expression → exact-source → return walk remains |
| C1 · Right companion: chat face, Run\|Agents\|Context panel sides (Ta-Onta) | `agent/agent-chat-ui-2026-09-18` — advanced from `a808da20` by the commissioned worker: `f5ac578d` (build repair — the lane tip had 17 tsc errors from snapshot fragments whose siblings lived in other lanes' trees) + `d1e2f5fd` (skill-assignment pipeline stages: real SKILL.md corpus search through the kernel files seam, bounded literal-comparison proposal with reasons/gaps/basis digest, truthful `profile_use_apply` receipt + `profile_use_plan` re-read) | files seam (`files_list`/`file_read`/`world_read`); `profile_use_plan`/`profile_use_apply`; missing named: owner mint op, work-start op | agency surface on the lane (`groundSkillSource.ts`, `MintAgent.tsx`) | uncommitted lanes preserved; fixtures dev-only | lane walk `skill-pipeline` 22/22 + tsc clean in the worktree | **advanced 2026-09-18** — Worker D (`be9295b8`) completed the commissioned standard's remaining items: the one resolved start interaction (StartPassage — intent as the composer's own draft, then Agent/Harness/Model/Environment/Skills/Context/Permissions rows each independently revisitable over real reads, Run-scoped Start naming its missing owner op), Direct work genuinely startable through the ordinary start/read pair, and Send/To:-address/Delegate-bounded-work as visibly distinct acts. Walk 23/23 against a real resident AIKit SessionSpace + controlled ACP fixture; skill-pipeline 22/22 regression; tsc clean. Still owner-side (named in the UI): model roster seam (AIKit), Run-scoped work start (#220), agent mint, bounded-delegation grant |
| B1 · Base files/editor/terminal/preview + Day/Flow documents | main @ `fe92e1d0` base workbench | kernel file ops; Day/Flow HTML on main | Base mode | existing left-files UX is accepted | `walk/mode-sweep-probe.mjs` (every entrance in the one shell, return to Base clean, no errors) + `walk/footer-probe.mjs` (reveal law: edge 3px→24px on hover, actions summary reachable) | **verified 2026-09-18** |

### Superseded presentations (retired by name, never silently)

- `BuildSurface` as the Desk's operating Run face (F1/F2 above) — retained
  only inside the dev-only development console.
- The six-tab Factory sidebar of PR #373 — already superseded on main by
  Run | Agents | Context (#379); nothing here reinstates it.
- The captured `factory-ui` demo as the Factory centre (per the 2026-09-18
  commission): every depth now binds the owner's real readings.

| M1 · Tested migration of old persisted workspace state (§7) | `workspace/store.ts` legacy adoption + `surface/persist.ts` lenient codec (pre-existing) | localStorage `oi-cradle.layout.v1` → v2 book | the legacy top-level pin state upgrades to per-pane presentations at adoption | lenient decode drops foreign/invalid parts without crashing the shell | `walk/persist-migration-probe.mjs` (legacy draft restores, "list" → pinned-vertical in the base tree, foreign fields drop, closed stack valid, no errors) | **verified 2026-09-18** |

| F5 · The standing factory-development walk drives the Desk (handoff §10 known seam) | Worker E (`a0fd39c8`): the scenario now drives the production Desk through the walk bridge — 18/18 green | real owner CLI through the bridge; owner refusals verbatim; exact-identity conversation law | the board's coverage/refusal bands + Tasks centre | scope/search survive round-trips | `walk/run.mjs factory-development` ×3 + self-contained `walk/desk-probe.mjs` (bridge-booted, all four depths, Map refusing through the real owner) | **landed 2026-09-18** |
| F6 · Kernel build_snapshot direct-binary route (E's finding 1) | `kernel/src/factory.rs` + `lib.rs` (working tree, deliberately uncommitted with the owner's kernel state): the FactoryBuildSnapshot arm now resolves suite-route vs direct-binary exactly like the development arm — `OI_FACTORY_BIN` no longer hears the `factory` prefix twice (`factory factory build snapshot …`) | the kernel op is schema-verified as before | kernel arm only — no UI change | — | `cargo check` clean; the scenario's suite route unaffected (18/18) | **landed 2026-09-18** (working tree) |
| B2 · Day/Flow document joins verified; DayDieFace hardened | Worker E: forms resolve through the O-I project path with Work/O-I fallback; the die face now joins by roster identity (kind+file), not roster position | kernel file ops; build-time template | `receiving/DayDieFace.tsx` | reordering the forms roster can no longer silently re-shell the Day | `day-edit` walk 8/8 | **landed 2026-09-18** |

**The Desk read-pairing finding, resolved honestly:** the board enumerates Runs through `factory development project` (developmental schema) but per-Run build views exist only as `factory build snapshot` (build-local schema) — one developmental state path cannot serve both on the installed owner CLI, and `factory development build` (the would-be pairing read) is refused by the installed owner ("unknown command 'build'"). The board therefore renders the owner's own refusal verbatim per Run — partial coverage, never a fabricated card — and the real fix is the owner CLI growing the developmental build read (named: Factory owner). Proven by the 18/18 scenario's refusal legs.

| E3 · Expressions hosts the O:I Expressions application itself — the Point-Cloud-Demo workspace full-screen with ITS UI, Library and engine (owner direction 2026-09-19) | Work/Point-Cloud-Demo `dist/` (the #329 intake line's application) | the owner's `oi-material://` file seam (FND-04) under Tauri; the walk bridge's mirror under probes; honest absence in a plain browser | `expressions/PointCloudHost.tsx` as the Expressions centre (ExpressionsSurface superseded as the centre face, retained for its parts) | the application ships its own engine bundle and expression corpus (M1–M3 body); deeper kernel joins named, not faked | `walk/expressions-host-probe.mjs` | **landed 2026-09-19** |
| T2 · Instrument 0 is the source-backed Wiki→Expression projection (supersedes the wiki-list landing the same day — the list was the generic browser the owner rejected) | Worker H `50430a43`: `techne/wikiExpression.ts` (pure projection) + `WikiExpressionBody.tsx` | wiki.json through the files seam + live `knowledge relations` (typed edges, provider/authority/revision); deterministic `oi.expression/v1` document opened in the kernel; stage presentation under the shared id; content-addressed identity (`expression:techne-m0.<register>.<basis-hash>`); QL sixfold layout as presentation warrant only, engaged only where the wiki declares positions — zero invented edges (adrift endpoints named) | Technè M0′: overview scene with the register's constellations as addressable objects → constellation scenes with actual nodes and typed relations → node focus on canonical refs → page/source through the frame's open paths → position restored from the kernel document | live-native end to end; no fixtures | `walk/techne-m0-wiki-expression-probe.mjs` over the walk bridge on real ground; tabs/mode-sweep/epi probes green | **landed 2026-09-19** — owner seams since resolved by the owner's 2026-09-19 rulings: portal-inside-Technè was a stale kernel disclosure (the expression-world seam IS the portal runtime — capabilities corrected, the page opens as a real pane placement in the current mode's tree) and the cross-mode knowledge-open handler now leaves the same reading trail sources do; still named: SharedField staging (EX3A6 unadmitted) and a racy `oi:epi-open-source` layout placement (reproduced 3×, recorded). 2026-09-19 second pass: the dedicated mode stage is UNCONDITIONAL (owner ruling) — the knowledge page's kernel surface and mode-tree placement are the truth the probe asserts (the panel's Active Context is the surfacing seam); the m0 probe re-lawed accordingly and is green |
| C2 · The mode's panel sides reachable from the chat face (owner finding 2026-09-19: "panels not properly folded on") | companion-lane law: the chat face is the rest, the sides continuous grounds | AgentLayer's offered planes + PlaneNav | the chat face carries the mode's side planes as its own slim strip; selecting one turns the face onto it | conversation state survives (one session, kept mounted) | panel probe (sides listed for expressions/techne, selection flips face to the plane; Ta-Onta content renders) | **landed 2026-09-19** |
| T3 · ONE Wiki→Expression projection state — the Technè left body's LIST/TREE/GRAPH apertures and the Expressions graph navigator read from and drive the SAME projection Instrument 0 presents (QL-MEF #213 one relation/selection state; #366 bidirectional law; owner continuation 2026-09-19) | this thread: `techne/wikiProjectionStore.ts` (the one state: per-register standings reading→projection→standing kernel document, selection mirror, aperture requests); `WikiExpressionBody.tsx` is the only kernel actor (ensure/open/focus with one revision-conflict re-apply) and publishes its selection through `selection.ts`; `WikiMapNavigator.tsx` rebuilt as apertures over the store — LIST flat index, TREE nested, GRAPH drawn from the document's own positions and bound relations; no second wiki.json read anywhere, no parallel navigator law; a map/graph row click is a kernel focus edit (`requestWikiSelection` → centre consumes); the centre's focus marks the same canonical row selected in the map; cross-mode: the Expressions navigator's ask on an `expression:techne-m0.*` row enters Technè and focuses the same entity (`wikiRegisterOwning` resolves the owning register, the ask brings the projection forward); the Epii face carries Lane M's kernel channel relay | oi.expression/v1 documents in the kernel — the projection identity is the store of record; the store mirrors, never copies | Technè left body (three apertures) + Instrument 0 centre + Expressions graph navigator | selection lives in the kernel document; aperture requests consume at-most-once; a request aimed at a superseded generation is refused by name; projection currentness is cached per register this session (file_changed-driven invalidation is the named seam) | `walk/techne-m0-unified-apertures-probe.mjs` ALL CHECKS PASSED (map renders the projection's own members with canonical refs from the real wiki.json; sidebar→centre focus; centre→sidebar selection; LIST + GRAPH over the same state incl. a graph-node focus; projecting another register from the map; the cross-mode ask enters Technè and focuses the same canonical entity); `walk/techne-m0-wiki-expression-probe.mjs` all checks; `walk/mode-sweep-probe.mjs` green; tsc clean | **landed 2026-09-19** |
| L1 · The two navigation horizons inside the REAL Expressions Library (owner spec: augment the established interaction, never a second Library) | Lane L `058904d1`: `libraryScopes.ts` (new) + `libraryHTML` augmented; the horizons are scope controls at the top of the one Library body reusing the app's own `library-section` action | MY WORLD / PERSONAL (default): current-project, project-collections, personal-collections, kernel-held, curated-corpus scopes in the existing card grammar; O:I WEB / SHARED: the verbatim named unavailable state ("Projected worlds are not reachable from this application yet — the SharedField provider is the native owner"), zero invented results | kernel-held rows consume Lane M's pinned API feature-detected, carry native `expression_ref`s, and join the existing search — native subjects only, no copied database | `walk/library-horizons-probe.mjs` 20 checks ALL OK; expressions-app-vendored probe green; app build type-clean | **landed 2026-09-19** — named seams: opening a kernel-held expression into the editor (app.ts, Lane M/N); the horizons' CSS ships as an emitted `<style>` block (fold into workspace.css when the whitelist opens) |
| K1 · The kernel's expression ops meet the vendored app's engine/documents — one store, no second copy (owner continuation Lane M) | Lane M `9b8c5e19`: host channel `relayKernelChannel` (hostedApp.ts + PointCloudHost) — versioned envelope, kernel-expression ops list/inspect/create/edit (the kernel document IS the store) + generic central-read through the files seam; app-side `kernelExpressions.ts` pinned API (available/list/read/save with diff-based edits at the document's expected_revision; conflicts return `{ok:false}`, never force-written) + `kernelDocumentBridge.ts` oi.expression/v1→oi.journey conversion with named notes for everything unmapped; the Technè Epii face hosts the same relay (parent stitch) | `walk/expressions-kernel-roundtrip-probe.mjs` green (list cross-checked against the kernel's own op; save round-trip verified; stale save refused honestly; no localStorage/IndexedDB copy of kernel documents); cradle tsc clean | **landed 2026-09-19** — named seams: Library-UI wiring of `openKernelExpressionIntoView` (Lane L's file); `?expression=` boot lookup for kernel-only refs; kernel ops open/fork/save-to-file/propose/review/invoke not relayed on this cut |
| C3 · Collection manifests close D1 + D3 so Central-backed collection readings can serve the Library (owner continuation Lane N) | Lane N `d58ae390`: export script gains the `oi.collection-provenance/v1` envelope (register/root/paths/ground/exported_at/generator; old manifests still import; unknown envelopes carried verbatim); `model.ts` + `collectionManifest.ts` add `importCollectionManifest` (membership in manifest order, every failure named at its slot — `restored + failed === membership.length` is structural, zero silent drops); cradle-side `collectionReadings.ts` (`readCollection` over the files seam: ready / manifest-absent / the owner's refusal verbatim; pure core feedable by Lane M's relay identically) | `walk/collection-manifest-probe.mjs` 28/28 (envelope fields; 40/40 round-trip order intact; pre-envelope manifest imports; unreadable member named at its slot; retained default + light/dark variant proven live on the regenerated dist; real Central read through the files seam); cradle + app tsc clean | **landed 2026-09-19** — named seams: the app's file-input import still calls single-document `importDocuments` (nativeBridge.ts/app.ts integration); Library-UI consumption of the three bridge states |
| R2 · The three named shell leftovers (retention law R1's follow-ups) | Lane O `2f6ea477`: (1) Factory centre retention — `factory` joins `RETAINED_CENTRE_KINDS`; the frame's chat node rides down through DesktopShell into `ModeCentreRetention`; one `FactoryCentre` body, same adopt/release law, no park re-implementation (the named frame dependency is CLOSED); (2) `ArrangementActions` RENDERED in the footer row (it was composed and passed but never presented — the pane tools already defer to it); split/maximize operable, declared disabled law on empty workspaces kept; (3) the files walk re-lawed to the owner's current hover-reveal grammar; plus one minimal shell repair named under the lane's clause: MaterialSurface's `view==="source"` early return stood above the component's hooks and crashed the subtree on first Source toggle | `walk/retention-probe.mjs` 29/29 (new C2 leg: Factory parks the SAME node and returns it); `walk/arrangement-actions-probe.mjs` 19/19; `SKIP_BUILD=1 node walk/run.mjs files` 23/23; mode-sweep + footer + reveal green; tsc clean | **landed 2026-09-19** — named remainders: a centre binding living only in `layout.sidePane` is not declared by the park (pre-existing trust edge, now shared by five kinds); ArrangementActions' disabled-on-empty maximize law differs from the keyboard path's (kept as declared) |
| W2 · The 13-step connected walk (owner spec "Do not hide behind fixtures") — evidence recorded per portion | this thread: `walk/expression-walk-13-step.mjs` over the walk bridge on real Central ground; artifacts `walk/artifacts/expression-walk/` (8 screenshots + record.json) | ALL 13 STEPS WALKED, no console errors: 1 Expressions loads the real hosted application (live-native); 2 the real Library opens (live-native); 3 MY WORLD scopes + O:I WEB named-unavailable (live-native + named-unavailable); 4–6 the real ProjectCentral whole (O-I) opens as an oi.expression/v1 Expression — overview constellations, exact nodes (live-native; O-I's thin wiki truthfully places its one entry as the whole); 7–8 canonical-ref focus and the one selection across map and centre (live-native); 9 page: kernel surface + mode-tree placement under the owner's second-pass dedicated-stage ruling (live-native); source: proven by the m0 probe's cycle, this walk's run did not grow surfaces (the named placement race, recorded); 10 return restores register/scene/selection (live-native); 11 Explore opens as O:I Web's discovery operation (live-native shell, shared scope honest); 12 SharedField staging named-unadmitted (#366 EX3A6) — one projection identity, no second public graph (named-unavailable); 13 search resolves native subjects (live-native) | **walked 2026-09-19** |

| E4 · The application vendored into the repo — built inside, all owner directives executed (2026-09-19) | Worker I `a42e7ea6`: `desktop/cradle/expressions-app/` (source of record; Work/Point-Cloud-Demo retired as the app of record; one build law in its README; served through the same files seam by Central-relative path — no copy-sync) | the files seam + `?expression=` deep-link grammar through the material protocol | inside the app: `oiMark()` default (one expression, two scenes — Day ink-on-paper / Night light-in-the-dark, matching the base O:I image), orbit anchor bottom-left with menu-relative reveal kept, collections exported to `legacy-collections/` (3 featured + 37 starters as oi.journey data; oi-mark + source-twelve-faces retained non-legacy), masthead aligned to the shell's window cutout (live `oi-shell-cutout` posted geometry, icons 24px); Instrument 0 opens onto the Epii face — `source-twelve-faces` ("Twelve faces · one mask", the app's own corpus) via deep-link, stepping into the wiki·graph whole projection | the app is the app of record — no outer-repo serving | `walk/expressions-app-vendored-probe.mjs` ALL OK + `techne-m0-wiki-expression-probe` 24 checks incl. the face entry; cradle tsc clean for the lane | **landed 2026-09-19** — collections/library refit seams named: M0-M5 production manifest D1 (no source/provenance envelope in oi.journey) + D3 (no collection semantics at import) are the gaps a Central-backed collection reading must close |
| S1 · Workspace load failure rides the footer; one click reloads | `f92c5e3b` — no more recovery-workspace route | store keeps the last good book; footer message + dismissal both reload | footer status system | protected-bytes retry retained | footer probe + manual | **landed 2026-09-19** |

| R1 · The three-tier retention law (owner-approved spec, 2026-09-19) | Worker K `766cc322`: panes concealed-retained (every open tab keeps its body — same DOM node — through tab switches; only `sources`/`blank` release; close releases all); file-tree listing store (workspace-keyed, kernel `file_changed` receipts invalidate — one event per owner-recorded write), collapsed folders keep children mounted, ONE tree-level reading indicator; mode centres retained through a portal park (expressions/techne/epi-logos/system — the expressions iframe is the same node across mode switches); per-workspace quarantine (a broken workspace loads empty and is named in the footer, never rejects the book) | kernel `FileChanged` event (events.rs, tested); the existing viewport/suspension laws | `surface/Workbench.tsx` (concealment), `files/listingStore.ts` + FileTree, `surface/retention.tsx` (the park) via DesktopShell, `workspace/store.ts` (quarantine) | tiers: active → retained(concealed, suspended by the existing display:none laws) → released(close); persisted layout untouched — this is runtime retention | `walk/retention-probe.mjs` 26/26 (same-node round trips, zero re-reads on re-expansion, iframe parked/adopted, quarantine named in footer, live file_changed invalidation); kernel lib 59/59; mode-sweep + footer green | **landed 2026-09-19** — named frame dependency: Factory's centre (the frame-built chat node must be passed down before Factory retains) |
| F6b · Dead-tree reporting: boundary renders no UI except one revive glyph | `5467a582`/`331bcd57` — render failure rides the footer workspace-messages route; one automatic keyed-Fragment remount; repeated failure leaves one 24px glyph (footer corner/vocabulary, tooltip = reason, click reloads) | `oi:workspace-message` sink | `Cradle.tsx` FrameBoundary | the shell owns layout (no wrapper elements) | footer probe + manual | **landed 2026-09-19** |

## Pre-existing findings (named, not introduced by this landing)

- `walk/tab-pane-probe.mjs`'s orient-tool "intercept" was probe drift, not a
  shell defect: the strip was legitimately FOLDED (unpinned — the hiding
  law), and its hidden tool sat under the left region's edge. The probe now
  approaches the reveal edge before clicking; resolved 2026-09-18. The
  horizontal fold keeps a 32px reveal edge (the vertical fold's counterpart)
  — recorded as the designed reveal, not a remainder defect.
- The ephemeral-vite-server probe pattern (`createServer({configFile:false})`)
  can no longer resolve the workspace engine deps (`three` from
  `packages/oi-design-system/expressions-engine`) since the engine entered
  the eager entry graph; `footer-probe` now reads the standing dev server
  like the other walks. Any probe still spinning its own config-less server
  will hit the same wall.
- The `factory-development` walk scenario still drives the pre-Desk
  development console (named in the handoff §10 known seams) — rewriting it
  onto Desk needs the walk bridge lane.

## Protocol for subsequent increments

1. Identify the new-UI receiving component and interaction.
2. Connect the real owner read/Action through the existing boundary
   (`development.ts`, kernel transport, encounter ops).
3. Preserve state, authority, error and lifecycle semantics (owner
   validators; honest refused states; no synthesised success).
4. Test the interaction in the NEW app entrypoint (dev probes against the
   live shell; fixtures labelled, never native claims).
5. Update this table and publish the increment (local commits on the
   convergence line; PR #370 remains the owner's).

## Session increment — 2026-09-19 late (ab053b87): the projection follows the wiki

| Obligation | Landed | Evidence | Standing |
|---|---|---|---|
| Projection currentness (the remainer the 13-step walk named: "cached per register this session") | `wikiProjectionStore.ts` invalidates a register's cached reading on a kernel `file_changed` receipt whose path is that register's wiki basis — stale-while-revalidate (the files broker's own law), fresh readings re-stand as projected (the centre opens the new content-addressed generation), absent lands honest absence, a refused re-read under a standing document is named as drift. The store's receipt function rides the shell's existing kernel-receipt feed beside the files broker (`CradleFrame.tsx`, two hunks only). | `tests/techne-m0-currentness.test.mjs` 5/5 over the real wire grammar (stub bridge answering the actual `files_list`/`file_read`/`knowledge` ops — the owner's real wiki.json is owner CAS territory no probe may write); `tsc --noEmit` clean tree-wide at commit time. | landed |
| Installed-cut verification (Mac) | Built the release `.app` from the live tree (HEAD `ab053b87` + the lanes' uncommitted refinements — the authoritative UI state), packaged `oi.desktop-bundle/v1` `aarch64-apple-darwin` (sha256 `7c11bea5…`), installed through `oi desktop install` (receipt `oi.installed-desktop/v1`, executable `~/Applications/O-I.app/Contents/MacOS/O-I` — renamed from tauri's `oi-cradle` + ad-hoc re-signed to satisfy the footprint's `exec_relative` contract; the previous installed cut named `oi-cradle` and the current installer refuses that). Verified: `oi desktop status` installed/present, `oi verify` PASS, launch → welcome → shell over the real ground (real project list, restored workspace, honest read-failure + Retry), the v2 Settings surface rendering live (search + @filters, tray law, source/effective/active disclosure, Workcell's 10 settings) with the agent plane beside it. Screenshots: `/tmp/oi-window.png` (welcome), `/tmp/oi-window4.png` (live settings). | NAMED DEFECTS, not fixed here: (1) the installed app's first window opened 116×142 instead of the configured 1280×820 — resize recovers; suspect the dirty `transparent:true`+`windowEffects` conf (known bundled-build transparency gap, Tauri #13415 area) — owner's vibrancy lane. (2) `install-footprint.json` still says `exec_relative: Contents/MacOS/O-I` while tauri builds `oi-cradle` — the bundle now conforms to the footprint, but the producer should name the binary `O-I` natively (build-stamp lane). (3) `package-bundle.sh` refuses real builds off linux while a darwin bundle demonstrably installs — the darwin packaging path deserves its own script. | verified with named defects |
| Stale probes named | `walk/techne-m0-unified-apertures-probe.mjs`, `walk/techne-m0-wiki-expression-probe.mjs`, `walk/techne-m0-currentness`'s DOM siblings and `walk/techne-convergence-walk.mjs` wait for selectors the in-flight dual-mode refactor removed (`.wx-face-frame`, `.wiki-expression`, `.tn-surface` — TechneSurface/TechneHud/dualMode are deleted live, centres unified on `PointCloudHost`). Their re-law belongs to the lane that landed that refactor; the unit-level currentness test above is selector-independent. | Probe runs + live-tree diff, 2026-09-19 ~22:00. | named, owned elsewhere |

## Session increment — 2026-09-19 night (8a68bbee + 0f898538): the settings page answers for itself

| Obligation | Landed | Evidence | Standing |
|---|---|---|---|
| O:I answers the configuration registry | `oi config-contribution --json` (read-only `oi.configuration-contribution/v1`: suite executable, managed root, build record, containing frame, install mode, present positions, update state; operations refused with the named reason, every setting `writable:false`) and `oi system --json` (`oi.product-settings-disclosure/v2` axes for the same facts). The kernel's registry read addresses owner `oi` through exactly these verbs; before this commit the mount degraded and O:I's own settings could never reconcile live. | `cargo test` cli 289 green; live end-to-end `oi config show oi:identity:build-record world --json` → real axes, reconciliation `satisfied`; `walk/verify-live-settings.mjs` on the production bundle: no fixture source, five answering owner groups (oi, central, ai-kit, software-factory, quaternal-logic) — ai-kit and QL woke via the same night's `oi update --apply --channel mainline`. | landed |
| Settings page legibility | Copy cut to states, not lectures (page-head tagline, absent/empty-owner paragraphs, tray sentence, the repeated per-row "Can't be changed here" line — the View only pill already says it); type hierarchy restored (row titles in ink and weight, values at body size in ink, descriptions quiet), rows and section boxes gain real padding, content column widens. | `tsc --noEmit` clean; production-bundle walk screenshot `/tmp/live-settings.png`. | landed |
| Installed cut re-landed | Rebuilt, packaged (`oi.desktop-bundle/v1` sha256 `d753f759…`, source `0f898538`), installed via `oi desktop install` (receipt names `~/Applications/O-I.app/Contents/MacOS/O-I`). | `oi desktop status` installed/present. | landed |
| Named defects (unchanged, now confirmed twice) | (1) the installed app's first window opens ~116×142–147×174 instead of 1280×820 — resize recovers; vibrancy/`transparent` conf is the suspect (owner lane). (2) tauri names the binary `oi-cradle` while the footprint requires `O-I` — packaging renames + re-signs; producer should natively stamp `O-I` (build-stamp lane). (3) `dist/` is a shared moving target across lanes: a concurrent `WALK=1` build can land in the .app (this happened once and was visible as the fixture banner in the installed app) — the packager should build dist into a private out-dir or verify the walk flag post-build. | Reproduction notes, 2026-09-19 evening. | named, owned elsewhere |
