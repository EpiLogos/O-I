# Epi-Logos mode: the situated Prime–QL agent in Expressions and Technē

**Owner commission:** 22 September 2026. **Standing:** architecture and implementation/test assignment, not an installed-runtime or human-experience verdict. **Campaign:** existing #65 C0–C5; **integration:** #220. **Native body:** [Actuation #107](https://github.com/EpiLogos/Actuation/issues/107); **domain:** QL-MEF #201/#135; **general preparation:** AIKit #388.

Read [Actuation's body architecture](https://github.com/EpiLogos/Actuation/blob/main/docs/EPI-LOGOS-AGENT-ARCHITECTURE.md) and [QL-MEF's full faculty architecture](https://github.com/EpiLogos/QL-MEF/blob/main/docs/integrations/epi-logos/EPI-LOGOS-AGENT-ARCHITECTURE.md) as complementary owners. This is a subordinate mode-quality contribution to [Expression Field](EXPRESSION-FIELD.md), the current Technē experience and [session grounding](SESSION-GROUNDING.md), not a new shell, campaign or plan registry.

## 1. What selecting the mode means

In both **Expressions** and **Technē**, the existing **Epi-Logos mode button** selects the source-owned **Prime–QL agent/body composition as the default option** for the mode's agent. The selected subject, source, scene and work remain themselves. The change is the operative intelligence available to engage them—not merely an icon, colour palette, prompt slogan or replacement demo application.

The containing `#0/1` is the actual agent loop, Prime body, relational orientation and QL-formed tools. **`#0/1 != #0`.** Explicit `#0` contains the full Anuttara/Bimba 109-node syntax and its Jev/Ananda-related/EBM basis; #1–#5 remain explicit structural/TDA, semantic-graph/ML, multimodal/process, protected Nara activity/NLP, and Logos-cycle pedagogical/developmental faculties. QL-MEF owns their meaning; Actuation owns the acting-body composition.

S4′ is Anima and S5′ is Aletheia. Nara is M4/M4′; Epii is M5/M5′. Preserve existing Nara foreground and Epii enrichment/development participation where the encounter calls for them. A body selection does not mint a different enduring Agent. Prime need not impersonate a realtime voice transport; voice/body capability and interruption remain with their actual native providers.

The first integration candidate uses the full twelve-tool reference composition. This is a proposed initial configuration for the complete-mode trial, not a finding that twelve is optimal. Preserve independent 4/6/8/12, prompt-only/tool-only and Prime-recursion comparisons. The actual profile reference, model and tool/faculty revisions must be resolved from native source, not invented by this planning note. Domain repertoires are not permanently limited to the four filesystem commands of a research fixture.

## 2. Selection, activation and restoration

Use existing native Agent/Profile/HarnessComposition, setup, context, SessionSpace and application mode state. Do not create a React-local agent registry, second session store, new capability taxonomy or parallel source graph.

**Mode off:** ordinary Expressions/Technē remains useful with its legitimate existing agent/harness selection and without mandatory QL/Jev/EBM. Hosted read-only Library visitors still need no local agent or API key.

**Mode selected:** resolve the authored Epi-Logos default within the current World/Project/NOW and applicable consent/authority. Display the actual semantic actor, Prime/native body, model/provider, prompt/tool/QL/faculty source basis and readiness. Preserve a deliberate user override as an override; do not silently overwrite it with the default or falsely label it the default composition.

**Activation:** enter or resume through a supported native boundary and show observed runtime state. Distinguish selected, resolved, delivered and actually invoked. A control being highlighted, a config file or an agent saying it loaded the prompt does not establish activation. Missing mandatory Prime/QL/profile capabilities produce an intelligible unavailable/repair choice. Optional analytical/learning faculties disclose their actual availability separately. No silent fallback to a generic agent while displaying active Epi-Logos intelligence.

**Mode change during work:** preserve the actual attempt and its source basis. Do not hot-swap an in-flight model, change authority or apply late scene operations from the previous encounter. Use the supported pause/stop/turn/session transition with explicit continuation or cancellation. Preserve the prior ordinary selection for restoration. Repeated toggles are idempotent and do not multiply workers, subscriptions or worktrees.

**Reopen/restart:** recover the same selected subject/scene and the mode preference, then re-resolve actual runtime readiness; a stale persisted selection must not claim a live process. Preserve current task and minimal NOW continuation. A new session can continue one Agent's work without becoming a new Agent or a new Factory Run.

The button does not acquire credentials, change global harness defaults, auto-train/adapt models, mutate canonical Bimba, admit private material to external services, start every Guardian or perform another clean-origin reset. Explicit native operations retain their normal permissions.

## 3. Actual source-to-expression operation

The full 109-node Anuttara computation and current experimental instruments remain available at #0. C′/Ta-Onta is the already-defined contextual SDK/API for real Expression/scene activity, not a replacement for that language. The actor must use source-qualified faculties on the same object the person sees.

The joined path is:

```text
existing mode control + selected subject/scene
→ native Epi-Logos default composition
→ prepared participant context and actual harness delivery
→ Prime–QL work with relevant #0–#5 faculties
→ permitted Ta-Onta / Expression / Technē native operation
→ actual visible/source effect and attributable Return
→ later source/practice use
```

Bind Agent/Nara, Agency, NOW, SessionSpace, provider session, Expression/profile/source revisions and Factory unit/attempt only where actually present. Direct work remains Direct. The app's existing Run/Agents/Context and source/selection/inspect routes receive the native records; no second context dashboard or model-loop timeline.

Consume [Jev/Redis NOW integration](JEV-REDIS-NOW-INTEGRATION.md) and AIKit #388 for general invocation, prepared context, changed-source delivery and recovery. Central/BKMR locates source; native Wiki/knowledge keeps understanding; Redis holds the working preparation. Domain-specific Jev questions and QL/EBM readings use that route instead of a second credential/cache/memory service. Warm reads do not require a fresh model call on every turn.

Preserve typed observations and exact source/asset/algorithm versions for TDA, graph semantics/analytics/embeddings, multimodal glyph/scene interpretation, Nara NLP/weighting and Logos improvement. Instrument disagreement, uncertainty and missing inputs are meaningful, not automatically failed user experiences. Energy or model confidence never becomes a universal source/permission/closure verdict.

For Nara, preserve source spans, interpreted contribution strength versus confidence, native elemental remapping and activity decay, with no automatic identity rewrite. Raw protected material remains scoped; external Jev use needs actual egress authority. For M5, the Logos cycle includes T/C/T′/C′ and learned practice; the etymological Atelier is one method, not the whole Return.

## 4. A parallel lane inside the current harmonisation

The owner explicitly admits this as a **parallel integration lane for mode quality**. The local lead can perform it in the **existing main feature tree** with bounded file claims. It does not request another checkout or rebase every other ongoing contribution. All shared Git/index/checkout/commit, install and service changes remain serialized by the same lead; native owners retain their assigned source trees.

Actuation #107 develops the body and child integration; QL-MEF #201 supplies full domain faculties and Ta-Onta bindings; the existing AIKit composition/admission work joins the actual default and #388 context services; O:I #220 joins the real mode and receiving UI. Workcell supplies actual provider/service material, Central source/NOW continuity, Factory the commissioned developmental path. No new six-product census or competing source/telemetry registry.

Native construction can proceed alongside the existing Jev/Redis and Factory bring-up. This lane does **not** make advanced QL/EBM/TDA/model-training work a prerequisite for #388's first ordinary Factory/NOW proof. Preserve the already-defined foundation condition before broad fan-out; publication itself launches no workers and mutates no machines. Reuse the current single harmonisation and preserve its newly produced evidence.

Inside the QL Wayfinder, this is a contribution to TA0/TA2, TA3/TA4 and TA6/TA7, not reopening accepted K/AW/SF/EX or replacing the wider corpus/shared-world/voice obligations. Candidate changes are admitted explicitly into the lead's source vector rather than testing against a hidden branch or older donor UI.

## 5. Joined testing through the existing stories

`vak-expression-craft.json` retains its original seven `vakcraft65:*` obligations and every existing story ID. This note is its `document_operations_source`, so the existing compiler retains the complete design and digest. Additional mode branches attach to those same obligations; they do not create another story family or evidence scale.

**C0/C1:** bind the full source and native capability field, selected episode, actor/context/authority, exact source/build/install/running cut and real default profile. Independent verifier inputs do not include the implementer's expected conclusions.

**C2:** in the actual installed current UI, enter Expressions, select an existing source-backed subject, select Epi-Logos and perform a useful agent task. Read back actual Prime–QL activation, relevant #0 operation, native scene/source effect and Return. Repeat entry and useful operation through Technē. Merely calling the same CLI while testing a different app build is not receiving-UI proof.

**C3:** delegate a genuinely useful bounded contribution to a real inexpensive eligible child through the admitted harness. Verify inherited faculties, scoped context and persistent worktree allocation. Independently attempt forbidden extra-worktree creation under the worker boundary; legitimate edit/test work must remain functional. Finish one partial contribution, record a pithy native NOW handover, replace the worker and continue from it without the parent reconstructing the investigation. Exercise a relevant source/Return change, view closure/re-entry, supported restart and interruption with a pending scene operation.

**C4/C5:** retain a failure and uncertain effects, repair its native owner, update the candidate through the lead and independently replay the complete affected act. Consume one warranted result into native Wiki/account/matrix/Method knowledge and show a fresh participant using it on a new input. Human experience assessment remains real and separately attributable, not inferred from source CI or agent approval.

Required negative branches are explicit:

- selected-but-not-running; missing required Prime/QL/profile; unavailable optional Jev/EBM; malformed or stale instrument answer; explicit alternative is not silently relabelled;
- mode off restores ordinary functionality without retained global injections, a new Agent or lost work; user override survives appropriate re-entry;
- repeated mode toggles do not duplicate actors/worktrees; changing mode does not silently retarget an in-flight attempt;
- late enrichment against an obsolete Expression/source basis cannot auto-apply; rejected proposal leaves state unchanged; accepted operation is independently observed;
- child omission of inherited faculty, leaking sibling/verifier/private context, conflicting claim, failed handover and stale resume all remain failures;
- all #0–#5 required coverage remains mapped, including meaningful TDA input/provenance, graph/inference/embedding distinction, multimodal ambiguity, Nara unknown/confidence/decay and Logos near-miss/transfer. A sample happy path cannot close the whole lane;
- authored source, analytical readings, execution evidence, lived response and human Recognition retain their standing; no trained voice or EBM result silently changes canonical identity.

Compare the useful complete-mode composition with relevant ordinary and research controls on the same source/task basis. Count preparation, actor/child inference, retries, verification, latency/cost and human assistance, and retain negative/inconclusive findings. A differentiating product experience is the research aim, not a conclusion supplied by this document.

## 6. Source checks, execution trigger and truthful return

Run the current source suite and compiler after mapping changes:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests/experience -p 'test_*.py' -v
python3 scripts/experience_map.py
```

The mode-specific source tests must detect a disconnected note, loss of the explicit #0 faculty or S′ identities, dropped mapped mode branches and confusion between declared selection and runtime acceptance. They test documentation/compiler integrity only. Actual provider, child, material/UI and human tests are the implementation assignment above.

**Execution trigger:** #220's current lead claims this bounded mode-quality contribution in the existing tree and consumes Actuation #107 / QL-MEF #201 plus #388's available contracts. Implement and integrate the missing native composition/default/receiving joins, then perform the two-mode episode as soon as their real dependencies are usable. Do not start another harmonisation/reset, force every research pipeline to completion before any useful mode trial, or declare the whole lane complete from one small demonstration.

Return through the existing #65 round and owner tickets: exact source/profile/tool/model/runtime basis; requested/resolved/delivered/used state; mode/default/override/off/re-entry behaviour; actual #0–#5 coverage; child/worktree/handover evidence; failures and repairs; fresh use of warranted Return; remaining exact gaps and human assessment. No private transcript, credentials or machine configuration is published in public evidence.

## 7. Implemented default route — 2026-09-22

The receiving implementation is published in [#477](https://github.com/EpiLogos/O-I/pull/477), consuming AIKit [#391](https://github.com/EpiLogos/ai-kit/pull/391), Actuation [#110](https://github.com/EpiLogos/Actuation/pull/110) and QL-MEF [#241](https://github.com/EpiLogos/QL-MEF/pull/241).

There is one mode law: while the current World is `epi-logos`, the Epi-Logos, Expressions and Technē apertures supply `agent-body/epi-prime-ql` as the default **for a new conversation**. Merely selecting or navigating the mode starts no process, acquires no credential and does not replace an existing encounter. The ordinary first-Send path creates the existing SessionSpace/AgentSession/Agency relation, asks AIKit for the body, opens the ordinary Encounter provider and requires the open receipt to read back the same body revision.

Provider precedence stays explicit. A person's held provider choice wins even if it is not Prime-QL, and is never relabelled as the mode body. Without such an override, absence of an exact Prime body is an actionable unavailable state rather than a hidden generic fallback. Ordinary Expressions/Technē outside the Epi world do not acquire the body. Existing encounter retention, draft, cancellation, prepared-context and Run/Agents/Context surfaces are reused unchanged.

The branch's complete triggered desktop/kernel suite is green, including Agent-session-native, O:I desktop, Technē constructive field, Native Expression backcheck, Canvas/context, shell recovery, adoption, Wiki integration and O:I Verify. That proves the receiving application and state law on the cloud cut; it does not assert installation on the owner's machines, a live Prime/Jev/EBM turn or human acceptance.

The general Jev/Redis/NOW implementation remains owned by AIKit #388. This mode consumes the same Encounter prepared-context seam; until #388 publishes its runtime provider, Jev preparation is a dependency standing, not a fixture renamed as live context.


### Native continuation follow-up

The descendant continuation mechanism now uses Central's existing
`projectcentral.now.return` owner rather than a chat transcript or an
Actuation-local handoff file. When the installed Prime provider is configured
with the optional Central `ctrl` binary/root/project inputs, the inherited
`ql-relational` Skill can write a pithy
`central.project-now.handoff/v1` record carrying the actual session,
source/evidence/preserve refs and repo/branch/optional-worktree lane claim. A
fresh cheapest-eligible Prime child re-reads that exact Central record through
NOW inspection and continues from its refs; the parent does not reconstruct the
investigation.

The installed acceptance runner in AIKit exercises this branch when its Central
and lane inputs are supplied. It separately retains cancellation/same-session
continuation and child-owned QL faculty receipt checks.

One material branch is still deliberately not called complete: Prime Agent
0.9.4 owns `rlm.run` child creation and exposes no per-child supervisor hook
through which Workcell's write boundary can be applied before exec. Workcell
does have a real Linux Landlock process-tree boundary, but wrapping the whole
Prime body would not prove independently assigned child worktrees, and a
writable worker directory alone cannot prove that an editable full-tree copy
was impossible. Until Prime exposes that host seam (or a Workcell-owned worker
session replaces the child launch), the installed verifier names
OS/worktree-confinement as unproved rather than using a command-name filter.
