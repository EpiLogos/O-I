# Inhabited system orientation — how to enter, develop and test the whole

**Standing:** owner-directed orientation/index, 16 September 2026.  
**Campaign owner:** O:I #65 and `docs/experience/README.md`.  
**Purpose:** keep the person and fresh Agents oriented across the final parallel development campaigns without creating another master architecture, evidence store or acceptance programme.

This document does not replace the UX specifications, product Wayfinders or native implementation owners. It tells an entrant **which world they are in, which sources govern the experience, which campaign a piece of work belongs to, and how the two active development campaigns converge into the one inhabited-system proving channel.**

The target is now singular:

> **an inhabited, self-hosting O:I development world which can take its own remaining acceptance backlog as work.**

That is the threshold between constructing the organs of the system and living inside the system while reality hardens it.

---

## 1 — Start from experience, not the issue list

The authoritative order is:

```text
human / Agent intended experience
        ↓
product and system architecture
        ↓
native contracts / ownership
        ↓
current implementation
        ↓
performed evidence
        ↓
Return / repair / Recognition
```

A current implementation tells us what exists now. It does not redefine why the system exists. A test tells us what one relation did at one cut. It does not silently shrink the intended experience.

For orientation, read only to the depth the present work requires:

### Whole-product / ordinary UX

- `docs/positions/FOUNDING-POSITIONS.md` — authored product meaning.
- `docs/cradle/03-UX-STATES.md` — the canonical human/application state grammar.
- `docs/cradle/04-VERIFICATION.md` — ordinary everyday-loop and verification spine.
- `docs/experience/README.md` — the one C0–C5 inhabited-system campaign.

### Agency / development / persistence

- `docs/experience/FACTORY-AGENCY.md` — how Factory and ordinary Agency inhabit the desktop without turning it into a monitor.
- `docs/experience/SESSION-GROUNDING.md` — source, SessionSpace, place, authority, body, recovery, remote continuity and Factory grounding.
- AIKit's current TUI/human-experience specifications — direct work, Worlds, Work, Knowledge, History and real places.

### Expression / deep Epi experience

- `docs/experience/EXPRESSION-WORLD-UX.md` — Expression front/verso/portal law and world-first human experience.
- QL-MEF `docs/kernel-rebuild/UX-SPINE-RECONCILIATION.md` — the Epi/QL lived instrument inside the same Cradle spine.
- QL-MEF `docs/L5-TECHNE-DUAL-READING-LOCK.md` — one M′ field, 3:3 Expression and 4:2 Technē.
- QL-MEF `docs/integrations/epi-logos/TA-ONTA-VAK-COMPOSITIONAL-EXECUTION-LOCK.md` — the shared Vāk → C′ → Ta-Onta → native performance → Aletheia Return language.

The last two are now on QL-MEF `main` through the accepted #209 merge. They crown the architecture; they do not create another application shell.

---

# 2 — The whole is one field with three development views

Do not treat the remaining Wayfinders as competing endings.

```text
                         ONE INHABITED O:I WORLD
                                   │
          ┌────────────────────────┼────────────────────────┐
          │                        │                        │
          │                        │                        │
   CAMPAIGN A                CAMPAIGN B              CENTRAL CHANNEL
   HARMONISE +               EXPRESSION +             #65 C0–C5
   SELF-STRAP                TECHNĒ / CORPUS           inhabited proving
          │                        │                        │
          └────────────────────────┴───────────────┬────────┘
                                                  │
                                         one submission candidate
```

They answer different questions:

```text
A — can the suite become one reproducible, native, self-hosting world?

B — can that world express, investigate, teach and publish through the
    complete 3:3 / 4:2 Epi field, while producing the actual content body?

#65 — can a person and their Agents actually live and work in the joined
      world over time, under ordinary, stressed, failed and recovered conditions?
```

No fourth campaign is required.

---

# 3 — Campaign A: harmonise, inhabit, self-host

This campaign makes the world dependable enough that the world can begin developing itself.

The governing movement is:

```text
HARMONISE
Mac + Omarchy receive the same exact candidate / receipts
        ↓
INHABIT
O:I desktop + native Omarchy + Herdr + Gateway + real Surfaces
        ↓
SELF-HOST
Factory performs one real bounded O:I change end-to-end
        ↓
TURN INWARD
remaining #65 discrepancies become commissioned work
        ↓
HARDEN
ordinary use + stress + failure → owner repair → replay
        ↓
FREEZE
one accepted submission-scoped candidate
        ↓
HOST + SUBMIT
```

## Current threshold

The meaningful connective edge is deliberately small:

| Relation | Current standing | What moves it forward |
|---|---|---|
| Exact suite cut | O:I #347 has proved managed update/apply/rollback on the primary Mac | exercise and accept the same managed-update path on Omarchy; receipts become the candidate identity |
| Open place semantics | AIKit #320 is accepted/merged; a SessionPlan can name open place technologies including `herdr` | land/reconcile the actual public Herdr adapter/route over that registry and exercise it as an ordinary SessionSpace place |
| Native Omarchy inhabitation | O:I #159 carries the complete host/shell/Hyprland/Quickshell/Herdr/Gateway relation | physically run it: placement, scratchpad, Attention deep links, restart/relogin and reconcile on the actual reference world |
| Factory connected execution | accepted C′/CFP execution already exists in Factory; Workcell placement and Actuation/Gateway seams exist | complete one real Commission → admitted Agency/Session → execution → artifacts/evidence → independent Return from inside the inhabited world |
| Campaign ownership | #65 already owns C0–C5 | after the first self-hosted act, let Factory carry failed #65 rounds instead of making the person a cross-product scheduler |

The **SELF-HOST** transition is the phase boundary. Before it, we are still connecting the organism. After it, testing and repair are themselves ordinary activities of the organism.

---

# 4 — Campaign B: Expression / Technē / Atelier / corpus

This campaign remains parallel. It should not hold self-hosting hostage to completion of every publication scene.

The architecture is now fixed as one M′ field with two readings:

```text
3:3 EXPRESSION                        4:2 TECHNĒ
living / material / dialogical        deep / professional / investigative

M1 · M2 · M3                          M1′ Canvas
        ↓                             M2′ Relation / Timeline
M4 Nara / Anima                       M3′ Journey / Scenes
        ↓                             M4′ Places / World
Expression                            M0′ Project / Wiki / Graph
M0 source depth                       M5′ Palace / Epii / Return
M5 Epii depth
```

The two sides preserve the same subject, refs, sources, constellation and occasion. Crossing them changes **how the thing is disclosed and what operations are available**, not what the thing is.

Ta-Onta is the shared inhabitable execution:

```text
ResolveExpression / Vāk
        ↓
C′  CPF · CT · CP · CF · CFP · CS
        ↓
Khora → Hen → Pleroma → Chronos → Anima → Aletheia
        ↓
actual native performance
        ↓
performed evidence / T·T′ / Return / recognised praxis
```

### Expression production

Point-Cloud is the live Atelier. Anima's `composition`, `material`, `temporal` and `integration` offices work inside a C′-composed act and operate the real editor/engine. Aletheia witnesses, critiques, curates and proposes practice from actual results.

### Technē development

The deep instrument suite is not another product universe. It matures the professional apertures over Project/Wiki, Canvas, Relation/Timeline, Journey, Places/World and Palace while retaining native owners and shared subject identity.

### Content body

Essay arguments, concepts, Bimba local wholes/depths, S-nodes, pedagogical scenes and their content-local glyph/image/ASCII material are a real production workload. Once Factory self-hosting is available, this is an ideal long-running developmental/creative workload for the system itself.

### EX/SF standing

SF0–SF6 and EX0–EX6 have returned their declared carrier/capability scope. They are inputs to the joined world, not unfinished campaigns waiting on premature owner approval. Later Ta-Onta depth, richer multi-Nara use, full deep instruments, final UX and hosting **consume** those carriers.

EX7 belongs after real use: it receives campaign evidence and asks what is worth retaining as reusable praxis.

---

# 5 — #65 is the one central testing channel

Capability development and acceptance standing are orthogonal.

```text
WHAT EXISTS
    SF / EX / Technē / Factory / Omarchy / SDK / Gateway ...

HOW FAR IT HAS BEEN PROVEN
    D → C → P → M → H
    deterministic
      → cross-product
        → provider
          → material/physical
            → human experience / Recognition

HOW THE JOINED WORLD IS EXERCISED
    #65
    C0 → C1 → C2 → C3 → C4 → C5
```

## C0–C5

```text
C0  orient from UX/source to actual operations and evidence obligations
C1  prepare the exact real world/candidate/actors/material safely
C2  do ordinary useful work
C3  compose and stress joined conditions
C4  preserve discrepancy, repair at native owner, replay
C5  independently prove the affected whole and request human judgement
    only where the intended experience is mature enough to stand
```

This is **horizontal compositional acceptance**. It does not blindly rerun every native feature test. Accepted SF/EX/native receipts stay valid at their cut; C0/C1 determine whether the current candidate contains those cuts or accepted successors and then select the smallest representative whole activity which pressures the new composition.

### Specialized proving grammars live inside #65

The newer maps do not create rival campaigns:

- **TD1–TD8** pressure the 3:3/4:2 Technē bridges inside C0–C5.
- **V0–V8** pressure the Vāk/C′/Ta-Onta execution language from Search through real performance, Factory repair, rehearing and Recognition.
- product-native test suites still own their deterministic and owner-specific guarantees.

A failed joined walk names a native owner; it does not create an O:I-local duplicate implementation.

---

# 6 — The physical world to keep in your head

```text
                  PERSONAL DEVELOPMENT WORLD

        PRIMARY MAC                         OMARCHY
        ───────────                         ───────
        principal working locus             persistent Reference World
        Central root/source                 exact managed candidate
        O:I desktop                         Omarchy/Hyprland/Quickshell
        ordinary local development          Herdr + Workcell
        direct Agent/harness work           resident Agency / providers
        candidate control/review            Gateway / services
        human UX / Recognition              SharedField / recovery
                 │                             │
                 └──── same semantic world ────┘
                      refs · Agency · Sessions
                      Surfaces · Gateway · Fabric
```

The Mac and Omarchy are not two independent O:I installations conceptually. They are two material loci inside one personal Workcell/development world. The candidate receipt tells us what software cut each locus is running; canonical refs tell us what subject/Agency/session is continuing; material placement says where execution actually resides.

Remote access is not automatic execution migration. Moving/focusing a Surface does not mint a new session. The system must remain able to tell those differences.

---

# 7 — How a person or fresh Agent should “get into” the system

The entry procedure should feel like orientation into an existing world, not booting a control plane.

## 7.1 Establish the actual world

Determine:

```text
Central root / current Project if any
machine / host / Workcell locus
current managed candidate receipts
available Agency / AgentSession / places
current subject or work if continuing
material/provider degradation
```

Do not manufacture a Project when root work is legitimate. Do not assume a visible pane is the AgentSession or that a running process is current simply because it survived an update.

## 7.2 Read the relevant UX source

Start from the person's actual intended activity, then read only the governing stories/states and domain UX needed for that activity. Do not begin by reading every open issue.

For example:

```text
ordinary writing / direct work
    → Cradle A–K + relevant story/practice

Factory work
    → Factory/Agency + Session Grounding + Cradle G/D/E

Expression encounter
    → Expression World UX + Epi UX spine if installed

Technē/deep research
    → dual-reading lock + instrument UX + same Cradle subject/session states

joined acceptance
    → #65 C0–C5 + selected V/TD/native obligations
```

## 7.3 Resolve, do not manually wire

Use the actual source/Skill/Method/Capability/Action routes. Human Search and structured Agent work share AIKit's typed Resolve field; Epi deepens the same relation through Vāk/C′ when present.

The person should not have to know which repository owns the next call. The lead Agent follows the native owner relation and carries the handoff.

## 7.4 Choose the native performance body from the work

```text
ordinary direct task
    → native Action / direct Agent / familiar harness

creative Expression work
    → Atelier

commissioned developmental difference
    → Factory

material body / room / model service
    → Workcell / provider owner

source / Wiki / knowledge work
    → Central / AIKit / owning source Actions
```

Do not turn everything into Factory merely because Factory now exists. Do not hide a real development change inside an Atelier mutation either.

## 7.5 Let failure become legible work

When reality disagrees:

```text
preserve original episode
→ identify failed relation and owner
→ retain exact UX/source expectation
→ commission bounded repair where necessary
→ produce evidence/candidate
→ re-enter the same activity
→ independent replay
→ Return
```

That is C4. Once self-hosting is functioning, this should increasingly happen from inside O:I itself.

## 7.6 Ask the human at the right time

Human observation can guide development at any time. **Human Recognition is not a premature build gate.** Ask for standing H/EX evidence when the composed experience being evaluated is the one intended to stand, not when a carrier is intentionally only halfway to that experience.

The human's office is then meaningful: use the thing, redirect it, reject it, or recognise it. They are not the build scheduler or missing cross-product message bus.

---

# 8 — Traffic laws for the final phase

1. **One UX spine.** New domain surfaces extend the existing human/Agent experience; they do not invent replacement shells unless explicitly authorised.
2. **One subject across apertures.** Desktop, terminal, Expression, Technē, Wiki, SharedField and Agent views preserve canonical identity where they represent the same thing.
3. **One central proving campaign.** C0–C5 receives the capability tracks. Native owner tests retain their own scope.
4. **No reopening returned carriers by default.** SF0–SF6 and EX0–EX6 are consumed as accepted floors; reopen only a concrete defect against their contract.
5. **No fake H gates.** Human Recognition closes mature lived claims, not intermediate construction phases.
6. **No duplicated orchestration.** Vāk/Resolve + C′ composition + Ta-Onta inhabitation already supply the semantic execution grammar; Factory is its developmental performance body, not a rival language.
7. **No issue-list orientation.** Open issues are work records. The person's activity and the UX/source relation determine what matters now.
8. **Returned reality can revise design, but explicitly.** If repeated lived evidence pressures an authored position, return a proposal to its authority. Do not let implementation or optimisation silently become product meaning.

---

# 9 — Current “next gate” view

For a human or lead Agent asking **what should we do next?**, use this order:

```text
1. Finish the harmonised candidate
   #347 Omarchy managed-update proof

2. Make Herdr an ordinary executable place
   consume merged AIKit #320 through the real public Herdr route

3. Inhabit Omarchy physically
   #159 native shell/desktop/place/Gateway/relogin/reconcile walk

4. Perform the first true self-hosted Factory act
   one genuine O:I discrepancy → repair → independent Return

5. Hand the remaining #65 rounds to that machinery
   the system starts carrying its own hardening backlog

6. Keep Expression/Technē/Atelier/corpus work running in parallel
   use the same UX spine and C0–C5 acceptance channel

7. Freeze only after the joined world, recovery and human-lived claims
   have the evidence appropriate to the submission scope
```

That is the shortest path to the new development mode.

---

# 10 — The submission boundary

The final cut is where two streams meet:

```text
                     SUBMISSION CUT
                     /             \
                    /               \
          INHABITED PLATFORM       CONTENT BODY
          ──────────────────       ────────────
          exact suite cut          essay Expressions
          self-hosting Factory     Bimba/local-whole Expressions
          Mac ↔ Omarchy            required S/material
          real UX campaign         curated key scenes
          failure/recovery         assets + fallbacks
          QL/Nara/Technē proof     source/provenance
          SharedField proof        final editions/captures
                    \               /
                     \             /
                       HOSTED WORLD
                           ↓
                         SUBMIT
```

The hard developmental boundary is reached when the exact suite can harmonise onto the two material loci, inhabit the reference world natively, commission and independently Return a real repair to itself, and then drive the remaining C0–C5 hardening while the person supplies the genuinely human judgements.

At that point foundational construction is over. Remaining work is candidate hardening and content finishing inside the system we built.
