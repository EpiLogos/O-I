# Adoption and installation variants of the same useful-work stories

**Authority:** [O:I #268](https://github.com/EpiLogos/O-I/issues/268), authorial lock of 13 September 2026, with the owner's **18 September 2026 hosted-learning correction** below. This file applies those positions to the existing #65 campaign, [AD/WK/MC/UI stories](STORIES.md) and their [agent practices](PRACTICE-CONDITIONS.md). These are variant obligations, not a second set of products, presets, stories or an installer implementation. Publication does not establish execution.

**Current correction:** `5/0` names engagement with the site's explorable Library and hosted Expression/corpus world. The host supplies the accepted point-cloud Expression renderer, shared corpus/editions and permitted knowledge services; the visitor opens a browser, not a Central + QL installer. The owner's curated published material is the default starting collection. This supersedes the earlier mandatory local `Central + QL` learner-bundle requirement in #268, this campaign and dependent UI/corpus planning. It does not change the seven Context Frames, forbid independently installing those products, or turn O:I's paradigm-neutral shared world into an exclusively QL service. Six forms of engagement do not require six installation procedures.

Earlier campaign references to #192 are also corrected: its six labels (`fresh-ground`, `existing-ground-reconcile`, `developer-source`, `existing-world-adoption`, `reference-world-host`, `harness-strap`) remain **historical operation/registration provenance**, not the installation taxonomy or QL Context Frames. Keep their old receipts interpretable; do not reinterpret old `cf5` or `5/0` installation evidence under newer semantics.

## The one containing frame

**CF5, `4.0/1–4.4/5`, is the material nesting frame in which the other six forms are situated.** Some computer/material environment is always the condition. Installing the Workcell management product to a greater or lesser degree is a separate choice. All-products deployment, with or without desktop, is supported inside that frame; it is not what makes CF5 exist.

Every episode records actual material placement even when no Workcell daemon/VM/management package is installed. It does not fabricate a registered Workcell identity when the native relationship is unavailable; it records the actual external/local condition honestly. For hosted learning, distinguish the visitor's browser/material environment from the host's installed service composition.

## Six forms, with concrete human and agent proof

| Form and existing stories | Person's useful activity | Conditions that determine the agent/operator's work | Proof and adverse case |
|---|---|---|---|
| **CF1 `00/00`**; AD01/AD02/AD08/AD09, UI01–UI07 | Add the integrated app to my existing files and tools; open the real document, ask for help where the selected backing supports it, then remove the app without losing the work. | Desktop + Central + explicitly selected backing. The normal new-install backing is Central/Actuation/AIKit, not an identity equation CF1=CF3. The operator discloses the backing and actual source/agent capabilities. | App opens real data, actual requested agent work where selected, preserved native configuration; adding/removing desktop neither recreates nor deletes the underlying world. Interrupted setup preserves pending data. |
| **CF2 `0/1`**; AD02/AD04/AD08, WK01/WK02 | Keep my existing agent tool and durable instructions, without installing the resolver/development stack. | Central + Actuation; existing realised agency/harnesses are legitimate. AIKit/Factory/QL are absent, not silently invoked as required helpers. The operator uses actual supported native practice. | A meaningful scoped task uses the intended source/authority and existing tool; absence of unselected products causes no demand to install them. Test refusal without breaking permitted ordinary use. |
| **CF3 `0/1/2`**; AD01/AD04/AD05, WK01–WK05, GV03, PX01–PX03 | Have my relevant information, practices, models and tools accompany ordinary work; I need not open the desktop. | Central + Actuation + AIKit. Resolve selected sources, real descriptions, trust/loading, credentials and session body; no mandatory Factory. | Actual fresh-agent task uses the correct instructions and knowledge; a second harness retains native differences. Missing selected capability is explained; unrelated tools stay usable. |
| **CF4 `0/1/2/3`**; AD08/AD09, DV01–DV08, WK04/WK05 | Let agents carry an organised development job while I continue ordinary work elsewhere. | Central + Actuation + AIKit + Factory; exact task/source/context, real independently checked effects and intended working environment. | A real coordinated change completes through supported owners; ordinary sessions coexist without invented Factory ancestry. Removing Factory preserves retained source/results and permitted direct work. |
| **CF6 `4.5/0`**; AD06/AD08/AD09, MC02/MC03/MC05, UI02, UI07 | Use this lightweight local machine to reach work hosted elsewhere. | Central + minimal Workcell client/connectivity. QL is **not** required by `4.5`; local Actuation/AIKit/Factory are not prerequisites for the client relation. Remote software and local software are accounted separately. | An authorised remote task/service is actually reachable from the intended client, with private access and explicit source projection. Denied/expired connection does not turn into public exposure or hidden local fallback installation. |
| **CF7 `5/0`**; AD08, KN01/KN02/KN07/KN09, UI01/UI02/UI03/UI05, and applicable public-web/QL stories | Visit the site, enter the explorable Library and inhabit the published work through pages, live Expressions and source-backed relations. | No local O:I, Central, QL, AIKit, Actuation, Factory, Workcell package, API key or agent setup is required of the visitor. Host-published corpus/edition and accepted Expression renderer use the same native refs/projection machinery as desktop. Nara/Epii guidance and shared-live participation are optional richer activities. | Clean browser → curated Library → real essay/Bimba subject → live Expression → declared relation/source → same reading position. Public reading does not require joining a shared session. Test deep-link reload, unavailable renderer/service, revision drift and private-source exclusion; no silent localhost service or visitor installer. |

The row lists are **entry variants**, not declarations that every listed story can run in every form. An agent-dependent UI action is unavailable in a writing-only CF1 backing; hosted CF7 reading does not imply personal agent admission or live collaboration. Preparation selects the applicable activity and preserves richer unmet obligations elsewhere; it cannot mark an entire family passed from one minimum.

## Hosted 5/0: one Library and one published world

The site is the learning entrance to O:I Web. Its Library opens at the owner's curated published work: the Return-of-Zero essay, Bimba, the distinct Epii M5-1/Antichrist material and the S-product/research field where admitted. Their canonical source identities remain distinct. The default collection is not another catalogue, a hardcoded toy corpus, or the definition of all O:I worlds.

Use the existing source → Wiki/Knowledge bounded reading → Expression → WorldPresentation → audience-scoped Projection route. Ordinary native files need not be converted to Wiki merely to be opened; knowledge-backed corpus Expressions use #366's real seam. Visitor navigation and an Expression's visible geometry do not invent semantic relations or acquire source authority. Follow [WorldPresentation](../../shared-field/WORLD-PRESENTATION.md) and [shared state/discovery](../SHARED-FIELD-STATE-DISCOVERY.md).

The host installs and operates the necessary providers; the browser receives admitted published material and runs the accepted interactive renderer. A visitor must not supply a local daemon, private developer checkout, paid model credential or package install to read the public work. Public read/search uses its admitted read horizon. Identity, consent and bounded authority remain required for personal tutoring, contribution, publication or privileged actions where their native contracts require them.

Basic reading and individual exploration are independent of presence, Watch and Shared Stage participation. SpaceTimeDB carries those live shared relations when selected; it is not a mandatory session-join gate for opening a published essay. Host source, Projection revision, corpus edition and browser reading/draft state remain distinct. Renderer fallback, cached reading and network loss are labelled at their actual support; do not promise uncached offline content or call a still image proof of live interaction.

### The required visitor walk

On a clean browser profile with no O:I software or developer credentials, follow the published starting route; search/open a real source-bound subject; enter its live Expression; select a bound object; inspect an eligible page/relation/source; return to the same subject/scene/reading position; reload a deep link. Test a new published revision against an older reading or browser draft, unsupported rendering, restricted source and unavailable optional services. Ordinary reading remains usable when tutoring/live-stage facilities are absent. Exercise keyboard, narrow layout and reduced motion through the actual site controls.

Record site/client build, published edition/projection/source/renderer revisions, browser/OS and tested service availability. Server/native tests, browser interaction and human assessment have separate standing. Sandbox browsers can automate the route; actual Mac/browser/GPU-specific proof and human sensory/pedagogical judgement retain their own cases. The complete corpus submission boundary is unchanged by a representative walk.

### In-place implementation handoff

- **#268:** reconcile setup/catalogue/help and current-world consumers that still turn `5/0` into a mandatory visitor installation. Preserve historical receipt meaning and explicit independent product installs; do not reinterpret existing `oi.current-world/v2` records silently. This planning change does not claim the runtime migration has landed.
- **#375 / #366:** common Library and the new UI/site consumer retain world/corpus/subject/scene/source continuity. Epi-Logos remains a selected paradigm across arrangements; six app entrances are not installation switches.
- **QL-MEF #201 / Antykathera-Essay-Work #65:** publish the same source-bound Epi/corpus edition and starting route consumed by the site and app; no dedicated learner executable or second submission dataset.
- **#18 / #306:** consume the completed shared/Expression carrier; naming the hosted learner does not reopen SF0–SF6 or EX0–EX6.
- **#65:** replace the superseded CF7 local-install test with the visitor walk, while keeping native product independence, other composition variants and full QL/source obligations separately accounted.

## Cross-form interaction stories

These remain branches of AD08/AD09, MC and DV, not a linear upgrade ladder:

1. **Add or remove a surface.** Start with useful headless work, attach the desktop, inspect/edit permitted material, detach/remove it and continue in the native tool. No source duplication or forced session restart merely from presentation. Visiting hosted 5/0 does not first install a surface.
2. **Add a capability and later withdraw it.** Move between actual chosen combinations through their owner operations; preserve identity, history and configuration. Show effects on pending work before removal. Choosing richer hosted participation is separate from changing local installed composition.
3. **Separate three operations.** A software update changes selected installed software. Source synchronisation changes only explicitly selected source with direction/conflict/authority. Material reconnection re-establishes a supported connection/lease. An “update” must not silently do all three.
4. **Change machine, not identity.** Open the same remote work from a second permitted client; document the source and continuation actually available rather than assuming identical local installs.
5. **Remove and explain residue.** Preserve pre-existing native programs, personal source and useful artifacts. Revoke only owned access and release only owned resources. A purge of disposable tests needs its own specific authority. Browser-local drafts/history and shared account withdrawal are not a fictional CF7 package uninstall.
6. **Historical evidence.** Read an old receipt without relabelling its composition. New readings use the corrected current-world contract; source/time/code changes remain explicit.

## Settings across the installed forms (the settings × composition interface)

The unified settings plane — `oi config`, the Desktop Configuration view, the profiles store — renders one world's settings, and each installed composition sits inside one world. The mode and the settings are joined by a reading, not by a rule per product. The following installed-world law does not make a hosted 5/0 visitor configure a local world or expose host administration in a reader page.

**The composition facts come from the world, never from the settings.** What is requested (`oi mode set`, never inferred from presence), what is effective (the present positions of `oi.current-world/v2`), which basis is speaking, and the reading's own shortfall warnings are disclosed beside the settings data. The settings surfaces re-decide nothing: no second composition decision, no second settings database, no mode inferred from a contribution or a product count.

**Each owner stands somewhere, and the standing is disclosed.** A product position inside the effective composition renders exactly as its owner discloses — its settings are actionable through the owner's own operations, subject to its own availability probe. A product position outside the effective composition is visible only as disclosure (`absent from the effective composition`): no settings rows, no controls, nothing instantiated behind it. `oi` and connector owners hold no product position and stand inside the world. When the world reading itself is unavailable, every standing reads `unknown` and the reason is named — honest absence, never a guess.

**Absence never hides, and recognition precedes mutation.** An absent product's settings cannot be held, planned, applied or reset from these surfaces — there is nothing to address that no owner contributed, and the view layers gate on the standing so a stray document can never render as an actionable control. Adding the product is an explicit owner operation (`oi mode set`, the lifecycle planner) that names the mode consequence. Reality-exceeds-request and request-names-degraded-world apply verbatim: settings of products present beyond a smaller request stay actionable, and a requested-but-absent product's shortfall stays in the disclosed warnings.

**Transitions are readings, not reconstructions.** Standing is derived per read from the world's own facts, so `0/1 → 0/1/2 → 0/1/2/3` and back, and Desktop add/remove, change the disclosure without touching the world. Desired intent that outlives its owner — a product removed while settings were held — is retained state: it resolves `blocked` with the failed discovery named, in `diff` as in `doctor`; it is never deleted silently and never renamed into a false status.

**Desktop preferences stay separate.** The Desktop app's own preferences are not world constitution; the configuration plane renders only what owners contribute, and the composition disclosure travels with the data (`oi.config-listing/v1` `composition` block; `oi.cradle.config-registry/v1` `composition` + per-mount standings) through the same engine, once.

Mechanism pointers: `cli/src/current_world.rs` (the pure standing join), `cli/src/config_surface.rs` (`CompositionDisclosure`), `cli/src/kernel_surface.rs` (engine binding, retained-desired resolution), `desktop/cradle/kernel/src/configuration.rs` (registry join), `desktop/cradle/src/configuration/composition.ts` (view law), proven by `config_list_discloses_the_world_composition_and_each_owner_standing`, `registry_carries_the_mode_disclosure_and_the_absent_shortfall`, `tests/configuration-composition.test.mjs` and the configuration walk.

## Preparation and closure

Use `campaign.json`'s retained `installation_composition` source key as the planning projection of #268, now explicitly distinguishing installed forms from hosted engagement. It is not the executable installer schema. Record requested/effective/installed composition where applicable, native alternatives, visitor versus host surface/environment, authority and actual lifecycle evidence. Never infer them from a CF string or product count.

The owner reports Omarchy sandboxes available as of 18 September 2026. Use [LOCAL-CAMPAIGN.md §2.1](LOCAL-CAMPAIGN.md#21-omarchy-sandbox-proving--available-development-material) for isolated parallel install/update/rollback/removal, functionality, browser and failure/recovery rounds. Inspect the actual provider's isolation and lifecycle first; the host and the Mac are not disposable. A sandbox pass does not certify macOS packaging, bare-metal shell/reboot behaviour or another physical machine.

Native #268 implementation and its tests own composition/lifecycle behaviour. The #65 campaign verifies the person's first use, ordinary repeat use, failure/recovery, change and teardown for each promised form, and the actual hosted visitor journey for CF7. Static planning validation proves only that the source requirements were retained. Missing production operations stay with the connective implementation lane; the human should not discover preventable prerequisites halfway through a test.


## Implementation return — adoption lifecycle #423, 20 September 2026

Owning Wayfinder: #268, with native engagement #109 and configuration #299. Extend the delivered #406 controller/forms rather than recreate settings. The #423 native plan/journal and terminal consumer now connect selected composition and installation to capability setup; its dependent System ingress is required for desktop integration. All product identities, the 1+6 organisation, historical receipts and hosted `5/0` correction above remain unchanged.

Source-built native commands are offered only when their real build prerequisites are discovered; unsupported/missing native bundle offers remain blocked, not replaced by fixtures. Receipt-owned teardown preserves foreign installations and human source. Source activation rollback is distinct from uninstall and must use its existing native receipt. Initialisation and project placement disclose missing native authority instead of fabricating a human-adopted Control policy. New authority still needs its actual owner contract.

Executed results and exact implementation/dependent heads live on #423; the [existing local campaign packet](LOCAL-CAMPAIGN.md#72-installationadoption-extension--423--268--299--109) supplies clean/existing, partial, optional-product, Desktop add/remove, external-edit, interruption, unknown-reply, restart and owner-supported rollback checks. Controlled settings/transport success is not native package installability, an installed Mac or the published full corpus. Local integration owns candidate installation and material proof; web commits and tests through PRs without merging or mutating machines.
