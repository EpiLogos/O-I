# Cradle UI experience v2 — GitHub handover

This is the repository-hosted handover for the owner's Cradle UI refinement commission. It replaces the previous draft's interaction design, not the active application's implementation. No owner checkout was modified by publication.

Read [the local lead commission](docs/cradle/UI-LOCAL-EXECUTION.md), then [the development wayfinder](docs/cradle/UI-DEVELOPMENT-WAYFINDER.md), the [surface constitution](docs/cradle/surface-specs/00-EXPERIENCE-CONSTITUTION.md) and its seven companion surface files. The six bounded worker prompts are in [handover/prompts](handover/prompts/). [control-specimen.html](control-specimen.html) is an isolated menu/drawer/workmark study: TEST SPECIMEN, never a production import or reconstruction of the accepted local Context.

## Read directly from GitHub; do not switch the active tree

Repository: `EpiLogos/O-I`.
Publication branch: `docs/cradle-ui-experience-v2-2026-09-21`.
Handover root: `docs/cradle/handovers/ui-experience-v2/`.

Fetch the branch without checkout and pin the returned commit. Read its files with `git show`, or export this handover directory using `git archive` into a separate temporary reference directory. Do not merge/cherry-pick this documentation branch, unzip over the dirty repository, create another worktree or replace active source from main. Reconcile the specifications into the existing local documentation while preserving local inventories and held changes.

## Owner corrections already incorporated

- One + creation menu, not two standing creation buttons; contextual card controls with exact gesture/focus rules.
- Existing Desk/Tasks retained. Desk provides ordinary working-object depth for agents, teams and skills while preserving full SSSF. Tasks remains conversation.
- Existing Context canvas insertion for files, terminal and browser is protected as the desired current reference. The v1 replacement lists are withdrawn.
- Only automatic completion/status notices are one line; useful long answers remain.
- Shared components, scope-safe native operations and independent verification; no new management app or second source/runtime store.

## Source freshness

This branch starts from O-I `a2c68f1b6e9d334dab851d259780950300bc32ae` (21 September 2026). Its #454 landing reports first-Send session provisioning and real chat/harness/model census integration. Treat earlier missing-seam statements in the preserved design as historical inspection limits, not reasons to remove or disable those newer paths. Inspect actual current local source and readback, keep first-Send usability and any newer real roster/model/harness functionality, and name a missing capability only after checking the actual operation. Remote main remains comparison material, never a replacement for the owner's dirty convergence tree.

## Authority and evidence

Publication of this handover to its dedicated GitHub branch was explicitly authorised in this conversation. That does not authorise local implementation commits, pushes, merges, deployment, paid-provider calls or unrelated machine/service changes. Local implementation remains uncommitted and unpushed until separately authorised.

The eight surface specifications retain the prior draft's binding appendices; the receiving lead must also preserve the unseen original local inventories. The original Mac screenshots, active app, native integration and Cradle build/walk/human acceptance remain local execution evidence. Included historical validation records concern the isolated document/control specimen only. Screenshot binaries from the earlier chat delivery are not prerequisites for the local execution: inspect the actual 24 repository screenshots and capture fresh before/after evidence. The specimen browser check can regenerate its own screenshots outside production.
