# D — Settings refinement


Work only in the lead's explicitly allocated files in the owner's current dirty /Users/admin/Central/Work/O-I tree. No commits/pushes/stash/reset/clean/branch switch/global formatting. Read the current local source and your surface spec; do not replace it from remote main. Respect the exact before-image/hash ownership receipt. You are implementing, not returning a design-only plan.

The latest correction overrides v1: preserve left Desk/Tasks, right Run/Agents/Context, accepted Context canvas file/terminal/browser insertion, settings v2 and native identities. No fixtures in production. Current missing native operations are one scoped owner/native-path response, not fake success. Use existing components/stores/owner routes. Ask the lead for shared changes; do not edit shared files without explicit transfer. Preserve keyboard parity, one-layer Escape, visible focus, drafts and exact scopes.

Return changed/new paths, lane-only diff or exact edits, tests actually run with exit status, screenshots at agreed viewports, subtraction list, and any shared integration requests/native blockers. Do not label controlled fixture tests as native or human acceptance. Do not run the owner's services, paid models or broad suites concurrently; coordinate proof with the lead.


Read 05-SETTINGS.md and the actual current v2 implementation. Own only lead-confirmed configuration/v2 presentation/section files and scoped styles. Do not alter configuration contracts, engine, sourceHost fixture gate, credential handling or shared package files.

Add first-class Harness/Models/Skills/Gateway destinations under actual owners in the existing rail/search. Real compact selectors, readonly values as readable values, one actual source/history overflow, no invented catalogue. Auto retains the native selection policy. Credentials are presence/reference only, no masked token field or copy-secret control. Skills sources/pins remain readonly where native mutations are missing; Review skills reuses the shared working plane. Gateway consumes actual Workcell status/config, with one scoped attach/stream obligation until exposed.

Preserve v2 authored/effective/active, scopes, desired state, plan/apply/receipts. No permanent Apply shelf without changes. Zero capability deltas is exactly one line, no empty table/remove rows. Stale/refused changes preserve draft and cannot apply under a changed scope.

Prove S01–S07, including existing Visuals/adoption/configuration regressions. Coordinate tests and shared mounts with lead.
