# Local execution commission — Cradle UI v2

Work in `/Users/admin/Central/Work/O-I` on the current active dirty convergence tree. This is an implementation session: deliver changed UI, regression tests and before/after evidence, not another planning response. Preserve held work; no commits or pushes, no stash/reset/clean/branch switch/global formatting, no new machine workspace and no unrelated live-service changes.

## Acquire the published handover without changing the working tree

Use the exact publication commit supplied by the owner prompt. Repository `EpiLogos/O-I`; publication branch `docs/cradle-ui-experience-v2-2026-09-21`; handover root `docs/cradle/handovers/ui-experience-v2/`. Fetch the branch without checkout. Read with `git show <publication-commit>:<handover-root>/START-HERE.md`, or export only that directory using `git archive <publication-commit> <handover-root>` into a separate temporary reference directory. No Downloads/ZIP dependency, branch switch, merge, cherry-pick, reset or additional worktree.

Read START-HERE.md, docs/cradle/UI-DEVELOPMENT-WAYFINDER.md, surface specifications and handover/prompts/. Reconcile the documents into the existing local docs/cradle/surface-specs/ and wayfinder, preserving every local binding inventory and current native join. The published files are design input, not a patch to overwrite held source.

The publication base is `a2c68f1b6e9d334dab851d259780950300bc32ae`; #454 reports first-Send provisioning and real chat/harness/model census. Preserve locally landed newer capabilities. Historical missing-seam statements in this handover do not justify disabling working paths, adding a chooser before first Send, or replacing a real catalogue with an obligation. Verify the actual current local seam before classifying anything as missing.

The owner's latest corrections are binding:

• Left Desk/Tasks is already good. Desk is the actual SSSF Run workbench and the place to open/work with project agents, teams/sets and skills using ordinary existing material tabs. Tasks remains the real agent conversation, with the selected Grok/Slack message-room visual grammar and existing Buzz To:/@ mechanics. No new Manage mode or duplicate roster app.
• Run/Agents/Context remain the right tabs. The detailed tape lives in Run; it does not replace central SSSF dependencies, lanes, barriers, independent verification, candidates and convergence.
• Agents uses one + and an attached New agent…/New team… menu, not two standing buttons. Specify and implement rest/hover/focus/selected/pending/error/Escape, object hit areas and contextual actions throughout—not just functional controls.
• Current Context canvas insertion already works for files, terminal and browser context. Capture it as context-canvas-current, freeze its component paths against redesign, and regress it before/after. Do not replace it with Sources/Skills/Tools lists or another attachment system.
• Preserve native identities/scopes, actual Guardians, the complete local Skills plan/apply/readback, settings v2 and one left Inbox. Missing native capability is one owner/path response at the invoked workflow, never fake success. Only status/completion notices are one line; real long answers remain intact.

Read current founding positions and the specified local source handoffs, visually inspect all 24 desktop/inspo-screenshot images and the actual running accepted Expressions/Desk/Tasks/Context. Record the exact HEAD plus dirty-file before-images/hashes, original failures and protected good behaviour. Do this once, then implement—do not restart a whole-suite research programme or fall back to remote main.

Act as integration lead. Publish exact non-overlapping file ownership and the current open-object/open-activity/scope/callback contract. Use the six supplied subagent prompts: A Desk; B Tasks; C Agents/Teams/Skills; D Settings; E Navigator/Inbox/Launcher/Explore; Q independent verifier. Give each its exact whitelist and relevant source/spec slice. Run at most three implementation workers at once unless current harness supports more. A/B/C can start together; D/E follow while you integrate Run and Q verifies. If actual subagent tooling is unavailable, report that specific result and do the same work sequentially rather than stop.

You alone own shared shell/mode/AgentLayer/FactoryCentre/deskModel/sidebarModel joins, shared styles/tokens, fixture source gates, build configuration and walk registry unless an individual file is explicitly transferred. Workers must return bounded shared changes, not edit those files concurrently. You also own RunPlane/TrajectoryPlane integration. Preserve scopes: open/inspect is not add-to-project; team selection is not privilege union; skill selection is not apply; project defaults do not mutate a running Run; only actual admitted Start run dispatches Factory work.

Execute the wayfinder phases 0–6: subtraction/quarantine; Run tape plus Desk work objects; Tasks refinement; colleagues/Guardians/Skills; first-class settings; single Inbox; navigation/Explore and whole visual pass. Reuse existing components and native seams. Use the local good Context and SSSF bodies, not the control-study HTML as replacement app code. The HTML is an isolated TEST SPECIMEN for a few controls, never a production import.

Run npm run build and the existing relevant walks/scripts after inspecting their actual effects and prerequisites. Preserve generated/dirty personal documents, same-checkout engine provenance and the owner's running processes; use isolated ground and unoccupied ports. Do not invoke paid models or mutate personal/live sources for tests. Keep native, controlled, snapshot/live and human evidence separate. After WALK builds, rebuild plain production with WALK unset and prove banned-copy/fixture string absence, emitted module-graph quarantine including lazy chunks, production ?fixtures=1 negative and no stale fixture state. Test keyboard, focus return, narrow/wide, light/dark, zoom/reduced motion and real refused/disconnected handlers.

Have Q independently walk: retained file/terminal/browser context → Desk SSSF step → exact Tasks chat → colleague/team/skill working object → real scoped review/apply where supported → exact Run event → native delivered-material review → back to unchanged draft/context. Fix regressions rather than only documenting them. A missing owner operation stays explicitly open, while independent UI work continues.

Finish with actual lane-only modified/new paths, per-surface removals, before/after images including the protected Context comparison, commands/exit statuses and audit receipts, any original failures, exact remaining native obligations and a short runnable manual walkthrough. Do not claim human acceptance or all tests passing without evidence. Leave the working changes uncommitted and unpushed.
