# O:I Development Field Protocol — Current Execution Wayfinder

**Status:** current implementation Wayfinder / design commitment  
**Execution topology:** eight owner-native sessions, each executed as one coherent track  
**Primary remote implementation surface:** ChatGPT ↔ GitHub  
**Final convergence / machine proving surface:** local machine, expected to be run with Claude  
**Deferred aperture:** deeper QL/intelligence interaction grammar, Wiki cognition/retrieval scripting, and Agent Development-Field Skills/Methods are intentionally left to the next intelligence specification rather than guessed here.

---

## 0. Destination

Make software development inside O:I one addressable, inspectable and executable field in which authored meaning, intended experience, capabilities, plans, code, CI, package state, material execution and returned evidence remain related without collapsing native ownership.

The whole developmental relation is:

```text
AUTHORED WORLD / PROJECT SELF-DESCRIPTION
        ↓
UX — intended experience
        ↓
CAPABILITIES
        ↓
PLAN / COMMISSION / INTENDED DIFFERENCE
        ↓
FACTORY DEVELOPMENTAL CONTINUITY
        ↓
AIKIT OPERATIVE RESOLUTION
        ↓
WORKCELL MATERIAL DEVELOPMENT WORLD
        ↓
ACTUATION / ACTUAL AGENCY
        ↓
IMPLEMENTATION + PR
        ↓
D / C / P / M EVIDENCE
        ↓
EX — actual human experience
        ↓
RETURN / RECOGNITION
        ↺
AUTHORED WORLD RENEWED WHERE WARRANTED
```

The Development Field is not another project-management database. It composes systems already present:

- **Central** — durable authored/source World and machine intent;
- **Actuation** — actual Agency, authority, Activity and Return;
- **AIKit** — operative resource/context/Git/harness/session resolution;
- **Software Factory** — Commission, Journey, Run, Evidence, Candidate and Recognition;
- **Workcell** — material execution worlds and lifecycle;
- **Quaternal Logic** — canonical structural shapes/relations and portable shape grammar;
- **O:I** — whole-suite composition, command doorway, CI composition and installation/update field;
- GitHub issues, PRs and native product CI.

The protocol is accepted when a fresh Agent can start from one developmental reference, recover what the work means and which native powers it affects, develop the intended difference against an exact basis, return exact evidence, and leave the accepted product/world state more intelligible rather than creating another archaeology layer.

---

# 1. Ground and provenance

Every execution session begins by re-reading current live state.

Required ground where relevant:

```text
O-I/docs/positions/FOUNDING-POSITIONS.md
Central/docs/CONTROL-CONTENT-PROTOCOL.md
Central/docs/CAPABILITY-MATRIX-PROTOCOL.md
Central/docs/MACHINE-WORKCELL-RELATION.md
O-I/suite/mainline.json
O-I/suite/manifest.json
each product .oi/product.json / current lifecycle descriptor
QL-MEF canonical shape contract or accepted successor
AIKit current SemanticWiki / Knowledge / VersionedWorld / Git contracts
Factory current Commission / Journey / Run / Evidence contracts
Workcell current material-world / client SDK contracts
```

Also inspect the actual issues, PRs, branches and current `main` of the repository being changed.

Preserve:

```text
authored position
≠ design commitment
≠ research proposition
≠ implementation fact
≠ observed result
≠ current development state
≠ inference
```

Prompt-time SHAs, this document and issue prose are orientation. Current native owners and accepted code determine implementation reality.

---

# 2. Ownership field

## 2.1 Central — durable World/source owner

Central owns:

```text
source identity and filesystem aperture
source revision
authorship / adoption / provenance standing
personal ↔ Project scope
privacy / retrieval treatment
retain / relate / migrate / retire source lifecycle
root and Project self-description source homes
UX source standing
EX human-source standing
machine role / intended machine state
Skill / Method source homes where those sources exist in Central
```

Central does **not** own Wiki cognition, Skill/Method runtime resolution, Git execution semantics, Factory developmental semantics, Workcell material lifecycle, Actuation, or suite version selection.

## 2.2 AIKit — operative resolution owner

AIKit owns:

```text
KnowledgeApplication / SemanticWiki traversal substrate
Search / Resolve / Explain / History
ContextResolution
resource addressability
Git capability / VersionedWorld reconciliation
model / provider / Harness resolution
SessionSpace / AgentSession composition
effective runtime eligibility / availability
QL-shape client parity where already accepted
```

The deeper intelligence grammar, Development Field retrieval scripts and Development Field Skills/Methods are **not** implemented by this tranche unless the next intelligence specification explicitly returns them here.

## 2.3 Software Factory — developmental meaning owner

Factory owns:

```text
Commission
intended developmental difference
Journey
Run / WorkflowUnit / frontier
developmental continuity
Claims / Evidence / Candidate
proof obligations as developmental obligations
Return / Recognition
Development Field as the foundational software-development technique
```

Factory references AIKit operative resources. It does not duplicate the resource/Skill/Method/Git engine.

Do not add a new `RunMode` merely to name this technique if current Commission / WorkflowUnit / ExecutionDisposition contracts can express it.

## 2.4 Workcell — material development World owner

Workcell owns:

```text
material environment identity / binding
placement
compute / storage / network materiality
workspace preparation
process / service lifecycle
VM / sandbox / remote / cloud / local materialisation
observation / health
collect / release / reconcile
```

Workcell does not own Git branch/worktree semantic identity, Factory Run identity, Harness semantics or Agent identity.

## 2.5 Actuation — realised Agency owner

Actuation owns:

```text
Agent / Agency semantic identity according to current accepted contracts
WorldBinding / authority
actual actuation
Activity / ActuationStream
Return
```

This tranche changes Actuation only where Development Field integration reveals a real missing provenance/actuality seam.

## 2.6 Quaternal Logic — canonical structural-shape owner

QL owns:

```text
forms
shapes
shape addresses
relation families
conjugacy / D completion
shape operators
derivation
recursive Return law
portable structural shape contract
```

Product-specific content bound into a QL shape remains owned by that product/source.

A shape address is a place where a relation can be asked or recorded. It is not automatically a semantic assertion.

## 2.7 O:I — whole-suite composition owner

O:I owns:

```text
six-product installed whole
`oi` command doorway
cross-product Development Field envelope
suite candidate / channel resolution
active suite receipt
install / update / repair / rollback composition
whole-level status / verify
whole Development Field composed readings
cross-product CI orchestration where O:I owns the relation
```

O:I never becomes a second semantic implementation of an owner operation.

---

# 3. Central self-description and document field

## 3.1 First-class self-description aperture

Central should distinguish what a World/Project says about itself from the person's general writing and from the Agent Wiki.

Target conceptual source shape:

```text
~/Central/
└── Control/
    ├── self/
    ├── user/
    ├── agents/
    │   ├── governance/
    │   └── wiki/
    ├── skills/
    ├── methods/
    ├── machines/
    └── <accepted personal temporal aperture>

Work/<project>/
└── ProjectCentral/
    ├── self/
    ├── user/
    ├── agents/
    │   ├── governance/
    │   └── wiki/
    ├── skills/
    ├── methods/
    ├── now/
    └── project.json
```

Semantics:

```text
self/**
    what this scoped World / Project says it is

user/**
    ordinary human-authored writing / retained source

agents/governance/**
    standing human-authored relation for Agents

agents/wiki/**
    Agent-maintained knowledge about/across the World

skills/**
    reusable praxis source

methods/**
    contextual praxis-composition source

now/**
    temporal working field

machines/**
    authored machine roles / intended state / opaque external bindings
```

`self/` is not a Wiki, generated account cache, or general human notebook.

Retained native source may participate by relation rather than forced relocation.

## 3.2 Six-tier document staging

The existing six document-stage tiers remain authoritative. This Wayfinder does not rename them.

Machine-level compatibility uses stable tier identities `0..5`. Central binds those stable IDs to the existing canonical names/paths.

Required semantic offices:

```text
0 — originating authored ground / why / positions / intent
1 — intended experience / vision
2 — design / capabilities / functional form
3 — architecture / contracts / owned relations
4 — implementation / active development / current operational form
5 — evidence / returned reality / Recognition pressure
```

These are semantic glosses only until the canonical tier names are recovered and bound.

The self-description contract must allow sources to remain in native locations while declaring their tier role and source provenance.

---

# 4. Development Field references

Use stable semantic refs instead of path or filename coincidence.

```text
ux_ref
    intended human experience

ex_ref
    actual human experience report

x_ref
    generic experience ref = ux_ref | ex_ref

cap_ref
    capability identity

plan_ref
    developmental plan / Wayfinder-unit identity

ev_ref
    non-human evidence / verification identity

src_ref
    native owner source identity
```

Existing native refs remain native:

```text
WorldRef
ProjectRef
AgentRef
AgencyRef
AgentSessionRef
ActionRef
JourneyRef
RunRef
CandidateRef
SourceRef
Workcell binding refs
Git commit/worktree refs
```

Do not create O:I duplicates.

## 4.1 UX

UX is normative intended experience.

Human-readable authored source remains primary; CSV/JSON may be compiled/projected.

Minimum structural reading:

```text
id
kind
parent_ref
actor
story
entry_state
act
experienced_outcome
return_state
branch_condition
surface_refs
source_refs
standing
shape_binding?
extensions
```

## 4.2 EX

EX is descriptive human return and is reserved for human actor input.

A machine/Agent may prepare screenshots, exact suite facts and environment evidence, but cannot fabricate EX.

Minimum reading:

```text
ex_ref
ux_refs
suite/environment receipt
surface
human report
disposition
artifact_refs
pressure_refs
timestamp
```

H-grade evidence requires EX.

---

# 5. QL-shaped Development Field carriers

This tranche consumes the canonical QL grammar but does not specify the deeper intelligence operations that will act through it.

## 5.1 ShapeDefinition versus ShapeBinding

Distinguish:

```text
ShapeDefinition
    QL-owned structural meaning

ShapeBinding
    attributable binding of actual project/product objects into that structure
```

Generic binding requires equivalents of:

```text
subject_ref
shape_ref
whole_ref
basis_refs
member_bindings
relation/address_bindings
derivation_ref
operator_ref?
return_refs
standing
source_refs
provenance
```

## 5.2 General object law

An addressable resource may remain an ordinary `1`, open a `0/1` whole, or carry a specific QL shape only when an actual QL definition and attributable binding exist.

Do not force every document into six positions merely because QL can read sixfolds.

## 5.3 Capability matrices

Capability matrices are relational fields over real disclosed/bound wholes, not arbitrary spreadsheets decorated with QL terminology.

General structural reading:

```text
QL-shaped whole A
        ×
QL-shaped whole B
        ↓
RelationField(A,B)
```

Valid cardinalities can emerge from constituent shapes:

```text
2×2
3×4
4×4
6×6
6×12
12×12
...
```

A 6×6 field is not automatically the canonical direct↔conjugate QL 6×6.

Partial/developed shapes remain positive wholes.

## 5.4 Wider carriers

Docs, plan records, evidence outputs, scripts, Skills/Methods and Wiki readings may later gain QL ShapeBindings through the intelligence tranche.

This execution only establishes the **carrier compatibility and owner seams** required so those later bindings do not need another migration of the Development Field protocol.

Shape binding never upgrades source standing.

---

# 6. Capability lifecycle

## 6.1 Native capability matrices remain owner-native

Keep `ql-capability-matrix/1` or its accepted successor.

A capability retains:

```text
id
need
operation
outcome
implementation_status
standing
source_refs
code_refs
test_refs
account_ref
relations / placements
extensions
```

The relation to UX is:

```text
UX ──requires──▶ capability
capability ──supports──▶ UX
```

Internal capabilities can support other capabilities transitively and do not need fake user stories.

## 6.2 Feature PRs do not fight over generated matrices

Normal implementation PRs declare their Development Field impact but do not regenerate large canonical matrix projections unless that PR genuinely changes authored capability meaning.

Representative PR declaration:

```yaml
oi-development:
  plan_refs:
    - plan....
  ux_refs:
    - ux....
  capability_refs:
    - cap....
  expected_proof:
    - D
    - C
```

Merge means implementation reality changed.

Merge does not mean UX was verified.

## 6.3 Serialized reconciliation

Canonical matrix/account regeneration is a **convergence/post-verification responsibility** for this execution tranche, not a parallel coding lane inside each owner session.

The reconciliation operation, when run against accepted owner mains, must:

1. read exact merged diffs;
2. read declared plan/UX/capability impact;
3. reread current owner source/code/tests/contracts;
4. update implementation facts and refs;
5. detect added/removed/superseded capabilities;
6. validate UX relations;
7. validate available QL shape bindings structurally;
8. preserve evidence standing;
9. regenerate CSV/JSON/Markdown/account projections;
10. rebuild the O:I suite catalogue;
11. emit a reconciliation receipt/PR if source-controlled projections changed.

This lane is deliberately serialized to eliminate matrix merge-conflict pressure.

---

# 7. Evidence

Use:

```text
D — deterministic contract/unit proof
C — cross-product conformance
P — real provider / Harness
M — physical/material World
H — human experiential acceptance
```

Evidence attaches to meaningful experience/capability relations.

Minimum EV reading:

```text
ev_ref
x_refs
capability_refs
grade
subject_refs
environment_ref
suite_receipt
base_revision
result_revision
provider_refs
test_refs
artifact_refs
observed_result
status
source_ref
```

Laws:

```text
CI cannot claim M or H
fixture model output cannot claim P
running process alone does not prove Factory developmental relation
H requires EX
```

---

# 8. Development workflow

## 8.1 Enter

Start from:

```text
human request / authored source / issue / Commission
        ↓
plan_ref
        ↓
ux_refs
        ↓
capability_refs
        ↓
required evidence grades
```

Resolve the exact source basis before coding.

## 8.2 Resolve sufficient ground

This tranche requires only deterministic addressability and owner-correct retrieval of:

```text
plan
self-description/authored ground
relevant UX
capability records
architecture/contracts
exact code/tests
current evidence
Git state
```

The deeper intelligence traversal and REPL grammar are deferred.

## 8.3 Material development World

For managed O:I execution:

```text
Factory developmental requirement
        ↓
Workcell material placement / preparation
        ↓
AIKit Git capability establishes/binds exact-base Git reality
        ↓
AIKit Harness / SessionSpace resolution
        ↓
Actuation actual Agency
```

Preserve:

```text
Workcell binding != Git branch/worktree
Git branch/worktree != Run
Run != Journey
material relocation != semantic identity change
```

If GitHub/ChatGPT execution cannot truthfully be represented by the accepted Workcell contract yet, record it as external provider/material execution rather than fabricating a Workcell identity.

## 8.4 Implement and return

Git-backed development retains:

```text
exact base
current HEAD
branch/worktree relation
returned commits/diff
intended uncommitted return if any
verification performed
provider/material provenance
```

The PR records:

```text
plan_ref
ux_refs
capability_refs
required proof
affected owners
exact base/head
material/provider relation where known
```

Owner merges precede dependent consumer merges where contracts changed.

Returned result consists of:

```text
implementation result
EV evidence
remaining proof obligations
new pressure/conflict
Candidate / Recognition relation where Factory is active
```

M and H/EX arrive later from the machine pass.

---

# 9. Packaging and update architecture

The stale-binary problem is part of this protocol, not an operational footnote.

## 9.1 Three levels only

```text
PRODUCT REVISION / ARTIFACT
    native owner fact

SUITE CANDIDATE / CHANNEL
    coherent O:I composition

MACHINE INSTALL RECEIPT
    what one machine is actually running
```

Exact SHAs/digests/attestations remain mandatory for reproducibility but are generated into immutable receipts; they must not act as scattered hand-maintained runtime selectors.

## 9.2 Historical manifests are receipts only

Historical pre-local build records remain available as evidence and reproducibility fixtures.

They do not answer:

> what should this machine run now?

## 9.3 Suite channels

Required policy:

```text
stable
    latest ratified known-good suite

mainline
    latest remotely proven coherent set of accepted native-owner mains

source
    explicit developer-source composition/override
```

The latest independent commit/release of one product does not automatically become the current suite.

## 9.4 One active suite

O:I owns one canonical machine-level active suite receipt/root.

Conceptually:

```text
OI/
├── suites/
│   ├── <receipt-A>/bin/...
│   └── <receipt-B>/bin/...
├── current
├── previous
├── receipts/
└── channels/
```

Invariant:

```text
oi aikit ...
Desktop → oi aikit ...
TUI → oi aikit ...
Agent → oi aikit ...

all resolve the AIKit executable named by the same active suite receipt,
unless an explicit source/developer modality is active.
```

No hidden registered executable may silently override that receipt.

## 9.5 Incremental update

A new whole-suite receipt reuses unchanged immutable product artifacts and only acquires/builds changed artifacts.

Whole-suite coherence does not imply reinstalling all six products every time one advances.

## 9.6 Required O:I command surface

```text
oi suite status [--json]
oi suite check [--json]
oi suite update [--json]
oi suite repair [--json]
oi suite rollback [--json]
oi suite channel <stable|mainline|source>
oi where <product> [--json]
oi verify [--json]
```

Development Field whole-level commands may include:

```text
oi develop inspect [--json]
oi develop status [--json]
oi develop reconcile [--json]
oi develop debt [--json]
```

They compose owner-native operations.

---

# 10. Central machine account / Workcell relation

Central already owns `Control/machines/current.json` and its opaque `workcell:local` relation.

Extend machine intent with an opaque O:I suite policy/binding where needed; do not write observed installed versions into human-authored machine intent.

Effective machine account:

```text
Central
    machine identity / intended suite channel

O:I
    installed suite receipt
    available channel candidate
    installed-vs-available drift

Workcell
    material executable/process/environment observation
```

Preserve:

```text
authored desired suite policy
!= installed suite receipt
!= material executable observation
```

---

# 11. CI architecture

## CI-A — PR Development Field declaration

Every relevant PR validates:

```text
declared plan refs are syntactically/structurally valid
declared UX/capability refs have an owner/address
proof targets are valid
no EX is fabricated
generated canonical matrices are not casually regenerated
```

The next intelligence tranche may strengthen this into real Wiki resolution; do not pre-empt it here.

## CI-B — native owner implementation CI

Normal build/test/lint/fixtures.

Produces D where semantics warrant it.

## CI-C — exact cross-product conformance

Runs selected current owner-to-owner compatibility cases.

Produces C.

## CI-D — immutable product artifact publication

On accepted product main/release:

```text
build
→ native verification
→ version + owner revision
→ checksum
→ attestation/provenance
→ product artifact descriptor
```

## CI-E — O:I suite candidate resolver

O:I resolves selected accepted product revisions according to channel policy and only advances a channel after the required compatibility/conformance checks.

## CI-F — serialized Development Field reconciliation

Runs during convergence/post-verification from accepted mains.

It owns canonical matrix/account refresh for this tranche.

## CI-G — installed-suite fixture

Disposable environment proves:

```text
install exact suite receipt
all owner commands resolve through oi
active binaries match receipt
update to a new suite candidate
new command surface becomes available
rollback restores prior receipt
```

## CI-H — branch/material debt report

Convergence produces non-destructive reports for:

```text
orphan branch
orphan worktree
merged PR with surviving worktree
closed plan with live branch
stale stack
proof-only branch not retired
unaccounted material environment where visible
```

Cleanup decisions remain explicit.

---

# 12. Execution topology — eight sessions

This is the governing correction to the earlier plan.

**Do not run multiple implementation tracks inside one execution session.**  
Each session owns one repository/whole-level concern and completes that concern coherently.

Subagents may be used *inside that concern* for bounded inspection/testing/review, but they do not become parallel programme tracks with separate unresolved integration states.

The eight sessions are:

```text
S0  O:I whole-level Development Field + suite packaging/CI track
S1  Central source/self-description/machine track
S2  Actuation actuality/provenance track
S3  AIKit operative-resolution carrier track
S4  Software Factory developmental-technique track
S5  Workcell material-development-world track
S6  Quaternal Logic structural-carrier track
S7  O:I convergence + reconciliation + local proving handoff
```

S0–S6 may be launched as separate GitHub/ChatGPT conversations according to practical dependency state. Each must inspect live owner state and avoid overlapping another ticket's ownership.

S7 begins only when the selected accepted S0–S6 work is on native mains or explicitly classified as not required.

---

# 13. Session S0 — O:I whole-level implementation

O:I owns the cross-product envelope and packaging/update correction.

Implement in one O:I session:

```text
Development Field cross-product ref/declaration envelope
PR impact declaration schema/validator
suite channel model
immutable suite candidate/active-install receipts
single active dispatch root
installed vs available vs source-override reading
suite status/check/update/repair/rollback/where
mainline candidate resolution
historical manifest demotion to evidence-only
disposable install/update/rollback conformance
whole-level command parity checks
convergence hooks for later serialized matrix reconciliation
non-destructive debt-report command scaffold
```

Do not implement owner-specific capability semantics.

Do not perform documentation/matrix migrations or Wiki-intelligence work in this session.

Acceptance:

- historical manifests cannot select active runtime binaries;
- `oi <product>` resolves through the active receipt or explicit source modality;
- one changed product can produce a new whole-suite receipt without reinstalling unchanged artifacts;
- disposable update/rollback proves active command availability changes coherently;
- whole-level JSON status can explain machine channel, active candidate, product artifacts and drift.

---

# 14. Session S1 — Central

Implement in one Central session:

```text
root + ProjectCentral self-description aperture
stable six-tier self-description binding/manifest
retain-in-place source relations for existing documents
UX source relation
EX human-return source relation
source standing/provenance/read models
schema-compatible migration/doctor behavior
machine → O:I suite-channel/policy opaque binding where required
public Action/read surfaces for the above
```

Preserve current `user`, Agent governance/wiki, Skills/Methods and NOW semantics.

Do not move capability matrices/docs en masse in this session. Establish the target source contract first; physical/source migration happens in S7 or after the intelligence tranche.

Acceptance:

- existing Central/ProjectCentral worlds remain valid;
- new worlds can expose `self/`;
- six tier identities resolve without inventing replacement display labels;
- native source can be related in place;
- UX/EX provenance stays distinguishable;
- machine intent can reference O:I policy without embedding observed versions;
- no AIKit retrieval or Factory semantics move into Central.

---

# 15. Session S2 — Actuation

Actuation is deliberately narrow.

Inspect current Agency/WorldBinding/Activity/Return contracts against Development Field needs.

Only implement returned gaps necessary to preserve:

```text
developmental caller / Agency provenance
actual Activity relation
Run/Session correlation supplied by callers without identity collapse
Return/result provenance
material/provider movement not minting new Agent/Agency identity
```

If current Actuation already satisfies the Development Field requirements, the session should produce conformance/receipts and make only the smallest missing structured-output/CLI changes.

Do not invent a Development Field subsystem in Actuation.

Acceptance:

- Development Field callers can preserve actual actor/authority/Activity/Return lineage;
- absence of Factory relation is representable;
- external/direct sessions remain valid;
- provider/material identity never replaces Agent/Agency identity.

---

# 16. Session S3 — AIKit

This tranche makes AIKit ready to operate the Development Field without prematurely building the next intelligence system.

Implement only the substrate required now:

```text
address/read/index support for Central self-description and UX/EV carriers
stable ResourceRef/owner provenance for Development Field records
accepted QL ShapeBinding carrier ingestion/parity
Git/VersionedWorld basis needed by Factory/Workcell development worlds
bounded Development Field read/packet surface sufficient for deterministic consumers
CLI/application parity for those readings
source/developer-install modality interaction with O:I active-suite selection
```

Explicitly defer until the next intelligence spec:

```text
full Development Field Skill/Methods corpus
REPL/intelligence traversal grammar
retrieval helper scripts beyond immediate deterministic conformance need
performance-oriented Wiki traversal redesign
generative QL contemplation logic for Development Field records
```

Acceptance:

- AIKit can address/read the new carriers without a second store;
- QL structural bindings remain pinned to QL owner contracts;
- Git basis and source provenance remain exact;
- no new intelligence semantics are manufactured.

---

# 17. Session S4 — Software Factory

Make the Development Field a first-class software-development technique through the current Factory model.

Implement the smallest current-owner representation for:

```text
plan_ref
ux_refs
capability_refs
required evidence grades
operative AIKit resource/Method refs where current contracts allow
exact source/Git basis
material-development-world relation
returned evidence/candidate relation
```

Use current Commission/Journey/Run/WorkflowUnit/ExecutionDisposition rather than inventing `RunMode` unless live code proves a new type is required.

Factory owns the developmental reason and continuity; AIKit owns operational resolution; Workcell owns materiality.

Acceptance:

- a Commission/Run can state the intended difference against stable plan/UX/cap refs;
- exact source basis survives;
- material relocation does not change Run/Journey identity;
- returned diff/evidence correlates to the developmental work;
- direct non-Factory agency remains fully valid;
- no AIKit praxis engine is duplicated.

---

# 18. Session S5 — Workcell

Implement Development Field material-environment composition through existing Workcell contracts.

Required relation:

```text
development-environment demand
→ discover/plan
→ prepare/materialise
→ expose/bind
→ observe
→ collect
→ release/reconcile
```

Support, where current provider contracts naturally permit:

```text
collapsed local
isolated local material region
VM/sandbox/OpenSandbox
remote host
cloud/CI material execution
```

Expose stable material binding/provenance that AIKit/Factory can reference.

Git worktree/branch creation remains AIKit/native Git.

Acceptance:

- same developmental requirement can bind to at least two materially distinct fixture placements;
- binding identity and provider identity remain distinguishable;
- provider loss/degradation is honest;
- release/reconcile leaves inspectable outcome;
- no Git or Factory semantic ownership moves into Workcell.

---

# 19. Session S6 — Quaternal Logic

This session coordinates with the already-moving QL/intelligence grammar.

Do not fork or pre-empt the active grammar programme.

Its Development Field responsibility is to ensure the accepted canonical structural contract can express or cleanly extend toward:

```text
ShapeDefinition
ShapeBinding compatibility
positive partial/developed wholes
RelationField(A,B) over legitimate constituent shapes
shape-address non-fabrication law
derivation / Return provenance
portable language-neutral contract for consumers
```

Where the active QL grammar already lands these requirements, consume/ratify it and add only missing Development Field fixtures/conformance.

Do not implement the higher intelligence interaction grammar in this ticket; that is the next authored specification.

Acceptance:

- Development Field matrices can refer to actual QL shape/RelationField contracts without treating arbitrary dimensionality as QL;
- partial shapes remain valid;
- shape binding does not confer semantic truth or source authority;
- AIKit can pin the portable contract;
- no duplicate shape algebra is introduced.

---

# 20. Session S7 — O:I convergence and machine-proving handoff

This is expected to be the **local/Claude-led session**, because it owns both convergence and the transition into actual machine testing.

It must start by re-reading every S0–S6 owner issue/PR/main state.

## 20.1 Repository convergence

For every selected repo:

```text
inspect current main
inspect open Development Field PR/branch
verify owner acceptance
merge/rebase/converge as appropriate
rerun owner verification on main
retire proof/superseded branches where safe
```

No mixed branch suite becomes canonical.

## 20.2 Serialized Development Field reconciliation

Only after selected owners are on accepted mains:

```text
read merged changes since matrix/account basis
resolve affected capability records
resolve UX relations
validate QL shape bindings
refresh source/code/test/evidence refs
regenerate product matrix/account projections
regenerate O:I suite capability catalogue
produce reconciliation receipt
```

This is where matrix conflicts are resolved once, serially.

## 20.3 Source/document migration and cleanup

Now perform or plan the source migration against the accepted Central self-description contract.

Classify current documentation/account/matrix material:

```text
retain in place
relate into self-description
move/copy only where canonical Central ownership requires it
supersede
archive/retire
conflict
unresolved
```

Preserve authored source and historical evidence.

Do not undertake deeper Wiki intelligence restructuring here if the upcoming intelligence spec will determine it.

## 20.4 Branch/worktree/material debt

Run the debt census and reconcile:

```text
PRs
branches
local worktrees
Factory development refs
known Workcell environments
proof-only integration states
```

Retire only after provenance and accepted difference are safely reachable.

## 20.5 Suite candidate and local installation

From accepted mains:

```text
build/publish/resolve required artifacts
form mainline suite candidate
install/update active suite
verify active receipt
verify all six oi dispatch paths
verify installed vs available vs material observation
```

The stale-binary condition is a hard failure.

## 20.6 Physical/material proof

On the actual owner machine:

```text
inspect/migrate ~/Central
adopt/read current machine role
inspect workcell:local
run oi suite status/check/verify
exercise source/developer modality
exercise one real local development-world binding
exercise real Git/worktree basis
exercise one real Harness/provider path
launch TUI
launch Desktop
```

Record M evidence separately.

## 20.7 Human EX

The human performs the experiential walk and records EX.

Only then may H be claimed.

## 20.8 Deferred intelligence handoff

At closure, produce an exact packet for the next QL/intelligence specification:

```text
accepted carrier refs
accepted shape contract
Central self-description source state
AIKit addressability/read surfaces
current Wiki/Search/Resolve behavior
current matrix/account state
retrieval/performance gaps
candidate scripts/Skills/Methods gaps
unresolved intelligence questions
```

This next tranche owns the Wiki REPL/intelligence grammar and agent praxis improvements.

---

# 21. Work explicitly deferred to the intelligence tranche

Do **not** scatter these across S0–S6:

```text
full Wiki/Development Field REPL interaction grammar
QL-oriented intelligence navigation
shape-driven contemplation/generation over Development Field records
Development Field Agent Skills/Methods
retrieval helper script suite
Wiki traversal performance redesign
familiarity/route learning for Development Field navigation
rich graph projections of the full Development Field
```

These may be inspected during owner work when a concrete blocker appears, but implementation waits for the next authored intelligence shape.

---

# 22. Work deferred to S7 / post-verification

Do not turn these into parallel owner-session programmes:

```text
bulk document → self-description migration
bulk capability-matrix relocation
generated account cleanup
historical documentation debris retirement
cross-repository matrix regeneration
global branch/worktree cleanup
large-scale link repair
```

S0–S6 should only make the source/contracts ready so S7 can do this safely against accepted mains.

If the intelligence tranche supersedes part of this migration plan, follow the newer authored relation rather than completing cleanup for its own sake.

---

# 23. Dependency and merge law

The sessions are independent owner tracks, but cross-owner dependencies still matter.

General rule:

```text
owner contract lands
        ↓
consumer rebases/reconciles
        ↓
consumer acceptance
        ↓
O:I convergence
```

Expected dependency pressures:

```text
QL structural contract
    → AIKit shape consumer

Central self-description/source contract
    → AIKit carrier reads
    → Factory source refs

AIKit Git/resource contracts
    → Factory / Workcell integration

Workcell material binding
    → Factory material-development relation

all owner mains
    → O:I S7 reconciliation/candidate
```

Do not impose seriality where two tracks genuinely do not overlap.

Do not merge consumer assumptions against branch-only owner semantics.

---

# 24. Required acceptance before S7

S0–S6 are ready for convergence only when:

- their accepted implementation is on native `main`, or explicitly declared not required;
- native verification passes on `main`;
- owner docs/read models/CLI expose the new seam sufficiently for consumers;
- no active semantics are available only on an abandoned feature branch;
- machine-only gates are explicitly named rather than simulated.

---

# 25. Final closure

The Development Field v1 tranche closes when:

## Architecture

- Central has a first-class self-description source aperture;
- the existing six document tiers can bind into it without forced migration;
- UX and EX are separately addressable and provenance-bearing;
- capability records relate to UX without becoming test names;
- Development Field objects can carry canonical QL structural bindings without fabricated semantics;
- Factory carries development as developmental meaning;
- AIKit carries current operative resolution without prematurely implementing the next intelligence grammar;
- Workcell carries material development-world bindings;
- Actuation preserves actual Agency/Activity/Return lineage;
- O:I composes the whole without duplicating owners.

## CI

- feature PRs can declare Development Field impact;
- native implementation CI and cross-product conformance remain distinct;
- canonical matrix/account refresh is serialized at convergence;
- D/C/P/M/H remain distinct;
- install/update/rollback has a deterministic fixture.

## Packaging

- product artifacts have maintained immutable identities;
- O:I resolves coherent stable/mainline/source compositions;
- historical build receipts cannot select active binaries;
- all O:I product dispatch paths use one active suite receipt or an explicit source override;
- installed-vs-available drift is legible;
- update can advance one product without pointless reinstallation of unchanged artifacts.

## Convergence / physical

- selected work is on native mains;
- capability/account projections are regenerated from accepted state;
- document/source migration is performed or explicitly deferred under the new source contract;
- branch/worktree debt is accounted;
- the local owner machine runs the intended suite rather than stale binaries;
- M proof is recorded from the actual machine;
- H is recorded only through human EX;
- one clean intelligence-system handoff packet exists.

The returned whole should make development itself an ordinary first-class act inside O:I:

```text
mean something
→ express the intended experience
→ know which powers must exist
→ carry a developmental difference
→ act in a real material World
→ return evidence
→ recognise what actually happened
→ renew the World where warranted
↺
```
