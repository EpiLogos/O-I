// Specimen contribution file — test material for the oi.package/v1 proposal.
// It renders nothing real; it exists so a future loader has a fixture that
// was written before the loader.
export function WorkbenchPanel() {
  return null;
}
