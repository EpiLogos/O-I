# Bounded architectural alignment packets

**Report/specification only — 19 September 2026.** These packets restore the relationships checked in [SYNTHESIS.md](SYNTHESIS.md); they are not implementation, new issues, a replacement Wayfinder, a new acceptance programme or permission to deploy. Execute subsequently through the existing [O:I #65](https://github.com/EpiLogos/O-I/issues/65), [native integration owner #220](https://github.com/EpiLogos/O-I/issues/220), native owners and [current UI relationship #375](https://github.com/EpiLogos/O-I/issues/375). No issue state changes are requested by this document.

**Input limit:** `03-actuation.md` was still absent at the selected main snapshot. P08 specifies a checked cross-owner operation, not a complete Actuation repair programme or an invented AC report. Missing Actuation coverage prevents an all-six review-complete claim; it does not block independent, already-established repository corrections. The other reports' incomplete coverage remains in SYNTHESIS §6.

**How to read a packet.** Named tests below are proposed discriminating cases, not claims that those test functions already exist or were executed in this synthesis. Reuse an existing equivalent test where available. Begin with the original public entry and a negative that detects the actual defect; do not train the actor on the expected answer, bless a matching mock or change the source requirement to make a test pass. A completed code packet and a completed installed/human episode are different returns.

## 1. Exact source basis and environment vocabulary

### 1.1 Revision aliases

The aliases below make module assignments readable; every occurrence denotes this exact commit, not a moving branch. The original baseline, report blobs and returned Mac vector are in SYNTHESIS §1.

| Alias | Repository and immutable comparison |
|---|---|
| O | EpiLogos/O-I `31a392ff3fd9d527fe4a8d9bc406eb836669441f` |
| C | EpiLogos/Central `811eb5eb2264ca0f19b1d83c0b6f669b2112695d` |
| A | EpiLogos/Actuation `47f4fa3c184850e254ef5089696bf5fbbad907ec` |
| K | EpiLogos/ai-kit `f4aa38ad4e9e0c0099979fe0acccefd447d3f77d` |
| F | EpiLogos/Factory `bbb8f48943cf1cf398005ae40ee8fa29c111c3a5` |
| W | EpiLogos/Workcell `04160f51d37e78d600f32440b4f5275c02b78e9c` |
| U | O:I current published parallel-UI substrate `7747d5e67d2814a29a44788d0b901750cb553215` |
| C161 | Central #161 existing continuation `7bc00200e75427d69f726e2df94d669699637b0b`, still unmerged at this reading |
| FB | Factory existing developmental build-read work `93b8fa6af4be4f67bf02b417b8a181fd4a1a7d83` |
| OB | O:I corresponding donor `6a450b7467c84f928c8806b2e9f880d520b4795d` |

The actual Mac receipt is [#65 comment 5743244181][mac]. It names O:I `d6d92503a1d25e38c93aa0f5e11a306be2adb093` and the other owners at the original baseline. It is not O/K above. It preserves old residents and defers App/Cradle and physical acceptance. AIKit #344's new main-binary SessionSpace implementation is **already repaired in K**, not yet proved installed or loaded by that receipt.

Before a later implementation begins, resolve its actual branch and inspect only intervening changes to the packet's claimed modules/contracts. Reconcile unique work without moving or overwriting the protected local UI. Do not repeat the whole review or chase main indefinitely. A later selected revision belongs in that implementation's return.

### 1.2 Execution classes

**REPO / CI** means subsequent ChatGPT/GitHub implementation and normal checks: native code, schemas, source/Skill corrections, pure reducers, actual serialization/argv tests, disposable files, controlled subprocesses and browser fixtures. It does not mean the user must run everything manually on a personal machine. Normal native CI can establish the OS behavior it actually exercises.

**NATIVE / OMARCHY / SANDBOX / PROVIDER** means the specific real filesystem, process group, provider, persistent service, harness edition, isolation backend or model is necessary. Use existing available facilities, scoped credentials and disposable Worlds; retain evidence outside guest deletion. A fake provider is useful for deterministic faults, not proof that a named real harness loaded practice.

**MAC / HARDWARE / HUMAN** means actual managed Mac artifacts, GUI launch/PATH, Keychain, target-native restart and source loading, physical two-host correspondence or real human writing/readability/control judgment. These limits apply to the relevant proof only, not to all preceding code work. No premature general human-ratification gate is introduced.

### 1.3 Work and preservation rules

Use **one reusable code worktree per coherent feature line in each participating repository**, not per agent, task, finding, retry or test. Multiple agents may share that feature line with explicit file claims and serial Git/index/checkout/commit operations. Compatible cached builds can be shared safely. Disposable runtime Worlds, guests and provider allocations are a separate resource decision, not a reason to proliferate code checkouts. Keep the Mac usable as the personal interface/acceptance machine; Omarchy can carry native build/provider churn.

Do not delete user source, historical receipts, native identity, unknown document fields, valid old-generation records, dirty files, foreign hooks, native Skills or the existing UI to simplify an alignment. Preserve QL/M/M′ and the held installed QL basis. No packet can claim broader cancellation, source disclosure, model/provider fallback or teardown authority than the existing native contract and accepted commission.

## 2. First corrections: preserve the reviewed choice and active installation

### P01-U — Managed installation, exact activation and coherent entry points

**Existing owner and source basis.** O:I #65/#220, the current-cut section of [harness-first adoption §5][harness], the existing managed update flow, and the real [Mac return][mac]. Restores SYNTHESIS C10; findings **OI-001, OI-002**, with **AI-001 source repair retained rather than reimplemented**. This is one installation feature line. P01-C below is independent despite the adjacent numbering.

**Producer/consumer modules and revisions.** O `cli/src/update_flow.rs` (`stage_and_link`, `apply_entry`, `plan_product`), existing managed receipt/activation and product-dispatch paths; K `crates/aikit-cli/src/cli.rs`, `main.rs`, `session_space_cli.rs` and thin `bin/aikit-session-space.rs` are the already-landed receiving implementation. The O:I installer/bundler and owner declarations must be inspected for actual required companion/resource closure, not reduced to the primary executable. Source paths and direct comparison are linked in [SYNTHESIS C10](SYNTHESIS.md#c10--installation-establishes-material-correspondence-not-loaded-continuity).

**Complete positive behavior; prohibited regressions.** Stage an exact selected committed cut without modifying an occupied/dirty worktree. Verify its artifact and native smoke result before any existing active path points at it. A current receipt must agree with the actual managed artifact and the complete activation chain, not merely executable files at three names. Update only the selected owners while preserving held entries, old resident processes, resources and supported compatibility. Do not introduce a new installer, hand-copy a missing executable, remove the folded SessionSpace alias, or infer new resident/loaded currency from a CLI version.

**First failing/discriminating case.** In a temporary managed root with a known good active generation, make the next build produce a deliberately failing smoke response. Assert that the complete old active chain still resolves to the old digest after refusal. The current link-before-smoke path should fail this assertion. Separately replace a bin/activation link with a different executable while leaving the recorded artifact healthy; `plan_product` must not say current. These two cases catch ordering and correspondence independently.

**Smallest coherent correction.** Split verified staging from activation publication inside the existing updater. Establish a validated candidate artifact first, then publish the selected link changes and receipt with a recoverable, explicit boundary. Check resolved link-chain identity and recorded digest, including mismatched or broken intermediate links. Reuse existing content-addressed generations, atomic link helpers and exact candidate selection. Specify honestly what can become partial when publishing multiple owner entries; do not claim a distributed transaction or a data rollback the updater cannot supply. Check both SessionSpace entry spellings against K's shared implementation, rather than constructing another companion route.

**Migration, compatibility and preservation.** Read existing receipts without restamping their provenance. Incomplete/old correspondence becomes a named need to verify/reconcile, not automatic broad rebuilding or deletion. Keep prior artifacts useful for supported rollback and preserve all unselected product receipt entries. Test interrupted activation/receipt publication and repeated recovery; recovery must not falsely advertise a source cut never built. No live user update is part of the repository test.

**Receiving interaction and explanation.** CLI/TUI/desktop update plan reports selected cut, what is staged, verified, activated, retained and still resident. Proposed copy: “The new binary is verified; this running session still uses the previous generation. Your work has not been restarted.” A failed update names whether activation stayed unchanged or a known partial publication needs recovery; it does not end at a diagnostic JSON dump.

**Independent replay and recovery proof.** Fail build, digest, smoke, second-owner update and receipt write separately. Prove held QL/source/UI/resident state unchanged. Replay the same selection without unnecessary duplicate builds. In native follow-up verify declared companions/resources and GUI/headless resolution, then explicitly continue/rebind only the chosen resident and inspect its real generation. A stale PATH executable must make correspondence proof fail.

**Dependencies, write boundary and environment.** REPO/CI now; native source-install tests on suitable Linux/macOS CI; actual selected Mac install/resident continuation in P12. No dependency on P02–P11 to repair updater ordering. P06 owns Skill content; P01-U may test its packaging but must not edit the same Skill bodies. Candidate installation must not falsely mark the current active UI accepted.

### P01-C — Apply the exact reviewed owner plan

**Existing owner and source basis.** O:I #299's established Configuration Plane under #65/#220; [configuration §§6, 8–10][config]. Restores C09 / **OI-003**. This is a separate configuration feature line and may run concurrently with P01-U.

**Producer/consumer modules and revisions.** O `desktop/cradle/src/configuration/liveSource.ts`, typed configuration source/transport, `desktop/cradle/kernel/src/configuration.rs`, and the existing CLI configuration engine. Native producers are their selected C/A/K/F/W contributions/plan/apply operations. The owner contract already accepts an opaque owner plan via native `config apply --plan-file`; do not assume an O:I engine flag exists without inspecting it.

**Complete behavior and exclusions.** A person/agent sees owner plan A, including source basis, target scope and expected effect; Apply submits that exact plan through the same native engine. A changed basis is rejected or returned for explicit new review. Preserve O:I sparse desired state, profiles, legitimate shared composition, owner setting identity, desired/declared/effective/active distinctions, secret references, per-operation partial failures and restart-required states. Do not eliminate shared configuration, duplicate owner plan recipes, or silently generate plan B on Apply.

**First discriminator.** Obtain plan A through the live source; change the native owner's basis or expected effect before clicking Apply. Spy on the actual owner transport: the submitted plan must be A, and a stale/refused result must return. The current request-only replan path should fail this test. Replaying an executed `(owner, changeset, setting, scope, plan_digest)` must return the original no-op receipt without re-planning or effect.

**Smallest correction.** Carry the owner-minted plan and its identity from preview through the typed kernel request to the existing engine/owner apply transport. Keep validation at the owner; O:I does not recompute a foreign digest. Preserve a deliberately named noninteractive plan-and-apply route where existing callers require it, but do not use it to implement a reviewed-plan UI action. Use the current ChangeSet and receipt stores, not a second configuration journal.

**Migration/compatibility.** Existing desired/profile documents and native history are unchanged. Older request-based callers retain explicitly documented semantics or receive a versioned unsupported-operation result; they must not masquerade as reviewed-plan apply. Historical receipts stay historical. No credential material enters a plan, argv, evidence artifact or UI inspect body.

**Receiver and explanation.** Current Settings/Profile UI, CLI structured use and agents show “This plan is stale; review the changed effect” with the same named setting/scope, not generic failure or silent fallback. Multi-owner partial apply shows each owner's real result and the supported next action; no invented global rollback.

**Proof and placement.** REPO/CI: native plan round-trip, stale plan, replay, denied/unknown owner, unsupported scope, partial apply, deferred restart and external native edit. Native current-candidate UI proof only after the contract lands. Claim the configuration transport/types/engine; coordinate any shared `KernelOp` edit with P02/P05/P08 through one integrator. No semantic dependency on installation repair or optional cache work.

## 3. Preserve authored ground and complete the document/receiving relation

### P02 — Continue the existing Day/Flow and receiving line into the current UI

**Existing owner/source.** Central [#150](https://github.com/EpiLogos/Central/issues/150), [#153](https://github.com/EpiLogos/Central/issues/153) → [#151](https://github.com/EpiLogos/Central/issues/151)/[#152](https://github.com/EpiLogos/Central/issues/152), existing [#161 continuation][c161], O:I #220/#375/#65; [document operations][documents], [current UI handoff][ui]. Restores C05/C07/C08; **CT-004, CT-005 and the root-receiving part of CT-006**. This is integration plus bounded missing carrier/consumer work, not a fresh document product.

**Affected modules/current bases.** Producer C `ctrl/src/continuous_work/documents.rs` and existing receiving/temporal modules; reconcile C161's actual seven-file delta and `docs/CAW-NATIVE-CONSUMER-INTERFACES.md` against C rather than merging its old base wholesale. Consumers O `desktop/cradle/kernel/src/flow.rs`, `src/flow/instance.ts`, `src/receiving/client.ts`; U `src/receiving/DocumentReturns.tsx`, `src/contributions/factory/sidebar/ContextPlane.tsx`, and current original-document render/editor bindings. `src` here is under `desktop/cradle/`. Native Factory `attempt_receiving.rs` and AIKit temporal/placement consumers join by existing SourceRef and Return identities, not cloned ledgers.

**Positive behavior; non-negotiable preservation.** Original Day/Flow writing remains the source. Retain exact field identities, authored text, blank ordered entries, notes/replies/anchors, protected human contributions, unknown data, declared media and export/reopen semantics. Central owns history/CAS/receiving. A Return can arrive while the app is closed and be read at root or child scope, reviewed on its current source basis, acknowledged separately, accepted separately, included once or recovered without duplication. Preserve occurrence and receipt clocks. Cursor pages and partial/denied/unavailable/stale states must be visible; arrival never completes a human task or rewrites Day prose automatically.

**First discriminators.** (a) Put the open document's matching Return after the first 50 records; it must still be reachable. (b) Switch from a child Project to root while a child request is delayed; the root must call its real owner scope and never render the delayed child data as root. (c) Inject denied/unavailable receiving: the user must not see empty success. (d) For the C161 integration, execute the existing native recovery/replay cases on the reconciled tree before claiming its behavior landed. These can run independently of original HTML rendering.

**Smallest coherent correction.** Integrate C161's existing source-basis recovery, notes/entries and fair root/multi-Project receiving as a delta, retaining C's newer native work. Then complete original-carrier handling within the same document/source operations. Pass genuine optional NOW/Day/Run/session lineage through typed frontend/kernel inputs; do not synthesize it. Traverse owner cursors or use an already-supported owner filter with honest coverage, reset state by scope, and guard async response generations. Read failures are explicit receiving standing, not unconditional component disappearance. Reuse existing review/include/recover operations and the current Context/Inspect affordances.

**Source fixture and migration boundary.** #65 retains the exact original Day and Flow artifacts: Day 466,929 bytes / SHA-256 `c8e81e8a03ce526ab1421908d5e45054fb1d76ea9a19572989061c7c8822bd64`; Flow 44,191 bytes / SHA-256 `2f251b18360d44b340f34ae5a121b0e70257ad06ee061772c709d6092d9ac1dc`. Use those actual originals for their seventeen-key/layout fidelity case, not synthetic keys with the same count. Missing original bytes block that exact fidelity proof, not independent native/media/import coding. Test the existing 12 MB per-file media rule, original-template import, retained-copy restore and explicitly supported new-World adoption without granting imported metadata authority. Preserve source identity, native operation history, lifecycle and authored unknown fields; do not reset a World to obtain green.

**Receiver/human explanation.** In the current editor/canvas, a selected text/component becomes minimal active context with a subtle selection state, not a replacement participant modal. A document Return appears beside the document and by the same native ref in Context/Inbox. Root is labeled as root work, not a fake Project. Proposed status: “Received; not yet included. The document has changed since this proposal.” The person can inspect source/target, accept the current basis, defer, reject, include or recover through actual native actions. Expose exact explanation without forcing raw provenance onto the main writing surface.

**Independent/recovery proof.** External edit → conflict → human-authorized reconcile → exact retained source history → replay; denied agent reconcile; interrupted source/metadata publication; stale anchor retained rather than relocated; late Return and revocation before inclusion; accepted-but-unincluded survives archive/re-entry; cursor fairness with a busy root; original media/export/reopen on the actual standalone carrier. A disconnected include handler must fail the visible operation test. Keep C161's retained native logs attributed to their actual test-merge, not the new candidate.

**Dependencies/conflicts/environments.** C161 delta reconciliation is REPO/CI now, and does not depend on live models, Mac installation or P03's new adoption transaction when using an already-valid disposable World. Full receiving UI needs the selected native cursor/lineage contract first; typed fixture work can run concurrently but is not completion. P07 owns hook/search adapters; P02 owns root receiving and document Context UI. Share short claims for Central common Action descriptors/source-history and O:I kernel types with the existing integrator. Native filesystem and browser runtime tests on Omarchy/CI; original Mac rendering/writing/selection feel and actual late remote Return in P12. Preserve current unpublished UI work; publish/reconcile its actual source before claiming the final app proof.

### P03 — Source-preserving Project adoption with truthful recoverable registration

**Owner/source.** Central's existing Project lifecycle, source/recognition and federation owner under O:I #65/#220; [harness-first §2B–C][harness] and [Central report CT-002/CT-003][ct-report]. Restores C02. The findings are one adoption feature line with two necessary tests, not two new Project registries.

**Modules/revisions.** C `ctrl/src/projectcentral_ops.rs`, existing manifest/provenance and source-safety helpers in `file_map.rs`/`source_history.rs`; downstream K `crates/aikit-adapters/src/projectcentral.rs` and Central SourceRef reader. Current Central changes elsewhere do not repair the unlocked `ensure_root_federation` writer.

**Positive behavior/forbidden regressions.** Recognize, preview and adopt existing directories in place, retaining the original authored Wiki and files. Concurrent independent adoption must not corrupt shared federation; same-Project retry must preserve one identity and disclose partial state. A selected source's accepted location/standing must agree with what the downstream reader can retrieve. Preserve explicit legitimate external-source registration and exclusions; do not treat every symlink as consent, copy outside source into a new hierarchy, or silently weaken AIKit's withholding.

**First cases.** Reuse the annex's concurrent public `projectcentral.init` entry on multiple disposable existing directories, then add controlled interruption after manifest publication and before federation. Resume the same Project and inspect through AIKit. All successful publications must be readable; failure must disclose any already-created identity/stage. Separately preview a harmless lexical alias to an unregistered outside source: it must either refuse with a meaningful reason or enter the explicit native external-source relationship; it must not advertise an ordinary readable in-place source that AIKit later withholds.

**Correction.** Protect only the necessary shared federation read/modify/write, atomically publish it, and make the existing adoption stages recoverable/idempotent with identity/basis checks. Reuse native safe-write/locking/provenance mechanisms after checking their scope, rather than locking the whole workspace. Use the native source-location policy for selection/preview/readback consistently; do not add a consumer bypass.

**Preservation/migration.** Existing manifests, successful registrations, unknown Wiki fields and original bytes survive. Reconcile an interrupted old adoption by inspecting actual native state rather than deleting its manifest or minting a second Project. An unreadable existing artifact is not absence. The earlier 7-success/5-failure result demonstrates concurrent parse/partial-failure behavior, not loss of successful registrations or private-source disclosure; retain that evidential boundary.

**Receiver/proof.** Native CLI, agent and adoption UI return the same Project/source refs, preview, partial standing and exact recovery action. Proposed copy: “Your files are preserved. Project identity was created; federation registration still needs recovery.” Prove distinct/same-Project concurrency, every interruption point, stale source substitution, exclusion, permitted external source and idempotent retry through the downstream reader. No actor receives private test canaries as instructions.

**Placement/dependencies.** REPO/CI; native filesystem recovery on Linux/macOS later. No personal migration now. Independent of P02's valid-World document tests and P04–P10. Coordinate any common source-history/Action descriptor edit with P02; this is a write conflict, not a reason to sequence whole features.

## 4. Developmental work: prove actual actors, evidence and the current UI join

### P04 — One trustworthy Factory verification/admission relation

**Owner/source.** Existing Factory workflow/independent-reconstruction and attempt lifecycle work, including [Factory #222](https://github.com/EpiLogos/Factory/issues/222), under O:I #220/#65; [Factory report FA-002/FA-003][fa-report] and [harness-first §3][harness]. Restores C06. The source basis is the existing independent-verifier/Return architecture, not a proposal to make every Run await a new human gate.

**Modules/revisions.** F `factory/src/attempt_runtime.rs` (`VerificationReceipt`, `validate_verification`, `has_passing_verification` and late-result incorporation), `attempt_application.rs` (ordinary `ReturnArtifact` and `IncorporateLateResult`), existing orchestrator/independent verification and artifact assessment producers. Current O:I/AIKit Run consumers must display the returned standing without inventing verification.

**Positive behavior/prohibited regressions.** Verification must identify the exact subject/candidate/source/execution and the real independently authorized verifier relation, not rely on nonempty caller labels. Admission uses the current covering verification and actual reconciled effects; an earlier pass is not enough after a later failure or unknown. Preserve native Run/CAS/convergence, retry budgets, explicit resolution changes, independent reviewer work, historical evidence and strong ordinary Return guards. No automatic provider/model substitution and no treating every process exit as successful work.

**First discriminator.** Create the actual late-result scenario through the application API: a covering pass, then a later failed or unknown verification, followed by attempted late incorporation. Expect refusal while retaining the late material. The current any-historical-pass predicate should fail this test. Separately submit an actor-asserted verification with plausible strings but no corresponding independent subject-bound evidence; it must not become admissible. Keep a legitimate independently produced current pass as the positive control.

**Smallest correction.** Reuse the existing independent verification/assessment identities and producer path. Bind the verification record to its actual subject, source/candidate revision, execution and verifier provenance at admission, then use one semantically consistent current-evidence predicate across ordinary Return and late-result incorporation. Preserve each path's legitimate differences rather than mechanically sharing every function. This packet does not prescribe a new verifier service, credential scheme or parallel evidence ledger; the implementer must use the actual existing native identity/authority contract.

**Migration/compatibility/source preservation.** Retain historical receipts exactly. Records lacking newly necessary bindings can remain inspectable as historical/unbound evidence, but cannot be retroactively upgraded by stamping new labels. A compatibility reader may resolve a binding from real retained owner evidence where unambiguous; otherwise fresh verification is required for admission. Keep unaccepted late artifacts and uncertain effects available for recovery, not discarded as failed noise.

**Receiving interaction.** Run/Tasks/Context should say what was verified, by which actual relation, against which revision, and why an old or independent-looking receipt does not carry. Proposed copy: “A later check no longer supports this result. The returned material is retained; it has not been incorporated.” Agents receive typed refusal and the missing proof, not a demand for the person to adjudicate routine technical dependencies.

**Independent replay/negative/recovery.** Verify exact-candidate mismatch, stale source, actor impersonation, revoked verifier authority, pass→failure, pass→unknown, unresolved execution, repeated late result and concurrent admission. Replay the ordinary Return suite unchanged to prove it was not weakened. Actual independent model/harness verification is NATIVE/PROVIDER; repository tests establish binding semantics with controlled native records, not the real model's competence.

**Dependencies/write claims.** REPO/CI now. Claim attempt runtime/application and their evidence types; P05 claims workflow external refs/build/read/consumer, but any shared Commission/attempt shape must be integrated once. P04 is a real prerequisite for a final proof of trustworthy completed Factory Return, not for P05's pure build rendering or native reference test. No dependency on cache work or a Mac install to implement it.

### P05 — Native Commission identity, build/task read and current Desk execution

**Owner/source.** Existing Factory [#247](https://github.com/EpiLogos/Factory/issues/247), workflow/Commission ownership [#195](https://github.com/EpiLogos/Factory/issues/195), O:I #220/#375/#65 and [current UI handoff][ui]. Restores C01/C02/C06/C08, **FA-001, FA-004, FA-005 and OI-004**. Retains C12's [published expressive-craft extension][expression] within the same existing development line; QL/M/M′ internals are not touched.

**Modules/current bases.** F `factory/src/workflow.rs`, `core/identity/ref.rs`, `build.rs`, `developmental_read.rs`, existing build/task CLI and public read producers; FB already contains developmental build-read work. O `desktop/cradle/kernel/src/factory.rs` (`build_snapshot`, `development_read`), typed Factory consumer and real transport tests; OB is a delta donor. U/current published replacement for the later local Desk/Tasks source is the destination, with existing Run / Agents / Context, composer and native selection state. C `ctrl/src/world.rs` provides the checked `agent/…` native relation grammar; K's actual Factory/Agency resolution adapters remain its consumers.

**Positive scope and forbidden regressions.** A Commission may require the actual native Agent by its native identity. Local Factory `kind:ULID` records remain strict, but external refs are not coerced into them. A real Factory build snapshot reaches the UI using its actual serialization and version, with the complete developmental build/task/Run relationship. The current Desk shows the whole parallel Run and its real execution, artifacts and receiving; Tasks/chat retains appropriate context and continuation. Direct work stays Direct. Keep draft/editor/selection state and current UI shape; do not merge an old donor shell wholesale or equate a pretty task card with an executed attempt.

**First discriminators.** (a) Submit a valid current Central `agent/…` reference through the public workflow/Commission path and verify it survives resolution unchanged; an invalid Factory ULID still refuses. (b) Serialize an actual F `FactoryBuildSnapshot` and pass those bytes through O `build_snapshot`; it must accept `providerContract` and still reject a wrong contract/version. The existing consumer check for `provider_contract` is wrong. (c) Request the native build/task read through the real generic route; this separately detects the missing allow-list/handler join. A test fixture containing the wrong spelling must be corrected, not made the producer's new authority.

**Smallest coherent correction and integration.** Introduce or reuse owner-aware external-reference validation at the existing foreign-reference fields, without changing local Factory identity or minting substitute Agents. Fix the consumer's actual producer contract. Reconcile FB/OB's existing read delta against current F/O and the current Desk; inspect the exact changed public requests and receiver before reusing it. Connect Commission start, native readback, attempt/model/material/evidence and output/Return to the existing current UI controls, preserving source owner semantics. An existing implementation awaiting integration is not a blank mandate to rebuild.

**Complete intended expressive scope.** Where this existing feature line receives typed Vāk craft, retain its `*.vak.ts` source/import/compiler/bounds basis and pre-dispatch checks; compilation alone grants no effect. Real participant selection must resolve native Agent/Agency/context/tools/body/Return, not just labels. Keep lean operative Skills distinct from development history and keep maintenance practice discoverable. The current Expression/Library face must use the actual live repertoire, not a tiny legacy subset or per-frame LLM loop. This is a retained source obligation: do not claim it complete from report publication, nor invent an unrelated compiler/runtime or enter protected QL internals to fill a gap. First discriminate changed-source/import/bounds invalidation and disconnected invocation through the existing contract before expanding the receiving integration.

**Migration/compatibility.** Keep original Commission/Run/WorkflowUnit refs, artifacts, native source and old valid read records. Foreign refs remain opaque to owners that do not own their grammar. Version any genuinely incompatible public request/read change and pair producers/consumers; do not silently rewrite persisted IDs. Preserve old read-only compatibility where justified, not unreachable duplicate authorities. Published FB/OB ancestry is divergent; port the relevant delta, not unrelated work.

**Receiver/human explanation.** Current Desk/Tasks Run / Agents / Context show subject, actual participant and model, source selected versus loaded, material location, useful current state, outputs and genuine Needs-you returns. Empty composer/agent interaction remains available without an existing Run. A unavailable producer/control is named and disabled only for that operation, with supported recovery. Proposed explanation: “This is the native Run and its parallel work. This task has not started; these outputs came from the completed attempt.” Native CLI/TUI/agent entry must produce the same refs/effects, not the same layout.

**Independent proof.** Use the actual producer binaries/serializers, not a consumer-created specimen. Test declined permissions, unavailable model/place, concurrent different Agents/models, cancel versus completion, non-Factored Direct activity, hidden/missing owner, source change and interrupted Return. A disconnected start/control handler must make the user-level test fail. Preserve native existing tests and require a final real mounted current-UI episode with actual useful output; existing local walkthroughs are retained but not relabeled as that proof.

**Dependencies/conflicts/environment.** Identity and wire tests/corrections are REPO/CI immediately. Existing build-read producer contract must land before claiming its complete UI receiver; UI fixture construction may run in parallel. P04 is needed for completed verification/Return admission, P08/P09 for their affected stream/material evidence, not for independent read/selection work. P02 owns source/receiving mutation and Context return-list mechanics; P05 consumes those refs, not another inbox. Shared kernel types and Factory attempt shapes get one integrator/file claim. Actual provider and Mac UI proofs are P12. No full-feature merge of protected local UI is authorized by this specification itself.

## 5. Practice and context must actually reach the chosen body

### P06 — One source-aware Skill editing and apply/activation relation

**Owner/source.** AIKit [#114](https://github.com/EpiLogos/ai-kit/issues/114), existing Central repertoire [#164](https://github.com/EpiLogos/Central/issues/164), O:I #65/#220; [harness-first §2D–G][harness], [held guardian manifest][guardian], [current UI Agents/Context contract][ui]. Restores C03; **AI-002, AI-004, AI-007**. The September 17 author ruling recorded in the manifest governs the O:I-held strap source; older two-member prose is not a reason to remove it.

**Modules/current revisions.** K `crates/aikit-cli/src/skill_sources.rs`, shared `app/mod.rs`, `main.rs` apply tail, `client.rs` hook seams, `crates/aikit-tui/src/application_service.rs`, existing native target adapters/generation/trust. Producer C `ctrl/src/control_skills.rs` and file-map/source standing remain native. O `skills/suite-operator/skillset.toml`, `skills/suite-operator/SKILL.md`, `skills/central-session-strap/SKILL.md` and the directly contradictory operative instructions; actual current UI source-opening/edit/Agents/Context seams receive them. No private Control text enters public repairs.

**Positive behavior; prohibited shortcuts.** Discover purpose/trigger → inspect source/scope/revision/trust → edit writable native source or propose change → validate/review that revision → resolve selection → project through actual target → activate/reload/restart as supported → prove later effective use. Preserve immutable snapshots, strict new projection checks, per-scope selection, native AgentSet distinct from SkillSet, foreign hook/config entries, inherited/default/task scope and explicit unavailable capability. Method remains a Skill whose description begins `METHOD:`. Never mark written/linked as loaded, auto-trust a changed revision or treat source editing as permission to invoke all Guardians.

**First discriminators.** (a) Make one owner-backed Skill snapshot stale while unrelated local/other-owner catalogue/history remain valid. An unrelated read must succeed with named withholding, but projection of that stale Skill must refuse. (b) Apply the same scoped change through real CLI and TUI/shared application entry; assert identical native generation and hook-tail effects once, with foreign hooks untouched. (c) Resolve the held manifest and ask the shipped operator for its actual members/source editing route: stale two-member instructions must not contradict the three-member manifest. These are independent aspects of one full practice journey.

**Smallest correction.** Separate catalogue/history readability and per-source degradation from strict effect-time source validation, preserving the latter. Move the CLI-only target-hook tail into the existing shared application operation or its established target-effect pipeline, so CLI/TUI invoke one semantic apply. Make outcomes disclose each written, linked, pending, refused or unsupported effect. Reconcile source instructions to the held strap/derived-copy ruling and authoritative manifest; do not make a permanent hardcoded count or another foreign-Skill registry. Complete the existing editor's canonical-source opening/save/propose path instead of letting users edit a projection as source.

**Migration/compatibility/preservation.** Existing generations/trust history and target-owned files remain; a source revision change requires proper current validation/review, not inheritance of old review by name. Retract only integration-owned hooks/copies and retain user modifications with a conflict. Preserve existing native Guardian identity/default selection and review provenance. Do not replace the strap with an unrelated frozen manual. Correct only contradictory operative descriptions under their existing source owner, not the founding canon.

**Receiving experience.** Agents/Context and TUI show purpose, source, scope, revision, why selected/withheld and exact target standing. Offer edit only at a writable native source; otherwise propose a source change. Proposed copy: “Updated at the source and projected for this harness. This session has not yet loaded the new version; continue it using this restart/resume step.” Distinguish a missing Skill from an unavailable capability or denied action. Default onboarding explains existing Guardians separately from Skills.

**Independent proof/recovery.** Uncoached fresh target receives an unfamiliar useful task requiring the revised practice, performs the real operation and returns exact load/source/invocation evidence. Near-miss should not trigger; old source/injected answer/disconnected hook should not pass. Test foreign hook preservation, repeated apply, partial target failure, restart-only target with saved continuation, unsupported edition, source deletion/revocation and owned-only retraction. No automatic model work merely from installing a hook. Real loaded-use proof is NATIVE/PROVIDER; code/fixtures alone cannot close it.

**Dependencies/conflicts/environment.** REPO/CI now. P07 supplies repaired temporal material to hooks; neither need block the other's independent code tests. Coordinate their shared `hook.rs`/effect entry; P06 owns shared `app/mod.rs` and P11 must not edit it concurrently without a claim. P01-U packages current source but does not rewrite it. Actual target edition/load/restart/use in P12, with Mac-specific proof where that is the chosen target. Do not await all supported harnesses before proving a selected actual one, but retain untested editions explicitly.

### P07 — Current native NOW grounding, search and material continuation

**Owner/source.** Central's existing continuous-work owner, AIKit context/session owner, Workcell material owner under O:I #65/#220; [material temporal field][now], [session grounding][grounding], [harness-first §§2–3][harness]. Restores C05 / **CT-001, CT-008**, joins CT-006/WC-006 without redesigning valid native NOW.

**Modules/bases.** K `crates/aikit-adapters/src/central_temporal.rs`, `now_field.rs`, `central_placement.rs`, `crates/aikit-cli/src/hook.rs`, `temporal.rs`, `working_environment_field.rs`, `session_space_working_surface.rs`; C current `ctrl/src/continuous_work/temporal.rs`, source/placement Actions and their actual descriptors; W instance/place observations; O current `receiving/NowRelations.tsx`, kernel material/session reads. P02 owns the root receiving guard/cursors in ContextPlane.

**Positive behavior and exclusions.** Native Central current source and temporal Actions feed orientation for root and child work. NOW search discovers the same permitted source, not a separately maintained copy under the old path. Useful NOW remains available when an optional source is missing, with exact withheld/unavailable/stale standing. Preserve already-implemented root actor/Agency composition and placement. The UI/TUI shows host/root NOW/child NOW/session/place/process/work/result and supported continuation without claiming a tmux pane is NOW or an external process is a fully bound AgentSession.

**First cases.** Run the actual hook event entry against the selected Central binary in a disposable valid World. The obsolete `projectcentral.flow.list/read` calls should fail today. Check root invocation, child invocation, external source edit then next prompt, and an optional source denial while valid NOW remains. Query native NOW through the registered search provider and verify the current `ProjectCentral/agents/now` relation, not a test directory created at the legacy path. Keep root actor composition tests green; this is not its replacement.

**Smallest correction.** Bind the existing hook adapter to the actual current native temporal/source operations and result contracts, with explicit root/child selection and per-source partial results. Do not resurrect retired Flow storage to satisfy the old caller. Replace legacy filesystem-path assumptions in the registered NOW search adapter with native discovery/read or the current declared permitted source contract, retaining source revision and exclusions. Join material/session observations by native refs and generation, using existing structures rather than a central duplicate process database.

**Migration/preservation.** Existing Day/Flow/NOW sources, historical paths and refs remain recoverable; do not copy source backwards or rewrite old events into current time. A legacy reader may retain explicit read-only compatibility only where justified, with known source standing. Source changes do not silently retarget a historical invocation. End Day, close view, detach, interrupt, cancel, stop process and release place stay distinct.

**Receiver/human explanation.** Current Run/Agents/Context and the TUI show which machine, original work and actual actor/session are involved, what was last observed and the next legitimate continuation. Proposed copy: “NOW was refreshed. This optional Flow source could not be read; the rest of your current context remains available.” Root work is a genuine root context. Stale process/provider observations are not a green active badge.

**Proof and environments.** REPO/CI: original hook argv/result, root/child currentness, missing optional source, denied/excluded source, search discoverability and stale response. NATIVE: actual target prompt/compaction/resume boundaries and loaded material; provider replacement and reconnect. MAC+OMARCHY: the same original task address and late Return across two physical horizons and Day rollover, not two guests masquerading as machines. Verify closing a view leaves work intact.

**Dependencies/conflicts.** Basic NOW/action/search correction does **not** require the whole P02 HTML carrier or P09 remote lifecycle repair. Complete Flow/Return context depends on P02's selected contract; complete material navigation depends on P09's truthful address/generation. P06 owns hook installation/effect semantics, P07 owns temporal payload/reading. Publish those shared boundaries before the final live episode. No second NOW authority or new global lifecycle service.

### P08 — Owner-native Actuation observation and correlation, with honest coverage

**Owner/source.** Existing Actuation execution/stream owner and O:I Encounter under #65/#220; [Actuation README][actuation-readme], [harness-first §3][harness], [AI-005][ai-report]. Restores C04. Actuation's complete product report is missing; this packet does not invent its missing findings or assert complete adapter/authority coverage.

**Modules/current refs.** A `crates/actuation-cli/src/dispatch.rs`, current native stream/correlation/read implementations and `crates/actuation-runtime/src/realised.rs`, `stream_observer.rs`; O `desktop/cradle/kernel/src/encounter.rs` (`actuation_correlate`, `load_actuation_streams`, legacy corpus fold) and the actual current Encounter/Run consumer. K's session/body binding participates by its real native refs.

**Positive behavior/forbidden collapse.** Read actual owner activity with the exact AgentSession/request/recipient/model/grant/cancel relations and supported cursor/order semantics. Preserve no-record, ambiguous, denied, partial and uncertain standing. External work has no fabricated pre-adoption history. Native exit, dispatch acceptance, tool permission, execution completion and useful Return remain different. O:I must not interpret a foreign stream directory as the permanent owner contract.

**First discriminator.** Invoke the current O:I correlation path against the actual selected A command surface. Its `actuation correlate - --json` call must fail because A has no such command; a mock that implements it proves nothing about the actual pairing. Then construct real owner stream cases for absent, one exact match, duplicate/ambiguous identity and cursor replay, and make the disconnected consuming route fail the test.

**Smallest correction.** First map the current native owner read/replay/association capability to the consumer's required correlation semantics. Use it where equivalent. Where a necessary association is genuinely absent, minimally complete that existing owner relation, with explicit source identity and supported version, before changing the consumer. Do not simply resurrect a retired CLI spelling because a stale caller asks for it, or add a new cross-product event store. Replace O:I's filesystem fold only once the native read preserves its useful checks and a compatibility/remaining-caller inventory is complete. Treat the exact operation choice as an implementation decision within the existing contract, not a new authored ontology.

**Compatibility/source preservation.** Retain historical stream bytes and refs, partial/torn-tail provenance and explicit limitations; never rewrite missing history into apparently complete events. Version incompatible read changes and pair the consumer. Legacy stores may remain readable through native compatibility where justified, but are not a second active authority. No secret/private raw corpus goes into public evidence.

**Receiver/human explanation.** The current Encounter/Run displays actual body/model and effect standing, current cursor, permission/cancel state and what remains unknown. Proposed copy: “The request was accepted, but completion is not yet observed. Reconnect to the existing execution; do not send it again.” No invented success from a matching label or stale fixture.

**Independent replay/recovery.** Two concurrent differently scoped/modelled sessions; duplicate/out-of-order events; partial stream; deny a tool; cancel before/after a consequential effect; reconnect after lost acknowledgement; actual recipient/model mismatch; stale grant and uncertain completion. Native evidence must relate to the requested episode without training the actor on expected refs. Full supported-edition/model loading and Actuation review coverage remain separately unproven until their real evidence exists.

**Dependencies/conflicts/environment.** Contract mapping and repository correction can start now; the missing Actuation report is not a blocker to fixing this verified seam, but must be consulted when available before claiming all-owner coverage. Owner operation lands before its current-UI consumer. Coordinate O:I kernel enum/type edits with P05/P02; P08 exclusively owns the Encounter correlation path. REPO/CI for contract cases; NATIVE/PROVIDER for actual body/tool/permission/cancellation; final mounted current UI in P12. No full Actuation rewrite authorized.

## 6. Material lifecycle, bounded transport and proportionate performance work

### P09 — Truthful bounded place observation, allocation and teardown

**Owner/source.** Existing Workcell [#72](https://github.com/EpiLogos/Workcell/issues/72) material/CAW line, native place census/Factory interoperability and O:I #220/#65; [Workcell architecture][wc-architecture], [CAW material operations][wc-caw], [place census][wc-census] and report WC-001–006. Restores C11; **WC-001, WC-002, WC-003, WC-004**, with **WC-005 explicitly an administrative-scope/exposure question**, not a proved unauthorised caller.

**Producer/consumer modules/current revisions.** W `crates/workcell-runtime/src/place.rs`, relevant instance/provider adapters and `crates/workcell-cli/tests/places.rs`; F `factory/src/attempt_place.rs`, `attempt_material_admission.rs`, `attempt_material.rs`; K `workcell_instance_intake.rs` and session working-surface consumers. For the separate scoped administrative check: W `crates/workcell-opensandbox/src/reconcile.rs` and its actual caller/CLI route. The F place grant reader uses strict v1/`deny_unknown_fields`; changing producer fields requires paired compatibility, not an assumption that additions are harmless.

**Positive behavior; prohibited regressions.** Failed observation remains unknown; absence is established by a valid observation. Allocation fallback happens only before known effects or after proved reconciliation, not after a possibly successful provider create. Every operation addresses the exact host/provider/socket/session/instance generation it was granted. Proving one process does not by itself authorize closing a whole room containing unrelated work. Every native call has a bounded wait/cancel path that retains uncertain effects and supported recovery. Preserve explicit argv, name validation, generation checks, typed refusals, existing grants and Factory's no-implicit-retry client.

**First discriminators.** Use controlled provider faults: unreadable PID census must not yield stale/absent; a tmux server observation error must not mean gone; Herdr creates a room then returns malformed/failed output, and `auto` must not create a second tmux room; an address for socket A must not operate on the same name on default socket B. A room containing an unrelated pane must survive a close authorized only for the intended act. These tests identify distinct unsafe transitions in one lifecycle.

**Smallest correction.** Preserve typed observation failure through the native provider result instead of `unwrap_or_default`/error-to-empty conversion. Make fallback contingent on effect knowledge, returning a recoverable uncertain allocation when needed. Resolve and honor the full provider address; bind destructive operations to the proven scope/generation. Reuse existing provider/instance identity rather than building a second process registry. Bound provider subprocess lifetime/output with cleanup appropriate to its owned process group; never claim that timing out proves no external effect. Complete the minimum necessary typed grant/read evolution with F/K consumers before activating it.

**Administrative subcase, not blanket redesign.** Trace whether OpenSandbox reconcile's broad administrative semantics are reachable from a narrower tenant/agent authority. Add a refusal/selection boundary where that actual receiving exposure requires it. Preserve legitimate explicitly authorized admin reconciliation. A broad admin implementation alone is not proof of an exploit and is not a reason to pretend all orphan/expiry cleanup is forbidden.

**Migration/compatibility/preservation.** Preserve old grants as historical evidence. Old grant forms lacking necessary endpoint/generation proof must not gain destructive authority by guessed defaults; expose supported re-observation/rebind. Pair strict readers with versioned producer behavior or a contract-defined compatible representation. Do not delete unknown/orphan rooms, reuse an ID for new generation, or destroy retained Return/source/artifacts to clean the test. A code-worktree reference remains different from a disposable sandbox lease.

**Receiving experience.** Native CLI, TUI and current material/Run views show observed/unknown/absent, actual host/provider generation, authority/effect and the next supported inspect/recover/detach/cancel/release act. Proposed copy: “The provider may have created the place, but its response was lost. Inspect/reconcile that place before trying another provider.” Closing the visual pane is not the destructive recovery action.

**Independent proof/recovery.** Reused PID/name/server generation; two sockets; lost create acknowledgement; timeout with descendant pipes held; delayed success after cancellation; stale or revoked lease; unrelated co-resident work; refused admin scope; recover the same actual place without duplicate allocation. Retain source/draft/artifact/Return bytes before and after. Real tmux/Herdr and supported isolation backends are NATIVE/OMARCHY. Linux write confinement is not read/network isolation or Mac enforcement; actual unsupported modes must be disclosed. Two physical host proof belongs to P12.

**Dependencies/write conflicts.** REPO/CI fault cases and paired protocol repair now. P09 owns `place.rs` including its wait bounds; P10 must not independently refactor that file. Its public grant/endpoint agreement is a real prerequisite for F/K destructive-operation consumers and complete remote NOW proof. It is not a prerequisite for unrelated source edits, config plan tests or P02's native document tests. Native provider maintenance/shared registry changes are serialized even where repositories develop concurrently.

### P10 — Bounded owner calls and truthful receiving refresh/terminal lifecycle

**Owner/source.** Existing AIKit application/adapter and O:I kernel/desktop owners under #65/#220/#375; [harness-first cancellation/failure contract][harness], [current UI contract][ui], AI-003, WC-007 and the measured-risk portion of OI-005. Workcell WC-004 follows the same law but its `place.rs` correction remains in P09. Restores C04/C08/C11.

**Modules/revisions.** K `crates/aikit-adapters/src/runner.rs` and the actual affected adapter/owner-call sites; O kernel knowledge/session/Agency clients and the current kernel snapshot/receipt refresh path, `desktop/cradle/src/terminal/TerminalSurface.tsx` paired with `desktop/cradle/src-tauri/src/terminal.rs`. Preserve already-bounded Factory native transports and Workcell external process-group mechanisms rather than replacing them with a new universal runner.

**Complete behavior/exclusions.** Read/action calls have explicit timeout, output and cancellation behavior appropriate to whether they can cause effects. Timeout of an effectful operation is unknown until reconciled, not safe retry. A stale refresh cannot overwrite a later subject or revision. Terminal EOF drains final bytes, checkpoints the transcript and ends polling; view disposal is not an unrequested process kill. Bound responsiveness without dropping true errors or hiding pending/partial standing.

**First cases.** A controlled child blocks forever or exits while a descendant holds stdout: the public caller must return boundedly, retain the right uncertain/no-effect classification and not leak an owned child/process group. Deliver refresh B before delayed A and assert the selected subject remains B. Drive a native EOF-with-final-bytes response, then assert the terminal drains/checkpoints once and schedules no further poll after terminal EOF. Existing code continues to schedule a poll, so this is a real discriminator.

**Correction.** Add explicit per-operation bounds to the existing shared runner/callers, safe bounded draining and owned process cancellation/reaping; avoid a repo-spanning replacement transport where native mechanisms already work. Coalesce/sequence receiving refreshes by actual subject/basis and preserve last useful data with its stale/error state. Stop EOF polling after the producer's actual drained-EOF contract, keeping transcript/open-reopen behavior. An implementation may separate transport and terminal/refresh commits under coordinated file claims; they need not wait on each other.

**Compatibility/preservation.** Preserve public error/result identities or version them explicitly. Never truncate output into false success; disclose limit/refusal. Retain terminal transcript/final checkpoint and current composer/document state across remount. Do not classify all existing older adapters as dead or delete support by default. The actual called OS/platform behavior determines a safe bound, not arbitrary identical values everywhere.

**Receiver/proof.** The current user/agent surface states what is pending, timed out, stale or uncertain and offers the owner-supported resume/reconcile. Proposed copy: “The read timed out; the last observation is still shown with its timestamp.” Tests must distinguish read timeout from uncertain effect and prove a disconnected/reordered consumer cannot appear fresh. Include cancellation while waiting, output limit, child exit, descendant pipe, UI scope switch, EOF with/without bytes, remount and transcript reopen.

**Placement/dependencies.** REPO/CI implementation and controlled processes; actual native process behavior on Linux/macOS, real Tauri terminal use on the chosen Mac candidate. No need to ship all work to the Mac. Coordinate shared K runner changes with P06/P07/P08 call sites and P09's selected semantics; P09 owns its material provider path. Current KernelProvider/source refresh edits must be reconciled with the user's actual UI landing, not applied to an assumed empty donor. This packet is not a measured diagnosis of every observed load delay.

### P11 — Measured cache/read consolidation, optional unless its discriminator proves a correctness defect

**Owner/source.** Existing AIKit knowledge and Factory developmental read owners with current O:I consumers under #220/#375; [Central CT-007][ct-report], [AIKit AI-006][ai-report], [Factory FA-006][fa-report] and OI-005. Restores C12 without turning code health into a replacement architecture. **CT-007 and AI-006 are one knowledge invalidation operation, not two repair tasks.**

**Modules/bases.** K `crates/aikit-cli/src/app/knowledge.rs` (`with_knowledge`) and current Central source/revision reader; F `factory/src/project_development_store.rs` and `attempt_task.rs::coherent_read`; actual O/U receiving refresh consumers after their relevant integration. The selected source shows repeated invalidation/whole-state reads, but final current-UI latency and frequency were not measured by this synthesis.

**Positive behavior/non-regressions.** Reuse unchanged valid data without full unnecessary rebuilds while reflecting actual source/revision/permission/exclusion changes. Preserve Factory coherent-state/convergence safeguards. Keep source truth, cancellation and view state separate. Do not optimize away authorization, silently trust stale owner facts, weaken consistency to reduce counters, or build another authoritative cache/store.

**First discriminator and decision.** Instrument one actual repeated unchanged query/read and count owner reads, index rebuilds, decoded records and elapsed time under a stated dataset. Then change source/standing/grant and ensure immediate correct invalidation. For the UI, overlap refreshes at different subjects/revisions and measure whether stale writes occur. A correctness failure belongs in required correction; a large-file or static-cost observation without user/agent effect remains optional code health, not a release gate.

**Smallest justified change.** Cache against the existing authoritative revision/standing input where trustworthy, with explicit invalidation and bounded retained data. Reuse in-process knowledge/index structures rather than re-ingesting every unchanged read. Consolidate Factory repeated reads only where the same coherent snapshot/basis can safely serve them; keep before/after validation where its guarantee is needed. Avoid broad refactoring of all stores or rewrites justified only by line count.

**Migration/receiver/proof.** Derived caches are rebuildable; authored data, source history and persisted native records are never migrated merely for this optimization. The user sees prompt loading/stale/degraded status without losing the last valid view or document edits. Prove warm unchanged reuse, changed source, exclusions/revocation, missing owner, corrupt cache, interrupted rebuild and bounded growth; compare real read counters and identical output semantics, not subjective claims of speed alone.

**Dependencies/environment.** Optional REPO/CI measurement can start immediately on isolated reads, but common `app/mod.rs` edits serialize behind P06's claim. Measure the final relevant UI/producer integration before prescribing a UI-specific optimization. This is **not a prerequisite** for P01–P09 or source publication. Native representative workload is useful; Mac/human assessment only for actual GUI responsiveness. No invented performance target or unmeasured percentage.

## 7. Final composition is a selected returned episode, not another programme

### P12 — Replay the existing #65 journeys on the actual repaired composed candidate

**Existing owner/source.** [#65][campaign], [#220][native-owner], [harness-first §§5–6][harness], [material NOW][now] and [current UI handoff][ui]. This packet specifies the remaining integration proof already commissioned there. It is not a new scorecard, Wayfinder, acceptance gate or reason to demand human approval of routine native engineering. The real [Mac return][mac] is the starting installed observation, with its explicit skew and deferrals, not a green assertion about a future candidate.

**Affected composition.** The selected successor to O/C/A/K/F/W and the actually published current UI, their installed artifacts/companions/resources, registered paths, resident generations, target-native projections/hooks and later observed loading. QL/M/M′ and protected UI/research work remain held under the original cut boundary. Record exact selected revisions once; only recheck affected modules and proof after a repair changes them.

**Complete positive episode.** A person starts in an existing supported harness or launches through O:I, understands the proposed changes and can retain their work. The native source and selected Skill/Method reach the actual body at the correct scope; requested/resolved/observed model and recipient are known or explicitly unknown. The actor performs useful ordinary work without fabricated Factory ancestry, then deliberately commissions Factory work where appropriate. Native bounded authority/material evidence and independent verification govern it. The result returns by the original context/NOW/source relation, is received/reviewed/included distinctly, and is available after view close/restart/reconnect/Day rollover. The current Desk/Tasks/Run/Agents/Context makes these acts understandable and available, not hidden behind preloaded state.

**First discriminating proof.** On the selected installed candidate, deliberately supply one wrong PATH/companion generation, one unloaded projection, a stale source/plan or a disconnected receiving handler in isolated cases. Each must make the corresponding end-to-end assertion fail rather than pass by receipt, fixture or actor self-report. Restore the actual candidate through the supported native path, then repeat the original uncoached task and negative case with an independent observer.

**Smallest action, not more implementation by default.** Reuse the existing source/native tests and #65 episodes. Install only accepted selected repaired cuts through the corrected managed route; verify exact artifact/activation/resident and target activation separately. Reconcile the current UI's native contracts rather than replacing its shell. When an episode exposes missing code or a stale test, return it to the responsible packet/owner; do not relabel every failure “local proof pending,” send the human between product chats or start a new programme.

**Preservation and compatibility.** Record before/after hashes and identities for protected source, held product entries, user hooks, dirty work, source/history, drafts, artifacts and pending Returns. Deferred restart keeps old work usable and labels the old resident. Scope rollback to what its real native receipts support; installed code rollback is not reversal of irreversible effects. External conversation continuation is explicit successor/reconstitution where genuine in-place adoption is unsupported, never fabricated session continuity.

**Receiving/human judgment.** Explain what became possible, what changed, where the useful result lives, what is still pending and the exact supported next act. Human assessment is required for actual original writing/layout fidelity, subtle selected-context interaction, usable current chat/Desk/TUI, and genuine author/authority choices. It does not replace native verification or introduce another approval gate before independent repository work.

**Independent negative/recovery scope.** Cold bootstrap versus retained external harness; partial owner composition; declined credentials; source/Skill edit→actual activation→later use; real model/recipient/tool denial; concurrent different sessions; permission wait; cancellation race; lost acknowledgement; stale/duplicate/reordered stream; uncertain place creation; selective interrupted update; late Return while app closed; original HTML/media edit/export/reopen; root/child and Day rollover; owned teardown. Prove exact source preservation and no duplicate consequential effect. Use two physical Mac/Omarchy identities for the claim about two physical Workcells; two guests establish only their actual guest topology.

**Dependencies and execution environment.** Native/provider and actual current-UI proofs depend on the repaired contracts they exercise, not on unrelated optional optimization or testing every vendor edition. Install/resident correspondence requires P01-U; reviewed config action P01-C; source/receiving P02; new adoption P03; completed trustworthy Factory Return P04/P05; loaded practice P06/P07; stream/material evidence P08/P09; bounded receiving behavior P10. A case can proceed when its own dependencies are met, using an already-valid World rather than repeating completed adoption. P11 is not a global dependency. Missing Actuation report remains a review-coverage limit and must not be concealed by a successful selected episode.

The already-commissioned three proving assignments can run concurrently in separate disposable runtime material: **harness-first adoption**, **session/routing/Factory use**, and **installation/lifecycle resilience**. Shared physical services, registries, installations, data migration and GUI focus serialize. Native Linux/provider preparation belongs on Omarchy or suitable CI; actual Mac packaging, loaded target, GUI and hardware/human judgments remain on the actual Mac/physical hosts. Keep code feature worktrees bounded and reuse them; runtime isolation is not one worktree per actor.

## 8. Executable parallel order and write-conflict boundaries

### 8.1 Start immediately: no global contract or Mac gate

The following repository work can start together with explicit file claims: **P01-U**, **P01-C**, **P02 native continuation reconciliation**, **P03 adoption**, **P04 verification**, **P05 external-ref/wire/read integration**, **P06 practice delivery**, **P07 temporal adapters**, **P08 current owner correlation mapping**, and **P09 material lifecycle**. P10's isolated EOF/process fault cases can also start. These are existing feature lines; this statement does not commission ten additional local worktrees or ten agents. Reuse current lines, choose actual capacity, and let a feature's agents cooperate in its worktree.

Priority within that concurrency is consequence-driven: preserve active installation and reviewed effects; stop uncertain material duplication/destruction and false evidence admission; restore source-preserving document/receiving and practice/temporal joins; integrate existing native developmental work into the current UI. A missing source/authority proof may block one effect, not all unrelated reads and repairs.

### 8.2 The real contract dependencies

| Contract that must land coherently | Dependent receiving work | What may proceed in parallel |
|---|---|---|
| P01-C exact reviewed-plan carrier through engine/owner | Live Settings/Profile Apply proof | Native owner tests and UI layout/typed refusal presentation. |
| P02 selected Central document/source-basis, optional lineage and receiving cursor semantics | Current root/document receiving and complete original-carrier Return episode | Native source recovery tests, pagination/generation negatives, independent media/editor work. |
| P05 owner-aware external Agent refs and actual build/task read contract | Commission resolution and real full Desk/Tasks consumer | Real serializer regression and current UI construction using honestly labeled contract fixtures. |
| P04 current independent evidence/admission | Completed Factory late/ordinary Return acceptance | Pure Run read/render and Commission selection tests. |
| P06 shared apply effects and P07 current temporal source read | Actual target activation/use with current practice/NOW | Each native correction and controlled entrypoint tests; unrelated source/history use remains available. |
| P08 current native stream/correlation operation | Actual Encounter/Run event meaning and reconnect proof | Consumer state/refusal design and unrelated Factory artifact reads. |
| P09 address/generation/grant compatibility, producer plus strict readers | Material destructive actions and complete remote NOW continuation | Native source/temporal reads and nondestructive provider observation tests. |
| P01-U selected artifact/activation correspondence plus actual published current UI | Installed cross-product and Mac episode | All bounded repository corrections, provider fault preparation and compatible owner CI. |

There is **no new common contract that every packet must await**. Shared contract work is local to the existing owner boundary, and the existing integrator lands producer/consumer changes together where necessary. A fixture is a development aid, never evidence that an unlanded provider works.

### 8.3 Concrete write-conflict map

| Shared surface | Primary claim / coordination rule |
|---|---|
| O `cli/src/update_flow.rs`, activation/managed receipt helpers | P01-U only. No configuration, Skill or UI redesign in that line. |
| O configuration engine/types/live source/kernel binding | P01-C. Reuse native owner transports; common KernelOp registration serializes with the integrator. |
| C continuous-work documents/receiving and related descriptors | P02. Reconcile C161/current C once, preserving later native work. |
| C `projectcentral_ops.rs`/federation/provenance | P03. Coordinate narrow shared source-history/descriptor edits with P02; no whole-feature dependency implied. |
| F attempt runtime/application/verification evidence types | P04. P05 consumes the selected shape; shared Commission/attempt edits get one integrator. |
| F workflow external refs/build/developmental reads; O Factory kernel/native UI join | P05. FB/OB are delta donors, never alternative current-UI authority. |
| K source/trust/shared apply and O held Skill instructions | P06. P07 owns temporal content, not duplicated hook installation. P11 coordinates any shared app-service edits later. |
| K temporal/NOW search adapters and root/child material bindings | P07. P02 owns root receiving UI; P09 supplies material identity/uncertainty contract. |
| A native stream/correlation; O Encounter correlation consumer | P08. No independent foreign-store parser rewrite by P05. |
| W `place.rs`/provider lifecycle and F strict place client | P09. Includes its subprocess bound; P10 does not compete here. |
| K generic bounded runner; O affected kernel reads/refresh and terminal EOF | P10, coordinated with callers and the actual current UI owner. Preserve existing bounded clients. |
| Derived knowledge and whole-state read optimization | P11 only after measurement and active correctness claims; no unclaimed refactor across P06/P05 files. |
| Live managed install, provider registries, personal source migration, desktop focus | P12's selected native integration owner; serialized per actual shared resource, not by repository count. |

A conflict in a shared source file may require serial commits or a small agreed interface patch; it is not justification for one giant rewrite. An independent producer/consumer pair may develop in parallel but must return one tested compatibility pairing.

### 8.4 Optional sequencing that must not become a false dependency

Cache optimization is not a prerequisite for correct admission, update safety or source restoration. The missing Actuation report does not block fixing a verified update ordering bug. Mac installation is not required to correct native Rust/TypeScript contracts in repositories. Original HTML fixture absence affects its exact fidelity test, not all native recovery/media/import work. Already-valid Worlds do not need repeated adoption before document tests. Complete Factory verification is not required merely to display a native build snapshot. Testing every vendor model/harness is not required before proving the explicitly selected supported body; untested editions remain named. Current unpublished UI source is required for the final current-UI verdict, not for independent owner repairs.

The established Day/NOW foundation remains first for **broad live continuation/fan-out**. This does not prohibit bounded native/source/consumer corrections or isolated test preparation while that foundation's actual return is integrated. Preserve completed setup and its evidence rather than repeating it ceremonially.

## 9. Required return from each later implementation

Return through the existing native issue/#65 record: exact changed source and compatibility pairing; first discriminating case before/after; existing positive tests retained; migration/source preservation; actual receiving interaction; independent negative/recovery evidence; and precise unexecuted native/physical/human scope. State whether the result is **source repaired**, **awaiting integration**, **installed but old resident**, **projected but not loaded**, **observed active/use**, **wrong/stale test corrected**, or **unresolved evidence**. No generic health percentage.

One implementation does not get to close a whole architecture line merely because its local test passed. Equally, valid source work does not become worthless because the final physical episode is still pending. Keep the six original reports intact, preserve their counter-evidence and coverage limits, and carry each useful repaired capability into the same current native composition and UI.

## Source links

The exact report blob and full revision ledger is in SYNTHESIS §1. The sources below are existing authority and checked implementation evidence, not new specifications replacing them. Issue/PR bodies were read as available on 19 September 2026; the consequential standings are frozen above.

[harness]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/experience/HARNESS-FIRST-ADOPTION.md
[config]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/cradle/09-CONFIGURATION-PLANE.md
[documents]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/experience/DOCUMENT-OPERATIONS.md
[ui]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/experience/FACTORY-UI-INTEGRATION-HANDOFF.md
[now]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/experience/WORKCELL-NOW-TEMPORAL-FIELD.md
[grounding]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/experience/SESSION-GROUNDING.md
[expression]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/experience/EXPRESSION-FIELD.md
[guardian]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/skills/suite-operator/skillset.toml
[campaign]: https://github.com/EpiLogos/O-I/issues/65
[native-owner]: https://github.com/EpiLogos/O-I/issues/220
[mac]: https://github.com/EpiLogos/O-I/issues/65#issuecomment-5743244181
[c161]: https://github.com/EpiLogos/Central/pull/161
[ct-report]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/reviews/product-fidelity-2026-09-19/02-central.md
[ai-report]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/reviews/product-fidelity-2026-09-19/04-aikit.md
[fa-report]: https://github.com/EpiLogos/O-I/blob/31a392ff3fd9d527fe4a8d9bc406eb836669441f/docs/reviews/product-fidelity-2026-09-19/05-factory.md
[actuation-readme]: https://github.com/EpiLogos/Actuation/blob/47f4fa3c184850e254ef5089696bf5fbbad907ec/README.md
[wc-architecture]: https://github.com/EpiLogos/Workcell/blob/04160f51d37e78d600f32440b4f5275c02b78e9c/docs/ARCHITECTURE.md
[wc-caw]: https://github.com/EpiLogos/Workcell/blob/04160f51d37e78d600f32440b4f5275c02b78e9c/docs/CAW-MATERIAL-OPERATIONS.md
[wc-census]: https://github.com/EpiLogos/Workcell/blob/04160f51d37e78d600f32440b4f5275c02b78e9c/docs/PLACE-CENSUS.md
