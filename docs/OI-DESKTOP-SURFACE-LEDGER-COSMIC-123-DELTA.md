# O:I Desktop Surface Ledger — Cosmic 1/2/3 / Current Situated Matheme delta

**Status:** current Prompt-D branch-state delta; not accepted-main truth  
**Date:** 2026-08-19  
**Whole-application authority:** O:I PR #102 / `docs/OI-EPI-EIGHTFOLD-SURFACE-INTEGRATION.md`; O:I #104 / PR #113 Surface Ledger  
**Native semantic authority:** Epi-Logos-C-Experiments #17 / PR #22  
**Formal/conformance authority:** QL-MEF #69; exact PRE-D Map/provider revision remains producer provenance  
**Corrected Personal parent consumed:** Epi PR #23 + O:I PR #116

## Scope

This document is the Prompt-D delta to `OI-DESKTOP-AGENT-NATIVE-SURFACE-LEDGER.md`. It records only the returned `epi.cosmic.123` / Current Situated Matheme relation and its deep-open descriptors.

It does **not** replace the full #104 ledger, does **not** claim accepted-main implementation, and does **not** claim completion of `epi.deep.m1`, `epi.deep.m2`, or `epi.deep.m3`.

The governing separation remains:

```text
Epi semantic/current event
    !=
SessionSpace / AgentSession / selected-subject workbench context
    !=
active face / tab / split / focused-Surface presentation state
```

O:I hosts and projects the Epi-owned semantic relation. It does not compute QL, astrology, harmonic, inscription, quaternionic, or Current Situated Matheme semantics.

## Native placement

| Native subject / relation | Human Surface disposition | Host function | Semantic owner | Agent-Native relation |
|---|---|---|---|---|
| `epi.cosmic.123` | **CANVAS** parent-world + **INSPECTOR** provenance/readiness | present the current parent-scale Cosmic world over one Epi event | Epi | same canonical refs and Epi Actions used by human and Agent projections |
| Current Situated Matheme / `eventRef` | **INSPECTOR** / Explain / History; selected parent relation may anchor Canvas | retain the opaque current-event reference joining M1/M2/M3 and corrected Personal | Epi | Agent reads the same event relation; O:I does not manufacture a host event |
| M1 parent contribution | Cosmic Canvas compact parent expression; deep opening is alternate Surface | render returned current QL/topological act and exact M1 coordinate/provenance | Epi | native returned M1 ref; no O:I QL calculation |
| M2 parent contribution | Cosmic Canvas live/degraded world-condition expression + Inspector provider standing | render returned world-condition, provider class/revision and degraded-state truth | Epi | native returned M2 ref; fixture/derived state is never relabelled live-now |
| M3 parent contribution | Cosmic Canvas current clock/inscription expression + Inspector provenance | render the same-event deterministic clock/inscription state returned by Epi | Epi | native returned M3 ref; O:I does not infer symbolic state from provider data |
| corrected `epi.personal.450` subject | existing Personal Canvas/Agency/Knowledge composition from PR #116 | co-reference the same D `eventRef` without creating another PersonalEvent | Epi | same protected Personal subject/episode remains addressable under its existing authority |
| `epi.deep.m1` | **ALTERNATE-NATIVE / CANVAS on open-depth** | open descriptor against same `eventRef` + MCoordinateRef | deep Epi M1′ product | descriptor only; `completionClaimed=false` |
| `epi.deep.m2` | **ALTERNATE-NATIVE / CANVAS on open-depth** | open descriptor against same `eventRef` + MCoordinateRef | deep Epi M2′ product | descriptor only; `completionClaimed=false` |
| `epi.deep.m3` | **ALTERNATE-NATIVE / CANVAS on open-depth** | open descriptor against same `eventRef` + MCoordinateRef | deep Epi M3′ product | descriptor only; `completionClaimed=false` |
| provider / Map / source provenance | **INSPECTOR** | explain exact source/provider revisions, observation class, coordinate lineage and readiness | Epi / QL owner refs | same provenance returned to Agent; host may validate shape, not recompute meaning |

The parent Cosmic face, corrected Personal face and later deep Surfaces are therefore presentations of one semantic relation, not separate semantic worlds.

## Canonical refs and Actions

The D host consumes the Epi response as the authority for subject and relation refs. O:I's current adapter recognises these Epi ActionRefs:

```text
epi.action.current-situated.read
epi.action.cosmic.current.read
epi.action.cosmic.open-depth
```

The same returned `eventRef` must co-reference:

```text
Current Situated Matheme
    ↕
epi.cosmic.123
    ↕
M1 parent contribution
    ↕
M2 parent contribution
    ↕
M3 parent contribution
    ↕
corrected epi.personal.450 subject binding
    ↕
epi.deep.m1 / epi.deep.m2 / epi.deep.m3 descriptors
```

O:I rejects host acceptance when those refs drift. Profile equality is not sufficient evidence of identity.

## Personal / Nara identity law

Prompt D consumes the corrected C parent rather than rebuilding Personal 4/5/0.

The O:I host preserves the producer distinction:

```text
corrected Personal subject / episode
    !=
operative Nara identity ref
```

The Personal parent binds to the D `eventRef`; the producer's protected Nara identity remains separately referenced as the identity participating in the operative quaternionic relation. O:I validates returned co-reference and presence only; it does not calculate `q_identity`, `q_transit`, `q_activity`, or `Q_composed`.

## M1 → M2 → M3 current-event law

The host requires one causal/current path rather than three independently current cards:

```text
M1 current QL/topological act
    ↓
M2 world-condition observation
    ↓
M3 clock / inscription state
```

M2 and M3 must carry the same returned world-observation reference, and all three contributions must carry the same `eventRef`. This is a host conformance check over Epi-owned meaning, not an O:I causal computation.

## `.0 / .5` parent boundary expression

Prompt D exposes only truthful parent-scale boundaries:

```text
M1.0 ↔ M1.5
M2.0 ↔ M2.5
M3.0 ↔ M3.5
```

Inner `.1–.4` dynamics may be present in the Epi event where required, but are not automatically desktop chrome. Complete played K²/Ananda depth, dense M2 cymatic/frequency work and complete M3 transcription/clock workbench remain owned by Prompt E / the deep products.

## Provider and degraded-state presentation

The desktop must preserve the producer's observation class verbatim:

```text
live-provider
fixture
derived-only
```

Only a producer result whose provider contract permits `live-provider`, with the required observed state present, may be presented as live-now. Fixture/default/derived-only state must remain visibly degraded and must not be styled or worded as current astronomical observation.

Provider absence is therefore a valid current-state result. It is not permission for the host to substitute a fixture or infer a provider.

## First-D O:I disposition

Historical O:I PR #98 remains unmerged evidence. Its files are classified for the corrected D line as follows:

| Historical file | Disposition | Reason |
|---|---|---|
| `.github/workflows/oi-epi-pratibimba-bridge.yml` | **REFACTOR** | retain real cross-repo acceptance discipline, but target the corrected Current Situated Matheme / same-event contract and current producer ancestry |
| `desktop/core/src/lib.rs` | **KEEP** | retain the generic native-module seam only; no old D semantic ownership is retained by this classification |
| `desktop/core/src/local_epi_cosmic.rs` | **REPLACE** | old profile-derived/coarse Cosmic semantics are replaced by opaque hosting of the Epi-owned one-event response |
| `desktop/core/tests/cosmic_current.rs` | **EVIDENCE** | preserve the historical proof questions, but current assertions are rewritten around event co-reference, provider truth, Map refs, Personal binding and deep descriptors |

The old first-D assumptions that profile equality could stand in for event identity, that derived/default state could imply live-now, or that D completed the deep instruments are **RETIRE**.

## Acceptance boundary for this delta

This branch-level ledger is satisfied only when the current O:I D host demonstrates:

- one Epi-owned `eventRef` across Cosmic M1/M2/M3 and the corrected Personal binding;
- operative Nara identity remains distinct from the protected Personal subject;
- M2 and M3 share the producer's same world-observation ref;
- provider class/revision and degraded-state standing are preserved without host substitution;
- exact Map-derived M-coordinate/provenance refs are retained;
- `#4.4.4.4` / DAY/NOW / quaternionic composition are treated as Epi-owned returned semantics, not recomputed in O:I;
- parent `.0/.5` expressions remain parent-scale;
- `epi.deep.m1`, `epi.deep.m2`, `epi.deep.m3` open against the same event/coordinate refs and do not claim completion;
- human and Agent projections address the same native refs/Actions;
- no React-only or host-local fake depth is introduced.

P7/#111 should fold this delta into the regenerated final whole-desktop parity ledger after accepted ancestry is selected.