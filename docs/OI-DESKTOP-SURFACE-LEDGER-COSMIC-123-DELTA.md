# O:I Surface Ledger Delta — Current Situated Matheme + `epi.cosmic.123`

Status: **ACTIVE Prompt-D reconciliation evidence**  
Date: **2026-08-19**  
Parent ledger: `OI-DESKTOP-AGENT-NATIVE-SURFACE-LEDGER.md`  
Product authority: Epi PR #19 / current successor  
Desktop authority: O:I PR #102 / current successor

This delta supersedes the parent ledger's earlier point-in-time sentence that corrected C had not yet returned. Corrected C returned while D was being implemented and D was then explicitly reconciled onto it. The parent ledger's native-ownership, disclosure and status laws remain unchanged.

## Exact returned cut

| Relation | Exact current head | Standing |
|---|---|---|
| QL-MEF current `main` | `3e9fb929f0c34e8b4474eef59ce512b53082fba1` | current repository head; merged PR #70 adds holographic manifest/validator evidence over the PRE-D floor |
| QL-MEF PRE-D executable whole-Map provider | `d418abfff6f9e001c8c5ff083206329b298eddcf` | direct ancestor of current main; exact `ql-core`/`ql-mef` provider revision pinned by D; the `d418… → 3e9fb…` delta does not change executable QL/Map code |
| Locked Epi Map source | `daa660cbc1b8c5da83828698665a753852cb0287` / dataset tree `cd4f4f77c13f27e2563c5a6753d2f8bf2b605f15` | source identity retained by QL-MEF returned-reality provider |
| corrected C producer | Epi PR #23 `aa8e66e57834e1f13ffb227f597ab085a070bbb5` | active Personal parent owner; current D base + merge parent |
| D producer candidate | Epi PR #22 `310014c5d702568662f92346b706d1e5e87ebdbd` | active Current Situated / Cosmic parent candidate; exact-head CI queued at this ledger cut |
| corrected C host | O:I PR #116 `daa5adc43b780c359f9091011a5593de3273cb27` | active composed Personal host; current D base + merge parent |
| D host candidate | O:I PR #117 `ed0f1ce91e22e7513b7f320bc6b538e988cac8e3` | active opaque Cosmic host candidate; exact-head CI queued at this ledger cut |

`d418…` is therefore the exact executable PRE-D provider revision used by D, not a claim that it remains QL-MEF's branch head. A later green/failure result is an observed result for these exact revisions; it does not change the architectural status by itself. Any head movement requires this table to be refreshed before final acceptance.

## Corrected C → D identity law

Corrected C publishes one body-free `epi.personal-450-application/v1` with:

```text
productId = epi.personal.450
subjectRef = governed episodeRef
eventRef = absent
bindableToEventRef = true
parallelPersonalEventState = false
```

D's Epi producer resolves that actual application from the same protected Nara context/vault before emitting the Current Situated event. D may then add one `eventRef`; it may not manufacture another Personal subject or event state.

The resulting same-event proof is deliberately stronger than profile equality:

```text
corrected-C subjectRef == episodeRef
+ C unbound event socket
+ D eventRef
+ protected naraIdentityRef (kept distinct from subjectRef)
+ #4.4.4.4 / M4-4-4-4'
+ qIdentity
+ qTransit
+ qActivity
+ Qcomposed
+ one M2 worldObservationRef also carried by M3
+ exact M1/M2/M3 MCoordinateRefs
```

O:I validates this relation only. It does not recompute the Personal quaternion, M-state, QL state or world semantics.

## Surface / Action descriptor delta

| ID | Surface/product | Stable identity | Human + Agent address | Completion standing |
|---|---|---|---|---|
| E-D0 | Current Situated Matheme | one Epi `eventRef`; corrected-C subject/episode; separate Nara identity; DAY/NOW; M1/M2/M3; provider/source revisions | `epi.action.current-situated.read` | parent event contract only |
| E-D1 | `epi.cosmic.123` | Epi `surfaceRef` over the same event | `epi.action.cosmic.current.read` | parent-scale Cosmic world; rich native body may remain heterogeneous |
| E-D2 | `epi.deep.m1` descriptor | same eventRef + `ql:m-coordinate:pratibimba:M1` + `.0/.5` refs | `epi.action.cosmic.open-depth` | `completionClaimed=false`; played K²/Ananda depth remains Prompt E |
| E-D3 | `epi.deep.m2` descriptor | same eventRef + `ql:m-coordinate:pratibimba:M2` + `.0/.5` refs | `epi.action.cosmic.open-depth` | `completionClaimed=false`; dense cymatic/frequency laboratory remains Prompt E |
| E-D4 | `epi.deep.m3` descriptor | same eventRef + `ql:m-coordinate:pratibimba:M3` + `.0/.5` refs | `epi.action.cosmic.open-depth` | `completionClaimed=false`; transcription/clock workbench remains Prompt E |

Corrected C separately retains its parent-scale deep-open descriptors for M0/M4/M5. D does not upgrade those into completed deep products merely by binding the shared event.

## Parent `.0/.5` expressions

D retains the source-grounded boundary law rather than exposing every inner `.1-.4` as parent chrome:

- **M1.0** — receive the current QL/harmonic act from the shared Epi kernel/profile; no renderer-local M1 computation.
- **M1.5** — compact topology/oscillation return; full played instrument remains deep.
- **M2.0** — receive 72-fold harmonic/correspondential ground plus explicit world-provider observation; defaults do not become live fact.
- **M2.5** — return the current celestial/correspondential world condition; dense laboratory remains deep.
- **M3.0** — receive the same-event M1/M2 clock/world relation through the current 72→64 codec; no independent M3 event.
- **M3.5** — return deterministic current 360/720 inscription/symbolic address; full transcription/clock workbench remains deep.

## Provider truth / degraded state

Current accepted Epi code contains deterministic harmonic/correspondential projections and historical source material describes a desired real-time ephemeris/service integration, but this reconciliation found no verified accepted live astronomical/transit provider implementation in the current owner path.

D therefore has a provider contract, not a fabricated provider:

```text
live-provider  -> may claim live-now only with provider-observed planetary state
fixture        -> Degraded
derived-only   -> Degraded
```

O:I refuses to upgrade fixture/derived state. The coordinated acceptance specimen intentionally uses a fixture and must remain degraded end-to-end.

## First-D classification

Relative to superseded first-D work:

- **KEEP / EVIDENCE** — shared kernel/profile operators; source/operator provenance; no-local-table discipline; provider non-overclaim.
- **REFACTOR** — coarse M1/M2/M3 workspace handles into exact Map-rooted MCoordinateRefs + deep Surface descriptors.
- **REPLACE** — profile-derived Cosmic semantic event identity; derived-only M2 presented as current sky; profileRef-only Cosmic↔Personal proof.
- **RETIRE** — any implication that D completes six deep instruments/products.

## Prompt-E handoff law

Prompt E may rely on the D floor only when it preserves:

```text
same eventRef
+ exact MCoordinateRef
+ deep Surface ref
+ .0/.5 boundary refs
+ Epi owner/provenance
+ provider truth class
+ corrected-C Personal subject relation
```

It must not reconstruct a new Cosmic event merely to open deep M1/M2/M3, and it must not reinterpret D's parent-scale readiness as deep-product completion.