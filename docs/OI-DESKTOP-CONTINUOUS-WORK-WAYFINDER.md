---
Register: episteme
Standing: design-commitment
---

# O:I Desktop — current Waves 6–8 execution amendment

**Owner direction:** 2026-09-10.  
**Desktop owner:** O:I #190, the existing Cradle programme and its coordinated local thread.  
**Development dependencies:** Factory #195 / Continuous Agent Work (CAW), with native-owner children.  
**Whole-feature proving:** O:I #201 and #202–#205.  
**Entry/dependency lock:** O:I #220.  
**Standing:** approved planning/execution scope, not an implementation or native-acceptance receipt.

This is the current amendment of the existing desktop Wayfinder, not another desktop programme. It refines the remaining Waves 6–8 and the affected parts of Waves 1–5. The owner reports that Wave 5 has been pushed and the current desktop is a strong baseline. Preserve that work and its receipts. Resolve the actual current branch, files, owner bindings and latest `progress.md` locally; do not restart foundation or recreate an old `cradle-p1`/`cradle-rebuild` branch because a historical prompt says so.

For the topics specified here this amendment supersedes the older narrow “Factory and Workcell” Wave 6 gate and the older foundation-first next-execution prompt. Earlier design and acceptance requirements remain in force wherever not explicitly amended. An unproved requirement remains an obligation; an existing receipt is not erased merely because the programme has gained depth.

## 0. Read once, then enter the affected operation

Authoritative inputs:

- [Founding Positions](positions/FOUNDING-POSITIONS.md).
- [Existing Cradle Wayfinder and inherited unit identities](OI-DESKTOP-CRADLE-REBUILD-WAYFINDER.md).
- [Existing implementation programme](../.superpowers/sdd/cradle-rebuild/IMPLEMENTATION-PROGRAMME-2026-09-06.md), [Self/Other field UX](../.superpowers/sdd/cradle-rebuild/SELF-OTHER-FIELD-UX-2026-09-08.md), current design set and native execution ledger.
- [Exact Day and Dialogue/Flow specifications](https://github.com/EpiLogos/Factory/issues/195#issuecomment-5620703383). These govern behaviour across portable HTML and native projections.
- [Continuous Agent Work implementation entry](https://github.com/EpiLogos/Factory/issues/195#issuecomment-5620823870) and [bounded entry/ready gates](https://github.com/EpiLogos/O-I/issues/220#issuecomment-5621164525).
- [Desktop World-participation/placement/draft-return amendment](https://github.com/EpiLogos/O-I/issues/190#issuecomment-5621135989).
- [CAW P01–P24 proof map](https://github.com/EpiLogos/O-I/issues/201#issuecomment-5620840562) and [P25–P28 extension](https://github.com/EpiLogos/O-I/issues/201#issuecomment-5621154293).
- The owner's separately curated 0/1, 4+2 and raw Journal HTML files when supplied. Their final bytes, paths, metadata and exact visual treatment are not supplied by this amendment.

The latest blank-tab determination is explicit: **make the document forms central options in the existing blank tab, opening their real files through the existing source/Surface path.** This is a small entry refinement, not a template marketplace, creation wizard or new document-management subsystem.

Do not substitute issue age or a repository's old implementation language for current native contracts. Do not re-inventory the whole suite on every unit. The CAW operation ledger and relevant source/code/receipt are the bounded starting point.

## 1. Intended experience

> I can open the writing form I need, write freely, deliberately give a selected Agent some scoped work, leave it working, and receive the result back in relation to my original concern without reconstructing a session or surrendering authorship of my Day.

The full route is:

```text
blank tab → open an existing document/form file
    → human writing in Day / Flow / Dialogue / Journal
    → explicitly selected recipient(s), source scope and Send / Commission
    → authorised Agent work in an appropriate Workcell-hosted NOW
    → Direct execution OR Factory developmental execution
    → attributable Activity / result / evidence
    → pending human Return related to its source and target
    → review / inclusion / response, with Recognition distinct
    → continued work / archive / re-entry
```

A person can enter through an existing source, search result, received Return or current Agent as well. The blank tab is a convenient entrance, not a required funnel.

Keep the existing workbench and optional accompanying Agent panel. Shared Field details stay in ordinary canvas panes/full views/popouts, not a replacement right sidebar. New events do not move the layout or replace the open writing. Native components use the current design system and Surface host; the portable files remain faithfully round-trippable sources, not screenshots to imitate.

## 2. Blank-tab document choices — the bounded change

In the existing blank/new-tab presentation, make these initial choices prominent and keyboard-accessible:

| Choice | Initial document form | Opening behaviour |
|---|---|---|
| 0/1 | Dialogue / Flow | Resolve and open the actual supplied file through the existing owner source/open route. Flow and Dialogue are views of the same document entries. |
| 4+2 | The supplied six-position Day form | Open the relevant actual file/current Day through Central's existing source and temporal reading. Do not create another Day because a new tab was opened. |
| Journal | Raw, single-person journal, derived from the Dialogue form | Open the actual supplied single-author file. Do not manufacture an H interlocutor or redesign it into a task form. |

Use the owner's supplied labels where the final files refine display wording. These choices do not constitute new semantic IDs or a new registry. Bind existing file/resource refs using the current source/contribution mechanism. Native source identity remains authoritative.

The files exist independently of this menu. Selecting a choice opens/focuses the correct document through ordinary tab behaviour; it does not copy a template, mint a new canonical document, modify a curated master, invoke an Agent, allocate NOW scratch, or start a model. Explicit native creation and authorised Central Day creation retain their existing separate semantics.

Keep ordinary open/search/recent routes and ordinary file handling available. Blank writing is valid. No forced onboarding or completion checklist is added. Missing or inaccessible target files get a precise unavailable/open-location state rather than a fabricated payload or silent fallback.

The owner is curating the final HTML elsewhere. Record the real supplied payload ref/revision/digest and supported native projection once available. Until then, implement generic entry wiring and other independent work; exact template-fidelity acceptance stays pending. Do not author substitute templates or invent the seventeen field keys.

## 3. Document and human-work semantics

### 3.1 Day: writing and receiving

Preserve the exact CT4b positions and addressable fixtures:

| Position | Office | Required fixtures |
|---|---|---|
| #0 | Ground — Capture | Capture rows: ID, Capture, Type/Category, Context, Time; Quick thoughts; `p0_adjacencies`. |
| #1 | Definition — Tasks | Intentions; Must Do; Should Do; Could Do; Completed. |
| #2 | Operation — Sessions | `Claude Sessions` with Session, Time, Commit, Files Changed; Operations; Manual Activity. Retain the heading; a Session cell can truthfully name another Agent/session. |
| #3 | Pattern — Observations | Patterns Noticed; Recurring Themes; Observations; Connections Made; editable Mermaid source. The example diagram is a placeholder, not evidence. |
| #4 | Context — Structure | Morning/Afternoon/Evening timeline; Files Touched Today; People and Concepts Engaged; Today’s Graph Additions. |
| #5 | Synthesis — Crystallization | Three initial learning slots; Synthesis; Blockers/Open Questions; Tomorrow’s Focus; Today’s Quintessence; Teleological Aim. |

Retain the original seventeen `p0_…` through `p5_…` keys in the embedded `fields` map, plus individually addressable body-only fixtures. Recover literal keys from the actual supplied source. Retain `uuid`, `created`, `name`, `title`, `type: daily`, explicit date and timezone. Empty content is legitimate; placeholders are never recorded activity.

A task checkbox is an explicit human declaration. It neither moves nor duplicates the task into Completed. Agent output does not tick tasks, answer human questions, invent activity or rearrange the sixfold.

Authored fields, pending H Returns and included H contributions remain visually and operationally distinct. Receiving may appear beside/around the note without changing its section structure or field map. Review selects the exact target fixture and basis before native revision-checked inclusion. Inclusion does not certify truth or perform Recognition.

Root and ProjectCentral preserve the same human-source/agent-work ownership relation. Project narrowing filters eligible contributions or opens explicitly scoped source; it does not fork or synchronise editable copies of root prose. The open document, selected Project scope and inspected work/session retain their distinct identities through the existing focus/pin mechanisms.

Central's authorised clock policy may advance the current-day pointer and create the next blank carrier. It must not swap the open editor, close yesterday's writing, move tasks, invoke a model or archive active NOWs. Late Returns retain occurrence and receipt times and original work/source relations.

### 3.2 Flow / Dialogue and Journal

Flow opens an entry as a full writing page; Dialogue reads the same entry set conversationally. Switching preserves draft bytes, source revision, entry/selection IDs, reply anchors, annotations, During/After timing, note replies, media and relevant focus/caret/scroll state. Do not maintain independent editable source copies per view.

Support linked passage replies, whole-entry notes and between-entry writing. Anchors retain original text and local targets; changed targets remain stale and reviewable rather than silently relocating. Meaningful `[[path|display text]]` navigation survives. Preserve prose, emphasis, links, tables, code and supported embedded image/audio/video content.

Journal reuses the supplied single-author form and document primitives. Its detailed data/visual contract comes from the owner-supplied file, not a guessed new schema. A single-person journal is not automatically a two-person conversation or CT4b Day.

The optional reading profile is invoked explicitly. Only then apply its source-defined optional 3–4-sentence carryover where prior material exists and 600–900-word continuous reading without internal headings. The separate `SKILL.md` defines Beats 1–6; do not invent them. Ordinary replies, task results and receiving updates are not required to take that form.

The live AgentSession and the document's Dialogue view are related, not identical stores. A streaming or pending reply is not automatically included source. Native correlations connect message/turn/Return refs to document/entry/fixture refs.

### 3.3 Save, export and host boundary

Native Save exposes target, expected revision, pending write, acknowledgement, unsaved state, conflict, source unavailability and recovery. Workspace/layout autosave does not establish document persistence. Any document autosave must be explicitly disclosed and proved through its native owner path.

`Save HTML copy` remains a separate self-contained export. It does not overwrite the original source. Export/import/reopen preserves embedded editable data, local IDs, media, annotations and a readable no-script snapshot. Standalone Ctrl/Cmd+S remains export as specified; the native host's Save and Save HTML copy actions are separately discoverable and never ambiguously claim the same persistence effect.

Imported document payloads cannot invoke the privileged desktop bridge. Passive open, navigation, selection, mode changes and save never send content to an Agent or authorise operational effects. Trusted explicit Send/Commission/control uses the authorised native operation. Native owner reads/saves can use an explicitly bound storage connection with permission and persistence disclosure; offline exports remain network-free.

Use local-safe rich content, explicit attachments, the 12 MB per-file limit, and sanitisation of imported active HTML/remote media. Preserve the source's local Mermaid code fixture; do not render it through a remote service. No unsupported edit/import is allowed silently to discard source content. Report an exact unsupported operation rather than claim a lossy round trip succeeded.

## 4. The existing Agent panel becomes fully operative

| Plane | Required operation and state |
|---|---|
| Conversation | Actual root/Project Agent or formation selection; direct/group recipients; explicit Send with scoped source preview; attributable replies; pending contribution inclusion; real queue/delivery/error states; human drafts preserved. |
| Activity | Actual messages/turns/tools, waits, permissions, task execution, Returns, compaction, failures and recovery. Factory correlations only where genuinely present; Direct/external work stays truthful. |
| Context | Select whose operative context is inspected; identity/governance/source basis, exclusions, pins, pressure, continuation mode, compaction preview and receipt. Selected text never silently becomes a whole private document. |
| Inspect | Stable document/NOW/Agent/Paśu/Agency/WorldBinding/SessionSpace/session/harness/model/Workcell refs, revisions, provenance and genuinely available native operations. |

Selecting/opening an Agent is not dispatch. Preparing recipients is not permission or invocation. At commit, revalidate the actual recipients, source basis, scope, eligibility and authority. The payload must match the preview. An unsaved selection needs an explicitly supported attributable draft/snapshot basis; never silently substitute different saved bytes or save/send more than the person selected.

Agent-origin messaging uses the native send/contribute operation, not F's draft buffer. Preserve per-recipient outcomes and unavailable participants' permitted history. A native producer identity accompanies H attribution; a declared H label in imported HTML is not authentication. `H` as a template author label is not the `H` human-acceptance evidence grade.

Agent visibility follows caller-visible World participation, not mandatory profile files. Consume AIKit #274's ordinary producer/index/resolver seam. A valid profile-less participant can be addressed without Create Profile; optional Central adoption enriches the same identity. Declared participation does not itself prove live actuation. Withheld/unrelated subjects remain excluded; withdrawal removes false current presence while retaining permitted historical attribution. Do not create a desktop participant classification or adoption engine.

## 5. Continuous work, Factory depth and material truth

Appropriate task work resolves/allocates a valid NOW through Central before scratch writing; AIKit provisions and applies actual per-harness controls; Workcell hosts the material environment and any required hard boundary. NOW source/lifecycle remains Central-owned. Opening a writing file does not allocate an agent clearing.

Expose useful native operations: inspect clearing, see actual writable bounds and enforcement, recover from a refused placement through an allowed destination/retry, inspect material health and continue permitted work. `declared policy`, `effective policy`, `configured guard`, `loaded guard` and `observed enforcement` are different facts. Do not display a hard-protection badge because a hook file exists. If required protection is unsupported, disclose the actual unmet condition or eligible alternate placement; do not silently downgrade it.

Valid external/native Work entries do not become debris because they lack ProjectCentral. Create/adopt/repair must preserve original files, dirty trees and active bindings. Actual personal migration and policy adoption remain separately authorised native work.

Direct work creates no Factory ancestry. Factory work uses current owner-published Commission/Journey/Run/workflow-unit operations and readings, including Semantic, Live, Trajectory and Cognitive when available. Reuse the source-faithful Factory contribution, not a replacement run dashboard.

Selecting a Journey/Run/unit opens the matching Agency and source/Flow/Day context through stable correlations and existing Surface navigation. Preserve ordinary pane/full-view/popout behaviour without inventing a Run store or automatically hijacking the open editor.

Trajectory presentation provides chronological rows, input/model/tool bands, turn grouping, search, expandable detail and unobtrusive native duration/call/token/cache observations. The referenced DeepSeek screenshot is a visual-quality reference only, and must be supplied/located before claiming a visual match. Virtualise long timelines while retaining stable event/anchor navigation; preserve cursor/scroll position under incoming events and make follow-live explicit.

Planned/commissioned is not running; a completion report is not verification; cancellation requested is not actual stop or quiescence; unknown telemetry is not zero. Do not derive exact durations, throughput, latency or cache rates without sufficient owner observations and an explicit derivation basis. Sensitive trace detail follows source permission. T artifacts are explicit plans/findings/coordination, not reconstructed hidden model reasoning.

Closing a panel does not terminate gateway/NOW/Factory work. Reopen discloses actual resume, rehydration or fresh fork. A supported Workcell move preserves semantic refs; an unsupported continuity guarantee appears as a real break. Another authorised harness/CLI/TUI can read the Day receiving ledger while the desktop is closed. Delivery does not prove human awareness.

## 6. Human Return and safe inclusion

A compact Return answers: what happened, what changed, what remains open, what action is needed. It links source/result and optional deeper trajectory. Choose a target fixture, inspect basis, include/reject through native revision-checked operations or leave it pending.

Preserve separate transitions: receive, display, acknowledge/read where actually recorded, include, respond, adopt source, merge a code change, complete a personal task, verify a result, recognise developmental meaning. Do not collapse them into one green completion state.

Native operations enforce the portable rules:

- Day `append-to-field` names exact fixture, attribution, rich content, optional source refs and observation/interpretation/proposal standing; inclusion places a distinguishable H contribution beside the fixture, never replaces F's field.
- Dialogue `append-entry` and `replace-own` target exact document/revision. Replacement is only for an untouched contribution by its actual eligible producer. If F has edited it, return a new contribution instead.
- Stale revision, wrong document/fixture, F impersonation and replacement of F material are refused. Replay/late arrival follows native identity and deduplication, not desktop text comparison.

Recovered README drafts are a concrete receiving specimen: compact pending result plus Open draft / Inspect diff / native review operations. Do not paste an entire generated draft into the Day or treat inbox placement as authorship, merge or Recognition.

Day closure, NOW quiescence, compaction, archive and deletion stay separate. Archive operations account for active bodies, queues, pending permissions, uncertain effects, unreceived Returns and unresolved obligations. Preserve refs, source revisions, cursors, privacy and required evidence through archive/re-entry. Do not mark unfinished work complete to make cleanup possible.

## 7. Amended desktop execution map

These are sub-gates within the existing Wave 6, not new phase numbers or new CAW work packages. Preserve inherited U/FND/GW/W1.x IDs and link affected obligations to these cuts. Keep implementation, merge, integration and observation separately recorded.

| Cut | Human result and implementation | Dependencies / proof |
|---|---|---|
| 6A — Open and write | Central blank-tab choices; native Day/Flow/Dialogue/Journal; exact fixtures, anchors and view parity; native Save/conflict/recovery; independent portable export. | Existing source/Surface opening plus Central #150/#151 where needed. Final supplied payloads gate exact fidelity, not independent host work. P02–P04 and relevant P21. |
| 6B — Select and send | Root/Project participant and formation selection, including eligible profile-less actors; recipients and exact source preview; explicit Direct Send or Factory Commission; queue/delivery/errors. | AIKit #274/#275, Central source/placement contracts, Actuation accepted authority seam. P07–P10, P21, P25. |
| 6C — Inspect and continue | Four actual panel planes, permissions, scoped Context, compaction/continuation, NOW relations and truthful alternate-Surface continuity. | AIKit/Actuation/Gateway public readings; relevant recurrence/hosting where consumed. P09–P12, P16, P21/P22. |
| 6D — Develop materially | Source-faithful Factory depth, current Journey/Run/unit operations, Workcell placement/resources/lifecycle, lower terminal/material surfaces, enforcement and recovery. | Factory #221/#222 and existing public projection; Central #153; AIKit #277; Workcell #72; accepted Actuation migration seam. P13–P15, P17/P18, P23, P26/P28. |
| 6E — Receive and include | Durable pending Return, fixture targeting, basis/diff inspection, safe include/reject/replace-own, separate Recognition and stranded-draft Return. | Central #152 plus actual AIKit/Actuation/Factory producers. P04, P16, P21/P22, P27. |
| 6F — Leave and re-enter | Headless continuity, second harness/placement, restart, Day boundary, archival and recovery of pending obligations; native UI regression walk. | Accepted owner operations for each required case, exact installed candidate. P05/P06, P11, P14–P19, P21–P28 as applicable. |

**First complete vertical:** as soon as the required seams are usable, open a real document → select a passage → send to one actual Agent → obtain an attributable reply → receive and review/include one contribution. This joins 6A/6B/6E early; do not defer all integrated use until every convenience or provider is finished.

Then extend that same operation through group/private/shared exchange, two harnesses, Factory coordination, concurrent edits, permission waits, compaction, placement refusal, headless Return and temporal recovery. A truthful unavailable state enables independent development; it does not satisfy the unavailable feature's closure obligation.

### Wave 7 — Shared Field and Explore

Retain publication/audience, transport/provider loss, admission/quarantine, contribution history, hosted-provider and source-Return obligations. Reuse the same document, participant, NOW and Return refs from Wave 6. Local navigation is not publication, and local material, selected projection, admitted contribution and accepted source revision remain distinct.

The basic private/group exchange required by CAW belongs in Wave 6's working route; it does not wait for all Explore UI. Wave 7 proves the complete two-participant sharing/admission/withdrawal and source-return lifecycle using the same native boundaries. Shared details inhabit the ordinary canvas, not a second right-panel/chat architecture.

### Wave 8 — Whole-product and QL-integrated Wiki acceptance

Retain the original full-capability dispositions, rich material/format handling, accessibility, visual fidelity, bounded resource/lifecycle and FND-06/GW-06 acceptance. Do not drop prior whole-product obligations because this amendment concentrates on writing/work.

Add a proper Wiki/intelligence integration gate after the accepted native composition is available: actual document/fixture/source → NOW/work → Agency/session → Factory subject where present → returned artifact/evidence → human contribution and source basis, navigable forward and backward through current AIKit/QL owner operations. A working search box or pre-seeded fake graph does not close this route. Changed source and unavailable/private relations must remain truthful.

Consume accepted QL-MEF #123 and its current implementation successors rather than restarting reconciliation or freezing an earlier partial C′ contract. The whole is QL Kernel, faithfully projected as Syntax; Root (psychoids + coordinate-family heads); Harmonics (Context Frames active here and propagating); Geometry; Meta Epistemic Framework; Techne. QL and AIKit own formal/intelligence semantics; the desktop consumes them. This amendment does not design another grammar or a local shape algebra.

Prove ordinary no-QL correctness independently and record the required QL-enabled integration separately. Do not claim the latter complete from the former. Deeper integration can be developed alongside later Wave 6/7 work once its owner contract is accepted; its final native/human proof belongs before whole-product closure.

## 8. Native-owner handoffs and dependency law

| Owner / existing track | Desktop handoff, not a new desktop-owned implementation |
|---|---|
| Central #150 + #153 → #151 → #152 | Human Day/source, NOW lifecycle/placement policy, exact portable document fixtures and native operations, receiving/inclusion, reversible migration and archive/re-entry. Resolve literal paths/IDs from current owner code. |
| AIKit #274 → #275 → #276; #277 after policy publication | Caller-visible World participants and effective composition; selected-context messaging/dispatch; session/continuation/recurrence; actual per-harness enforcement. Preserve current praxis semantics rather than reviving retired Method stores. |
| Actuation #58 and its existing serial migration queue | Agent/Agency/WorldBinding actuality, authority, Activity/Stream, attribution and Return; public compatibility across implementation-language changes. No competing Actuation rewrite from desktop. |
| Factory #195, #221 → #222 and existing #201 self-hosting | Real developmental attempt/coordinator/public projection/verification/Return/Recognition; use native read/Action contracts, not consumer-invented enums. |
| Workcell #72 | Durable hosting, material bounds and evidence, second supported placement, recovery and authorised local installation/migration proving. |
| O:I #220, #201/#202–#205, #190 | Bounded operation lock, proving ownership and desktop composition/presentation. Suite/install/update remains O:I-owned. |
| QL-MEF #123 and accepted successors; AIKit intelligence owners | Canonical QL/Vāk/C′ and native Wiki/intelligence consumption; optional generic operation and explicit deeper integration proof. |

Use current successors if these tracks have advanced. A ticket being closed or a type existing is not proof that its consumer operation is installed and working. For each required UX transition, the shared ledger records:

```text
existing desktop unit + UX/capability refs where assigned
source specification / exact payload basis
native subject and public read/operation
contract revision + source revision + authority preconditions
expected transition + actual owner readback
consumer adapter/component + exact mutable file owner
fixture/test and CAW proof IDs
implementation / merge / integrated / observed standing
next executable join or exact dependency
```

Do not invent literal CLI commands, fixture keys or new refs to fill the ledger. Use the current public operation as returned by its owner. All Agent/headless routes use the `oi` doorway, including native product dispatch; desktop adapters preserve that owner route rather than selecting a private executable.

Three joins have one integration owner each: document↔message/source basis; selected Agency↔authorised execution; Return↔revision-checked inclusion/Recognition. Contributors may prepare disjoint owner work but do not invent both sides independently.

## 9. Parallel work and safe resumption

The existing coordinated desktop thread resumes implementation from its actual latest state. This documentation update does not launch another implementation session, restart providers or mutate the user's machine.

After the narrow affected-operation/source lock, parallel work is useful on:

| Lane | Boundary |
|---|---|
| Document/blank-tab UI | One UI lead owns shared writing/panel geometry and interaction. Bind supplied files; implement native projection fidelity. |
| Native owner consumers | Bounded owner/relation files against accepted public contracts. Product teams keep their in-flight hardening ownership. |
| Trajectory/material presentation | Stable published inputs, disjoint presentation modules by agreement with the UI lead. No private Factory/Workcell model. |
| Verification | Independent read-only review and disjoint tests for actual operations, failure pressure, accessibility, resource use and portable documents. |
| Wiki/intelligence consumer integration | Begins against accepted QL/AIKit contract, with one shared bridge owner; joins the final integrated gate. |

One mutable shared subject has one writer unless the native owner proves concurrency. One Central integrator, one AIKit integrator and one Factory integrator coordinate their current lanes; Actuation stays serial. Workers do not create/delete/retarget phase branches or touch the occupied native candidate. Use existing authorised isolated execution worlds; retire only work whose unique source/evidence is preserved.

Shared bridge/schema joins, native candidate builds, executable binding, service replacement, installed walks, evidence promotion and phase landing are serialized. No rebuild into an app under inspection. One matrix steward reconciles generated capability/account projections from accepted merges; parallel workers return impact refs and evidence instead of conflicting matrix rewrites. Merge advances implementation standing, not UX/H acceptance.

Repository work can implement source, adapters, controlled filesystem fixtures and executable D/C/browser tests. Local harnesses prove actual installed source/binary/credentials/provider/material conditions; a local fixture does not become M merely because it ran locally. Human EX supplies the experiential assessment. No private Day/Control contents are uploaded as public fixtures.

## 10. Candidate, proof and closure

Before a native walk, use the current O:I install/update/source-modality system to bind one coherent candidate. Record the actual `oi` and every delegated owner executable's path/revision/digest, relevant contract versions, source basis and explicit overrides. Test the exact dispatch route used by the desktop, not a fresher binary found elsewhere on PATH. Do not hand-edit historical pins or replace active binaries under the walk; a newer owner revision starts a new candidate.

CAW P01–P28 keep their existing meanings and owner allocation. This document's 6A–6F are implementation cuts, not replacement proof IDs. The blank-tab and Journal additions extend P03/P21 and their linked desktop unit checks; exact specimen-specific cases remain pending until the supplied files arrive. Keep P02–P04, P21–P24 and the P25–P28 deltas explicit in the desktop return; no duplicate whole-feature certificate is introduced.

Every control must complete:

```text
public owner operation
→ actual transition
→ authoritative owner readback
→ panel/kernel integration proof
→ required installed interaction evidence
```

The installed whole-path specimen includes root selection without a fake Project; profile-less eligible participation; real second harness; private/shared exchange; exact scoped dispatch; source edit during response; permission wait; compaction; genuine Factory and unrelated Direct/external work; invalid placement refusal and native retry; desktop closure; restart; received Return; stale inclusion refusal; Day rollover; second supported placement; archive/re-entry. Preserve the actual same subjects and pending obligations throughout. Missing mandatory providers/materiality remain named open gates rather than simulated passes.

Test keyboard/focus, selected-text boundaries, narrow panel/full-page/Dialogue modes, long documents, stable timeline anchors, incoming events without scroll theft, unsaved recovery, import/export/reopen and private-source boundaries. Human assessment addresses reading/writing quality and Return clarity, not merely visual similarity.

A fresh non-implementer verifies the full public route and consequential failure/recovery. A deliberately removed required producer/dispatch/enforcement/receiving connection must cause the appropriate regression to fail. Independent P24 plus the required human evidence closes the whole case, not a rendered placeholder, Commission receipt, screenshot or green component suite alone.

The execution ledger records requirement/UX/capability refs, exact native/code/test/candidate basis, actual command/UI path, initial revisions/cursors, expected effect, observed readback, evidence grade, negative case, independent reviewer and outstanding limits. Preserve D/C/P/M/H, and distinguish the template H author label from human H-grade evidence.

## 11. Immediate execution brief

Continue O:I #190 from the actual local Wave 5 return and coordinated Wave 6 branch. Read this amendment, current `progress.md`, the affected design/source specifications and O:I #220's current owner-operation ledger. Preserve the existing desktop and other active sessions.

Begin with the bounded 6A entry/fidelity work: blank-tab 0/1, 4+2 and Journal choices open actual source files through existing native routes. The owner supplies the final HTML; do not invent its payloads. In parallel, join ready native participant/dispatch/receiving and Factory/material contributions on disjoint files. Reach one real selected-passage → Agent → pending Return → reviewed inclusion route early, then progress through 6A–6F and the amended Waves 7–8.

Return exact changed files, owner operations/readbacks, executed tests and candidate receipts, the next native walk and remaining source/provider/human gates. Update the existing ledger and issues. Do not restart foundation, create another shell, bulk-migrate unrelated documents or claim complete acceptance from code alone.
