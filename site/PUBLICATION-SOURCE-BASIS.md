# Selected source and publication boundary — 2026-09-19

Continuation of #65 / #220 / PR #400. This supplements, and does not overwrite,
the historical site or native-owner receipts. No C0–C5, DAY/NOW or native matrix
standing is promoted by a website build.

## Current source basis

The selected essay §5 is the section-room at
`EpiLogos/Antykathera-Essay-Work@13aeac61c4f4f73f1b9eb0ee65dc7ab015ee9024`,
`submission-package/essay/section-rooms/06-objective-internality/ROOM.md`, blob
`1bdea0c253184fa67150d83d3741599a91d9a279`. It was read at that immutable commit.
The current master manuscript is an index; an older complete manuscript snapshot
must not silently become the current edition. The room's six offices are meaningful
continuity, living articulation, potency, transformation, situated existence and
Transcendent Relation. Public editorial wording now uses those distinctions and
keeps the constructed six-product paradigm within the wider Objective Internality.

This is source grounding for the bounded wording amendment, not a claim to publish
the current essay or its internal source links. The sole public editorial source
remains `site/content/public-site.md`. Its native site-edition builder records the
actual source hash and build commit, so the generated reading identifies its own
edition rather than borrowing the essay's or corpus producer's revision.

The native instruction basis is O:I `0b3583be80868bcc3edfb4a449ae17010f0bbd74`,
`docs/INSTALL.md` and `docs/INSTALL-UPDATE-FLOW.md`. Installation entry points stay
prominent. The public account distinguishes released artifacts, current source,
prerequisites, check versus apply, and operating/recovery instructions. No claim
that a registry package, machine receipt or full human experience is verified is
inferred from a command example or successful build.

## Deployment preparation is not deployment

`prepare-public-assets.mjs` stages only the declared generated edition files,
public-only native Explore seed and approved editorial media into `.public-edition`.
Vite's public config serves that same staged surface for development and build.
The repository's `field-proof-demo.json`, `self-other-demo.json`, raw owner inputs
and unrelated stale Library files are not copied. Test fixtures are retained only
in verification artifacts, never in the Pages payload.

The existing production workflow still verifies the full live Explore client and
its existing native contracts. It then rebuilds the separately admitted public
reader before the Pages upload, and runs both existing browser suites and the new
native receiving-route test. The live client and its service variables do not
become the public reader's runtime. The public `explore.html?ref=…` compatibility
address opens the same native-subject Library; live subscription remains a separate
application/hosting responsibility, not a hidden behaviour of reading an edition.

Only a successful main-branch Pages deployment establishes deployed standing.
PR preview artifacts, controlled browser tests and local preview addresses do not.
Track 3 still supplies the actual eligible corpus subjects, membership, complete
reading bodies, matching native Expression/Projection revisions, approved assets
and browser-safe depth. Configuring no producer outputs leaves an explicitly empty
native corpus Library alongside the separately labelled site-authored accounts.
The receiver never converts the E0 sandbox `oi.journey` return into an invented
native public catalogue. Exact deployment and producer standing belong in the
current PR acceptance receipt, with test fixtures clearly distinguished.
