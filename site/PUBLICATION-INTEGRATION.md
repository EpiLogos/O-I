# Public Library receiving integration — 2026-09-20

Owner: O:I #65 / #220, W8 coordination #375; receiving PR #417 on
`site/publication-receiver-20260920`. This updates the existing site Wayfinder,
not the programme or native acceptance gates. The 19 September receipt remains
in #400 at `48518f98088c9c5564dcc761a4f75136fd74ff63`.

## Exact continuation and ownership

The verified `site/redesign-v2` successor #400 was merged as
`45159b76a1677d847aef3aa6fc1788b7017f688b`. This receiver starts at published
application cut `a222c8bdf46a7b2146084770b704dfbe83ce5027`, not an assumed older
main. The newer local integration receipt requested on #65/#220 was absent from
the retrieved comments. This published cut is not proof about unseen local or
installed work. Approved hero/editorial/media and native instrument imports remain.

Single-writer scope is recorded on #375 comments 5747125177 and 5747261283:
site receiver, browser host, site-only tests and a new readonly CI workflow. No
CradleFrame, native engine, corpus producer, identity/catalogue, native install or
shared campaign writer is replaced. Local integration owns merge, adoption and
installation. No merge, force push, deployment or personal-machine operation was
performed by this lane. C0–C5, native matrices and DAY/NOW gates are not promoted.

## Native receiving contract

The common Library receives existing native subjects before the separately labelled
site-authored accounts. `createExploreSurfaceModel` owns identity, discovery and
relations; native `wiki.contains` or Wiki-reading binding refs own membership.
There is no second Wiki, graph, scene store or title-inferred catalogue.

The path is site → Library/collection → native subject reading → available native
Expression → permitted graph/source depth → same subject and paragraph. Source
Projection, Expression, Expression Projection and Scene revisions remain distinct.
URLs retain collection/query/result page/card, exact native refs, paragraph and
within-paragraph offset. Direct `library.html?ref=…` and `explore.html?ref=…`,
refresh and back/forward use the same receiver without storage, daemon or Agent.
A missing exact revision is unavailable, never silently replaced with latest.

Discovery covers the full admitted native set instead of stopping at 1,000;
rendering uses bounded 24-result pages. The accepted NativeStage/ProductionAdapter
remains the real renderer. It stays resident but suspended across reading depths.
The site host measures positive dimensions before fitting/drawing, and never
shrinks a hidden retained field's GPU buffers to one pixel. The existing adapter's
ten-formation limit is disclosed as a receiving limit, not the native system's
ultimate capacity. No larger native journey is truncated or converted into a demo.
Unknown renderer bodies, unpublished Technè instruments and undisclosed relations
remain explicitly unavailable; declared visual proximity is not a semantic edge.

## Public admission precedes every output

Accept only deliberately selected `oi.world-publication/v1`,
`oi.expression-publication/v1` or `oi.explore-browser-seed/v1` producer output,
outside `public/`. The receiver does not crawl raw Central readings or acquire
publication authority from file placement.

Resolve all field, membership and withdrawal restrictions before emitting any
Projection, search row, preview, HTML or download. Contradictory membership is
refused; withdrawal wins regardless of input order. A denied member embedded in
an outward Projection requires native reselection. Private fields cannot hide a
search row while leaking its public-labelled Projection. Encoded protected refs,
local/private locators, sensitive outward keys and credential-bearing transports
are refused. Listing text comes from the admitted Projection, not arbitrary rows.
Omission reports, raw readings, Actions, private Control/Nara/dialogue and internal
machine evidence are not copied. These are defence-in-depth checks, not automatic
certification of arbitrary prose: native selection/public permission remain required.

`build-publications.mjs` invalidates generated deploy staging before admission;
failed selection cannot leave a previous `dist` deployable. Realpath checks prevent
raw inputs or symlink aliases inside the copyable public directory. Existing raw
public seed stays empty. `prepare-public-assets.mjs` copies declared admitted files,
not all `public/data`. Fixtures exist only in tests/interception, never `dist`.

Outputs retain native Explore, Projection and `oi.world-edition-manifest/v1`
contracts. Hashed directories are transport addresses, not semantic identities.
Browser admission checks the complete manifest pair against source/ref/revision,
presentation, world and publication basis. Mixed deployment files fail rather than
offering another edition's reading or download. A digest verifies bytes, not consent.
This receiving build carries an edition, not an archive or live subscription.

## Founding, wording and install basis

Grounding remains `docs/positions/FOUNDING-POSITIONS.md`,
`docs/CAPABILITY-MATRIX.md`, `docs/experience/EXPRESSION-WORLD-UX.md` and the existing
expression/Technè Wayfinders. Recovered native capability is not inferred absent
from a missing desktop join; source/controlled/integrated/installed/observed
standing stays separate.

Selected current essay §5 is `Antykathera-Essay-Work` at
`13aeac61c4f4f73f1b9eb0ee65dc7ab015ee9024`,
`submission-package/essay/section-rooms/06-objective-internality/ROOM.md`, blob
`1bdea0c253184fa67150d83d3741599a91d9a279` (re-read 20 September). The current master
is an index, not the old full prose snapshot. Six offices: meaningful continuity,
living articulation, potency, transformation, situated existence and Transcendent
Relation. This is a constructed paradigm within wider Objective Internality, not
proof of subjectivity or a compulsory definition of every World. Public editorial
source remains `site/content/public-site.md`; no older essay basis is relabelled.

Setup links retain inspected accepted instructions at O:I
`0b3583be80868bcc3edfb4a449ae17010f0bbd74`: `docs/INSTALL.md` and
`docs/INSTALL-UPDATE-FLOW.md`. Historical prebuilt releases are not current source
installation; intended npm registry commands are not verified publications.
`oi update --check` is distinguished from explicit apply/rollback. Lane D's newer
adoption work is not called accepted or installed merely because it is committed.

The public capability account derives from actual received text/reference metadata,
Expressions, available relations and edition basis. It states JavaScript/WebGL
conditions, exact unavailability and onward work without issue-number prose or
percentage scores. Public reading needs no checkout, daemon, paid model key or
Agent session. Rendering is not verification of a person's provider, microphone,
audio route or native Mac application. Complete corpus publication AND usable
self-inhabiting installation remain required near-term outcomes.

## Recovered H/W7 delivery and exact remaining join

The actual `desktop/cradle/expressions-app/collections/return-of-zero/PUBLICATION.json`
and `docs/experience/RETURN-OF-ZERO-PUBLICATION.md` already deliver a real
`oi.collection-publication/v1` preparation: nine members (sovereign essay plus eight
rooms), `oi.legacy-collections/v1` / `oi.collection-provenance/v1` identities,
source `dbf3b17`, nine cover hashes and accepted `oi.journey v1` imports. These are
not fabricated demos. Earlier sandbox-census wording must not erase that delivery.

The missing join is audience-bound public subject/WorldPresentation Projections,
complete reading bodies and matched browser-native Expression output. Reception
of the real collection envelope was attempted and refused; filenames were not
turned into invented World/Wiki refs and Scene summaries were not called the whole
essay. Native Share → preview/omissions → Projection → publish owns this transition.
H's announced `fix/collection-source-identity-20260920` repairs native selection;
consume its exact export when published, and explicitly reconcile `dbf3b17` with
the current section-room basis. Do not overwrite its owned files.

The inspected production build has zero admitted native corpus publications.
The separate site-authored accounts do not establish complete essay/product/Bimba
coverage. Empty preview success is useful receiving evidence, not corpus acceptance.

## Executable local and host acceptance

On an already selected source revision, without touching the installed suite:

```sh
cd site
node --test *.test.mjs src/explore/relation-layout.test.mjs tests/publication-*.test.mjs ../shared-field/*.test.mjs
npm run build:public
node verify-public-edition.mjs --directory dist
```

The last command deliberately fails an empty corpus. `--allow-empty` means labelled
preview only. The verifier checks every manifest, HTML digest, Projection download,
embedded Projection and resolvable subject. Selected producer inputs additionally
compare the entire native set, not counts. After H's reviewed native output files
are supplied outside `public/`, set `OI_LIBRARY_PUBLICATIONS` to their actual
JSON-array paths (not examples or test fixtures), then:

```sh
npm run build:public
node verify-public-edition.mjs --directory dist --expected-inputs "$OI_LIBRARY_PUBLICATIONS" --out evidence/receiver/selected-edition.json
npm run preview -- --host 127.0.0.1 --port 4173
# In another terminal, with native refs from that same publication:
python tests/publication-local-acceptance.py --url http://127.0.0.1:4173 --ref "$ESSAY_REF" --ref "$PRODUCT_REF" --ref "$BIMBA_REF"
```

The real-host walk never intercepts data. It records OS/architecture, browser,
exact routes, reading blocks and renderer standing; it exercises depth/return,
keyboard, refresh/history, reduced motion, narrow layout and no capture request.
Use `--require-expression` where every selected subject declares one. Headed Mac
browser acceptance uses `--browser webkit --headed`. It is not an installed native
Mac/model/microphone test; native owners retain those explicit local checks.
Never publish their private receipts. These checks are not visitor prerequisites.

Readonly `.github/workflows/site-publication-receiver.yml` checks the exact PR head,
355 Node tests, production build and four browser suites, retaining evidence apart
from deployable bytes. Controlled fixtures are only test inputs. Canvas ink and an
in-bounds native selection target are required, not just `data-rendered`. Host
lifecycle tests execute the real scheduler/camera methods with controlled timing.

Preserved originals: twenty first admission/discovery regressions failed on the
old receiver; three of four field-lifecycle tests failed on the old host. Uploaded
`80a9571` failed TypeScript on a missing JSX delimiter in run 35486051649; `986dddb`
repaired it and passed 351 then-present Node tests and four browser suites in
35486243476. Visual review still exposed the unmeasured-field defect, so those
older DOM checks are NOT reused as pixel proof. The successor adds four lifecycle
checks and canvas-pixel assertions. Current exact head/run/results live on #417.
Local managed Chromium rejected localhost with `net::ERR_BLOCKED_BY_ADMINISTRATOR`;
that was preserved, not bypassed or labelled passed. CI supplies browser evidence.

## Operator-only deployment

Existing `site.yml` deploys only a successful accepted-main push through GitHub
Pages. This PR does not merge, deploy, install or alter machines. The operator must
join revision-pinned H output acquisition to the existing build, preserve admission
before staging, run strict selected-set acceptance and publish only `site/dist`
after local integration accepts the candidate. No private source, fixture or
internal evidence may enter the Pages payload.

Use the actual endpoint returned by Pages, then run:

```sh
node verify-public-edition.mjs --url "$PUBLIC_BASE" --expected-inputs "$OI_LIBRARY_PUBLICATIONS" --out evidence/receiver/deployed-edition.json
python tests/publication-local-acceptance.py --url "$PUBLIC_BASE" --ref "$ESSAY_REF" --ref "$PRODUCT_REF" --ref "$BIMBA_REF"
```

Record deployed build head, source-set/edition hashes, tested refs and receipts.
The preview workflow separately observes the candidate default Pages address without
writing to it. A failed strict observation remains **not accepted**, regardless
of the preview workflow's overall status. Neither corpus nor installation is closed
by this receiver's green checks.


## Native Expression fidelity gate — 25 September 2026

The publication failure discovered on the 92-member Return-of-Zero bundle is now
a named regression. The source corpus was not the flat monochrome material seen
through the public receiver. The producer had reduced every formation to six
portable values (`glyph/x/y/z/scale/share`), mapped non-text shapes to `O`,
and the browser rebuilt those values on a hard-coded 8,192-particle monochrome
field. That portable composition is useful for SharedField discovery and safe
fallback; it is **not the native Expression body** and must never again be
presented as one.

For any published member whose source is `oi.journey` v1:

- the producer carries the exact journey bytes beside the portable Projection;
- the receiver validates the journey with the existing native validator, checks
  its SHA-256, checks every public Scene → native scene mapping, and writes the
  body as a revisioned edition sidecar;
- the browser verifies the sidecar digest before loading it and gives the exact
  native Scene directly to the existing `ProductionAdapter`;
- background, palette, material, every field/particle parameter, engine setting,
  native entity shape or source image/ASCII, text/glyph content, transforms,
  tint, force, station, sequence, composition, morph, automation, page-text
  blocks and authored view therefore remain properties of the source body.
  No publication adapter is licensed to synthesize defaults for these when the
  exact native body exists;
- a missing, changed, unsafe or unmapped native body makes the live Expression
  unavailable. Falling back to the six-parameter composition is allowed only as
  an explicitly named portable/frozen representation, never as evidence that
  the native Expression rendered faithfully.

### Curated Return-of-Zero selection

The current public selection is `PUBLICATION-CURATED.json`: **135 authored
members** — 43 E0 corpus members (including A, A′, A/C and C), one sovereign
essay reading, eight section rooms, and 83 S0–S5 product Expressions from the
pinned Point-Cloud-Demo product corpus. The 40 legacy field-study/demo/starter
journeys from the former 92-member envelope are not members of this edition.
They may survive as historical/authoring material, but must only re-enter a
public Library through an explicitly named archive collection.

The S-product set is resolved from a separately checked-out, exact
Point-Cloud-Demo revision. If that revision is absent or different, publication
fails; the producer may not silently publish only the aggregate `roz-s-products`
register Expression.

### Development/curation standard

Schema validity and visible pixels are necessary but not sufficient evidence of
an authored Expression. New or revised corpus work must show why its visual and
material decisions belong to the represented source. The review packet for each
family therefore keeps source bindings/Profile rationale and inspects real-engine
captures across representative members. Repeated defaults are legitimate only
where they are an authored family continuity; they cannot be used as a generic
template in place of decisions about glyph/form, source imagery, colour/ground,
density/size/material, force and motion, morph/sequence/automation, spatial
composition and textual projection.

Executable acceptance protects **fidelity** (exact body/digest/mapping and
absence of legacy substitution). Human/owner review protects **meaningful
composition**. A green pixel test that proves only “something rendered” does not
satisfy either standard.
