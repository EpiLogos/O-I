# agent/oi-epi-personal-450-return

`origin/agent/oi-epi-personal-450-return` @ b1b42f3 (2026-08-19) · 42
commits · Class A QUARRY-DESKTOP (§3) · parallel sibling of the
nara-lived-vertical chain (not an ancestor of 450-composed).

## What it is

The Personal `epi.personal.450` return line: binding O:I to Central's
canonical NOW/DAY Action surface (`ctrl action run`) so an Epi Personal
proposal returns into human ground as a proposal — never as human-authored
source. Introduces `local_central.rs`, the narrow Central host that later
Epi branches reuse, plus the Nara-rooted Personal depth presentation.

## Feature/function inventory

- **Central NOW host** — `desktop/core/src/local_central.rs` (narrow, 4
  actions): `inspect_now` (`projectcentral.now.inspect`),
  `return_personal_proposal` (`projectcentral.now.return`),
  `reject_return`, `promote_human_source`
  (`projectcentral.now.promote`). Header law: O:I "does not reproduce
  Central's filesystem semantics or treat a returned Epi proposal as
  human-authored source".
- **Read-only NOW history summon** — "feat: summon Central NOW history
  without leaving the Nara object" + tests: NOW history is summoned
  read-only into the Nara-rooted Personal depth presentation.
- **Nara-rooted Personal depth** — "test(epi): lock Nara-rooted Personal
  depth presentation", "ci: prove Personal 4/5/0 against Prompt-C
  producer": depth presentation locked against the real producer.
- **Real-owner exercise** — "ci(oi): exercise the real Central NOW owner
  contract in Prompt C": CI runs against the actual `ctrl` binary when
  supplied.

## Map-unit mapping

- The four NOW actions + proposal-not-ground law → **U3.3 Remember-this**
  (§5 P3) — destination landing, recognition/promotion; also §2.2 row
  "remember a selection into a chosen destination, as proposal until
  recognised … because Central-authored".
- Nara-rooted depth presentation → fog row **Nara/Epi** (§2.4).
- Real-owner CI exercise pattern → §1 law 2 walk discipline (verify by
  operation, not doubles).

## Quarry verdict

**KEEP-FOR-UNIT (U3.3) + FOG-NOTE (Nara/Epi)** — the NOW action quartet
and the "return = proposal, promote = explicit human source" semantics are
the contract U3.3 re-derives; lineage depth details feed the fog row.
