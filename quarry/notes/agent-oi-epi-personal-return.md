# agent/oi-epi-personal-return

`origin/agent/oi-epi-personal-return` @ f9f1a53 (2026-08-19) · 22 commits ·
Class B QUARRY-EPI/FOG (§3) · contains `agent/oi-epi-mode-kernel-bridge`.
**Its tip commit is byte-identical to `agent/oi-epi-nara-lived-vertical`'s
tip** — the two branches are the same content under two names; one quarry
value, two deletions.

## What it is

The branch that landed "M4 prime Nara as first lived Epi canvas" and then
proved the Personal return against the real Epi provider: episode-scoped
selection authority, a protected Nara provider exercised for real, and the
Nara presentation contract verified in CI. It is the bridge between the
kernel-bridge (how Epi mounts) and the coordinate-parity line (what Nara
guarantees).

## Feature/function inventory

- **First lived Epi canvas** — "feat: land M4 prime Nara as first lived
  Epi canvas": Nara becomes the first Epi instrument actually lived-in
  (daily writing surface), not just displayed.
- **Episode-scoped selection authority** —
  `desktop/core/tests/local_epi_provider.rs`:
  `prove_episode_scoped_selection_authority` — authority to send a
  selection is scoped to the episode that owns it.
- **Protected provider realism** — "test: exercise real protected Nara
  provider": the protected class is proven against the real provider
  process, not a stub.
- **Nara presentation contract** — `nara-presentation.test.mjs` (as on the
  coordinate-parity tip; this branch is where those UX laws first landed):
  write-first canvas, no second cosmology, exact disclosure scope.
- **Epi in stable destinations** — "test: include Epi in stable desktop
  destinations": Epi surfaces become first-class stable shell
  destinations.

## Map-unit mapping

- Whole branch → fog row **Nara/Epi composition** (§2.4).
- Episode-scoped authority → D15 authority-at-commit (authority scoped to
  the semantic object, not the surface).
- Stable destinations → U0.3b Surface-binding grammar (tabs = Surface
  bindings; Epi contributions are stable destinations).

## Quarry verdict

**FOG-NOTE** (Nara/Epi). Operationally: quarried together with
`agent/oi-epi-nara-lived-vertical` — **one note's knowledge, two branch
deletions**; the owner can rule them in one breath.
