# agent/oi-epi-nara-lived-vertical

`origin/agent/oi-epi-nara-lived-vertical` @ f9f1a53 (2026-08-19) · 22
commits · Class B QUARRY-EPI/FOG (§3).

## What it is

The "Nara lived vertical" branch — **identical tip to
`agent/oi-epi-personal-return`** (both at f9f1a53; verified
`git merge-base --is-ancestor` both ways). It is the same 22-commit line:
M4 prime Nara as the first lived Epi canvas, episode-scoped selection
authority, the real protected Nara provider, and the Nara presentation
contract. The name records the intent ("lived vertical": an Epi surface
actually inhabited daily, not observed).

## Feature/function inventory

Identical to `agent/oi-epi-personal-return` — see
`quarry/notes/agent-oi-epi-personal-return.md`. Load-bearing files:
`desktop/core/src/local_epi.rs` (Nara daily/selection provider host),
`desktop/core/tests/local_epi_provider.rs` (episode-scoped authority,
real protected provider), `desktop/ui/src/NaraSurface.tsx` +
`nara-presentation.test.mjs` (write-first canvas; no second cosmology).

## Map-unit mapping

Fog row **Nara/Epi composition** (§2.4); episode-scoped authority → D15;
stable destinations → U0.3b — as in the personal-return note.

## Quarry verdict

**FOG-NOTE — duplicate of `agent/oi-epi-personal-return`.** Recommend the
owner rules QUARRY-DELETE for both in one action; keeping either branch
adds nothing the other lacks.
