# Wayfinder — Expression Production Suite

**Ground:** `docs/EXPRESSION-PRODUCTION-SUITE-ALIGNMENT.md`  
**Rule:** accepted K8–K10 / AW / L5 architecture is prerequisite, not work to redesign.

## Parent sessions

### T1 — QL-MEF K/AW + Bimba→M′

Parent integrates **3 development subagents**:

1. K8–K10/L5 implementation backcheck: exact M→M′ binding, current Profiles/Scenes/Actions, no kernel reopen.
2. AW/Ta-Onta continuity: S′ application contracts + Vāk C′ runtime + existing constitutional/specialist roster.
3. Bimba Expression production: source-bound coordinate/branch Profile/artifact generation into the QL namespace of Point-Cloud-Demo.

### T2 — Point-Cloud production sandbox

Parent integrates **2 development subagents**:

1. durable output convention + only the smallest repeatable generation/import/export helper actually needed;
2. shared editor/engine quality + common authoring defects + occurrence/provenance indexing.

QL and Antykathera own their content fan-out. No new runtime/service architecture.

### T3 — O:I desktop Expression world

Parent integrates **3 development subagents**:

1. generic native carriers/Profile/Edition/Portal/front-verso over the already-integrated engine;
2. Wiki/graph/constellation/Expression shared-ref state and source/deixis/portal behaviour;
3. desktop lifecycle/UX/SharedField plus consumer hooks for canonical Agents, including Nara when generic speech Agency lands.

### T4 — generic speech Agency + Nara consumer

Parent integrates **4 native-owner development subagents**:

1. AIKit #317 — generic speech/audio ModelSurface + HarnessComposition resolution;
2. Actuation #94 + #91 — model-bearing speech constitution, Nara Agency/authority/delegation;
3. QL-MEF #201 — Nara/Ta-Onta/M4 semantic context and exact deictic/Epii relations;
4. O:I #336 — desktop microphone/playback/transcript/deixis/Expression-act integration.

Joined acceptance happens in the O:I desktop cradle, not Point-Cloud-Demo.

### T5 — Return-of-Zero corpus

Parent owns E0 census, source packet production, artifact inventory and continuity. Use **6 content-production subagents**:

1. essay + section rooms;
2. A + A′;
3. C + A/C + S + Concepts/other Episteme;
4. Matheme;
5. Symbolon;
6. Whole Mythemes.

Each writes disjoint source-bound artifacts into the Return-of-Zero Point-Cloud collection. No common engine edits and no automatic worktrees for ordinary Profiles/HTML/artifacts.

## Runtime agent law

Development subagents above are implementation workers only.

Actual Epi runtime composition remains the accepted Ta-Onta system:

```text
Khora → Hen → Pleroma → Chronos → Anima → Aletheia
```

Anima's constitutional field is `Anima · Nous · Logos · Eros · Mythos · Psyche · Sophia`. Existing specialists `Anansi · Janus · Moirai · Mercurius · Agora · Zeithoven` are dispatched when the C′/CF/CFP/CS composition warrants them. Techne remains under Pleroma.

## Convergence gates

**G0** — correction docs landed; old Atelier architecture removed.  
**G1** — Point-Cloud output convention stable enough for both corpora; no new runtime required.  
**G2** — QL M→M′ bindings + Bimba production and Return-of-Zero production proceed in parallel over real engine.  
**G3** — O:I desktop loads/inhabits generated artifacts through existing integrated engine.  
**G4** — AIKit/Actuation generic speech model surfaces accepted; Nara consumer stack joins in O:I.  
**G5** — full corpus/SharedField/desktop acceptance and returned praxis through existing AW/Aletheia/Epii paths.

## Handoff minimum

Every parent session returns:

```text
native repo + exact revision
claimed lanes
landed PRs/commits
actual tests/evidence
source/artifact revisions
cross-product interface changes
unresolved native-owner defects
next convergence gate
```

Do not return a new conceptual architecture unless observed implementation reality genuinely forces a source revision.
## W5 / lane F — embedded native backcheck, 20 September 2026

Implementation owner is **O:I #419**, coordinated on #375 comments 5747132088 and
5747183605. This is a repair of the current imported application's connection,
not a reopening of K8–K10 or a new donor application. Starting published cut:
`a222c8bdf46a7b2146084770b704dfbe83ce5027`; native source/client cut:
QL-MEF `5970757e25532c7329f07f4819273155c4f3f135`. The requested new local
reconciliation receipt was not in the retrieved #65/#220 comments. Unpublished,
installed and running source therefore remain local reconciliation inputs, not
claims made by this branch.

The recovered code had an accepted focused-instrument native lease, but the
actual `expressions-app/field-studies-journeys` production stage was not attached
to it. The repair carries that existing numerical/audio contract into the
imported application. The generic scene remains useful without QL. Choosing
native follow is explicit; a disconnected producer freezes the affected work
and reports unavailable rather than switching to a self-running imitation.

### Stable host/adapter contract for Factory, Technè and Nara consumers

`KernelOp.native_expression` accepts `open {path, expected_revision}`,
`exchange {lease, request}` and `close {lease}`. The source path must be disclosed
by Central and is reread at its exact revision. An
`oi.native-expression-binding/v1` document contains the original native host
`basis` and `field`, plus `presentation.units_per_metre`, `slots_a`, `slots_b`.
Each slot array covers **the full GPU texture, including padding**, with explicit
indices into the native sample array. `retainedTopology()` reports drawn count,
texture dimensions and required slot count; it does not invent a correspondence.
Do not turn the test's repeated sample mapping into a production default.

Executable selection is host-only: absolute `OI_QL_FIELD_HOST_BIN` and
`OI_QL_FIELD_WORKER_BIN`. A frame cannot supply an executable. One kernel manager
admits one native driver; a second driver receives a named ownership refusal.
The same iframe's `host-mode` switch keeps its stage and lease. The shared shell
must reuse that mounted host to preserve seamless cross-arrangement continuation;
creating another iframe is not an alternate license to simulate the same event.
W1 retains that shared-shell writer boundary.

`oi.native-expression/v1` messages are source-window/epoch/lease bound. A late
open is closed; a reply from a released lifetime cannot acquire a new stage or
restart its work. Unknown acknowledgements are never automatically replayed.
Native process teardown owns only its dedicated process group, including pipe-
holding descendants. There is no machine-wide process kill or service restart.

The byte-identical QL `InstrumentSession`, `NativeAudioBinding` and
`RetainedFieldBinding` clients retain their provenance and tests. Native C/Rust/
C++ owns domain calculations, target positions, PCM and sample time. The existing
GPU owns resident particle mechanics; the original application owns its stage,
camera and render loop. Native follow disables the renderer's competing cymatic
and toroidal oscillators. No new numerical integration or audio synthesis is
introduced by this lane.

### Complete affected producer/consumer accounting

| Required family | Actual path and consumed effect | Proof and remaining boundary |
|---|---|---|
| M1 source/topology/harmonics | Native coupled M1 output and carrier enter C++ native targets; all returned M1 sources are inspected. Carrier vectors also draw directly on the existing stage. | Exact target mapping, stable identities and native carrier-tick change. Harmonic-row requests change native config; a separately selected canonical harmonic is not falsely described as retuned by row selection. |
| M1 geometry/phase | Existing C++ samples retain constituent and sample identity; metres are converted only by an explicitly supplied presentation scale. | Property/overflow/stale/ref/density tests. No renderer-inferred source geometry or eigenmodes. |
| M2 resonance/material/cymatics | Original complete M2 basis and stamps enter the accepted modal owner; native targets and native PCM share the InstrumentSession cursor. Renderer cymatic approximation is disabled while following. | Real owner replacement preserves the original failure for an unchanged M2 generation, then succeeds with an explicitly newer generation and matching nested input stamps. Arbitrary material replacement is refused by the existing owner, not faked. |
| M2 music/audio | Returned PCM enters the real AudioContext graph through the accepted bounded audio client; no browser oscillator reconstructs native music. | Nonzero native PCM, sample-rate/device checks, mute, hold, late/device/producer failure tests. Audible loudspeaker and empirical material response are separate local observations. |
| M3 transcription/form | Complete returned M3 codon, DNA/RNA transcription and source-form angles are consumed by the stage's text/form reading. The explicit RNA control changes the native seed/basis, not only a label. | Native new output and preserved original source. Arbitrary glyph meshes and a physical pose are **not** supplied by this compact target contract; displayed source angles are explicitly not promoted to such a pose. Full mesh/crease correspondence remains a native form-output join, not claimed by the overlay. |
| M3 inscription/lensing clock | Native `set-axis` operates the existing owner. Presented target packets provide the exact half-degree/turn readback and stage clock hand. | Actual target/clock changes; no display-frame clock used as a substitute. Exact integer turns are validated without lossy number conversion. |
| Subject/occasion/currentness | Original event, subject, registry, geometry/material/model refs and native generation/sample cursors remain with each receipt. Source inspection is separate from presented cursor. | Foreign/missing M1/M2/M3 source admission fails even when target arrays still look plausible. Source generations are not presentation-frame revisions. |
| Controls/presentation | Domain requests go through native `replace`/`set-axis`; camera and explicit scale remain presentation operations. | Scale changes actual target textures without changing native state. Authored scene membership/topology/density/configuration changes require rebind rather than silently remapping resident samples. |
| Lifecycle/resources | Open, hidden admission, hold, pending edit, resume, unknown transport, reload/unmount, source edit, release and explicit restart retain their separate effects. | Late-epoch/one-close/held-edit regressions; native single-driver/process-group cleanup/restart tests; software-WebGL interaction. Release does not imply historical rewind. |
| Checkpoint | Existing retained engine can capture position and velocity at the same native cursor and restore after supported context restoration. | Unsupported restore stays a refusal. Cross-process restart and arbitrary GPU rewind remain unsupported; actual GPU context-loss/recovery and mounted desktop continuation require local replay. |
| G/W6, H/W7 and full corpus | Nara/voice and Wiki/collection/lens consumers use the same host and native refs; this lane does not rewrite their source, Agency or scene semantics. | #336/QL-MEF #201 voice/microphone/live provider, Wiki/Technè source-return and complete hosted corpus obligations remain with their owners. This repair neither closes them nor makes them optional. |

### Executable checks and evidence standing

From `desktop/cradle`, the bounded commands are:

```sh
node --test tests/native-expression.test.mjs
python3 tests/native-expression-local.test.py
node tests/native-expression-browser.mjs
```

The dedicated workflow also runs the complete existing embedded regressions,
accepted native client regressions, strict TypeScript/application build, kernel
regressions, real native C++ build/tests, real manager acceptance and
`tests/native-expression-native-browser.mjs` through the **compiled production
walk-bridge, kernel manager, real QL host/worker and embedded WebGL application**.
Only Central disclosure and the named captured input are controlled in that last
join; its M1/M2/M3 outputs, PCM and targets are not fixtures.

The earlier failing browser correspondence and stale M2-generation replacement
are retained in run `35487032890`; the latter remains an executable negative
case. At the historical `9926f4d` cut, whole legacy React-shell lint was retained
as a failing check rather than skipped or weakened. The final repair below now
passes that same lint; the original failure remains in its historical receipt.
Source/CI receipts on #419 and dependent #446 give exact executed heads and
per-check results, not a claim that every wider integration gate is green.

Measurements record source/binary hashes, GPU renderer, viewport, slot/drawn
counts, machine/Node/browser facts and per-operation latency count/mean/p95/max
for the actual bounded run. Those measurements are not a sustained real-time,
Mac GPU, simultaneous multi-Nara or material-fidelity performance certification.
Source inspection and failed/refused work remain in the measured trace.

### Bounded local engine/audio/material packet

After the local integrator selects/builds the approved candidate (not this web
lane), run this read-only preflight with exact local paths:

```sh
python3 desktop/cradle/tests/native-expression-local.py \
  --expected-head "$ACCEPTED_OI_HEAD" \
  --host "$ABS_QL_FIELD_HOST" --worker "$ABS_QL_FIELD_WORKER" \
  --bridge "$ABS_CRADLE_WALK_BRIDGE" --input "$ABS_COUPLED_INPUT" \
  --output "$NEW_PRIVATE_EVIDENCE_DIRECTORY"
```

Exit 2 means **runtime proof pending**, not pass. Add `--run-browser-join` to
explicitly start isolated owned test processes; add `--hardware-gpu` to request
the default device instead of SwiftShader. The actual renderer is recorded.
The packet does not install dependencies, merge, replace a running application,
connect to another machine or export evidence. Its output may include private
basis data and device facts: keep it private until reviewed.

The installed Mac/WKWebView episode still must record actual source → built →
installed → resident-process references, then use the real native binding in the
actual Expressions surface: connect muted; inspect native refs; unmute only by
explicit choice; change carrier tick, a native material parameter, transcription
and each clock axis; hold; hide/switch/re-enter; resume; test a failed or stale
binding; release/reopen. Verify resident particle continuity and one audible
owner, actual speaker output at the declared sample rate, GPU context loss and
supported checkpoint recovery, repeated mount/unmount resources, and sustained
mixed field/audio/agent load. Original failures stay outside cleanup.

Physical material/acoustic comparison requires the declared resonator model,
actual measurements and tolerances; a supplied modal fixture is not that proof.
Microphone permission/capture/interruption/reconnect and a real model/harness
belong to G/W6's existing #336/QL-MEF #201 packet on the same host, not a second
voice implementation here. Human experience and independent replay remain
separate from implementation/controlled/CI acceptance. Both suite self-
inhabitation and the complete published corpus remain required near-term results.

Native backcheck run `35505265017` returned two additional concrete failures:
M3's seed stamp must match the new M2 basis generation, and a 40 ms audio lead
cannot admit an observed 64.6 ms native round trip at 62,000 drawn particles on
the software-GPU runner. The repaired consumer keeps the accepted generation
checks and uses explicit buffered delivery (8,192-frame blocks, 250 ms lead,
500 ms lookahead). Readback and the control depth disclose this latency cost;
late delivery still holds, with no automatic rebase, dropped-sample fiction or
reduced semantic input. The real-native workflow uses the production release
host; exact debug/release sources and binary hashes remain distinct evidence.


### Final repository-side repair before local integration

The final continuation retains the accepted native producer/relay contracts and
repairs the retained React shell rather than waiving its lint. Exact replay of
both `a222c8b` and `9926f4d` produced **22 TypeScript diagnostics** with identical
text; the previous PR summary's count of 42 was incorrect. Existing imperative
methods are now declared in the component ref; colour ranges use the existing
`ParamRow` (including negative speed and degree ranges); physical resonance
anchors receive view labels, not fabricated semantic name/colour fields; PNG
capture uses the already-bounded `renderImage` and existing PNG/download helper.
Render/encoding failures remain failures and cannot announce a successful export.

The broader desktop job actually failed the **pre-paint CSP** assertion, not the
previously reported mobile-overflow check. Its returned job log was accessible;
only an incorrect artifact-ID download returned 404. The exact inline bootstrap
had changed while its allowed script hash had not. The hash is reconciled
without changing bootstrap meaning, widening script permissions, or introducing
`unsafe-inline`/`unsafe-eval`. `native-expression-final.test.mjs` guards source
and built script bytes; the existing `theme-prepaint-csp.mjs` checks actual
browser enforcement and still refuses changed scripts.

`native-expression-legacy-browser.mjs` exercises real retained React controls,
resident-engine ref delegation, GPU-state-preserving PNG capture, and rendering/
encoding refusals. The native-backcheck workflow runs it alongside all original
checks, full build, full retained-shell lint and both CSP regressions. Exact-head
results belong to the final #419 receipt, not to this source declaration.
Browser execution in the web work container was denied by its administrator
policy; no policy was altered. Hosted GitHub CI is the separate execution venue.

This closes neither native arbitrary mesh/physical-crease output nor detached
outer-shell continuity by assertion. Those remain the concrete production and
integrated-runtime obligations in the table above. Existing native work is not
replaced by invented physical correspondence. Local integration must reconcile
its newer source cut, review this PR and run the committed opt-in packet; no
local suite has been accessed or installed by this continuation.

### Recovered final cut and receipt admission — #419 → #446

The interrupted continuation's committed source was recovered, not overwritten:
`6f444e60feb7b011103c55570e95916538d0e528` repairs retained controls/PNG/CSP;
`debc1c6d8830eef29f51843910471878e1bc2ab6` adds bounded isolated timeout and
SIGINT/SIGTERM cleanup; `bbb0571eaed2d060b87e1c9250a2d067869ba09c` makes the
native Node regressions use Cradle's locked dependencies without requiring an
unrelated nested installation. #419 remains unmerged at that source cut.

At `bbb0571`, native run **35524255474**, O:I desktop **35524255436** and Search
**35524255480** completed successfully. Downloaded native artifact **10609229448**
has SHA-256 `3e6e746c932a126d3fe9c30b2606bb10b38e1d3e41c1f84e621f57136a7b5111`.
Its logs prove 15 focused, 53 existing embedded, 32 accepted native-client,
10 packet-safety and two CSP-byte tests; 22 browser-enforced CSP cases; six
retained-control/PNG cases; both ten-check embedded/native browser scenarios;
actual GPU effect/disconnection and compiled native manager/C++ checks. The
normal kernel run has 100 passes and 52 ignored; this lane's native owner test
is separately executed, leaving 51 owner/environment-specific tests unproved.

Wider integration is not all-green: OI Verify **35524255475**, synthetic merge
`7618c2ea7b03ce37a0911595b627c324f026239b`, fails update-lock recovery at
`src/update_flow.rs:1320`. Shell recovery **35524255470** has shared-shell
failures and its wider Node artifact reports 144 passes/one failure in the
legacy Day roster (`personal-web.test.mjs:15`, five versus four). The old
mobile-overflow diagnosis is withdrawn, not carried as another defect.
These are separate owner/integration gates; this lane does not waive them.

Dependent **#446**, based exactly on #419's `bbb0571`, strengthens the actual
local packet: child exit zero alone is insufficient. It admits only an exact,
unambiguous successful joined receipt with the original four binary/input paths
and hashes, one native open/close, and reported executed checks. It rehashes the
files and rechecks the selected source HEAD and tracked bytes after execution.
Missing, failed, foreign, duplicate-key, nonfinite, oversized (16 MiB maximum)
or symlink evidence refuses acceptance without deleting the returned evidence.
The admitted receipt's SHA-256 and original child exit code remain inspectable.
This is consistency verification of the behavioural result, not independent
acceptance, a signature, or proof of installed devices.

`python3 tests/native-expression-receipt.test.py` adds nine deterministic tests;
the original packet fails their negative cases and the repaired packet passes.
Existing safety tests are unchanged. The native-backcheck now runs the shipped
Python packet around the unchanged real native-to-browser scenario, retaining
all original behavioural assertions and recording `local-join/local-receipt.json`
and `local-join/browser/joined.json`. Exact final-head results are recorded on
#446 after execution; this source declaration does not manufacture a pass.

Local integration should fetch and review **#419 then #446 as one dependent
stack** against the current #65/#220 reconciliation receipt. Preserve newer
local/main work; do not resolve conflicts by wholesale branch replacement.
Build and run the same checks on the actual reconciled candidate, then run the
bounded local packet with that candidate's exact HEAD and explicit native paths.
Merge only the reviewed/tested result through the local integration workflow.
Report source/build/install/running standing separately and retain the physical
form-output, outer-host continuity, real device/provider, corpus and independent
acceptance obligations above. No web-lane merge, deployment or machine mutation.


## T5→public Expression fidelity return — 25 September 2026

T5 corpus production and the O:I public receiver now have an explicit joining
gate. A produced `oi.journey` is the authored Expression body. The
`oi.expression-composition/v1` six-parameter SharedField document is only a
portable projection/index representation; it cannot replace the body at the
site, desktop or any other consumer.

For Return-of-Zero publication, the selected field is A + A′ + A/C + C, the
sovereign reading and eight section rooms, plus the complete S0–S5 product
Expression source set. Legacy field-study/demo/starter journeys are not part of
that current authored field and must be separately named if ever exposed as an
archive.

Publication acceptance must preserve, rather than infer, the source scene's
background/palette/material and complete particle parameters; native
shape/text/image-or-ASCII source; transforms/tints/forces/stations; sequences;
composition and morph laws; automation; engine/view settings; and authored text
blocks. Exact journey bytes, digest and Scene mapping are the executable
criterion. The qualitative corpus criterion remains source-bound symbolic
composition: a valid file with default particles or arbitrary glyphs is not
accepted merely because it renders. Family Profiles/bindings and real-engine
visual review must make the relation between represented content and its
material/glyphic treatment inspectable.

The regression behind this gate was concrete: the site producer collapsed
non-text forms to `O`, retained only `glyph/x/y/z/scale/share`, and the browser
then hard-coded an 8,192-particle monochrome field. This violated the earlier
publication law against lossy conversion despite green pixel tests. Future
acceptance must fail on that class of substitution.
