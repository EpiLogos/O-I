# Wayfinder — Expression Production Suite

**Ground:** `docs/EXPRESSION-PRODUCTION-SUITE-ALIGNMENT.md`  
**Rule:** accepted K8–K10 / AW / L5 architecture is prerequisite, not work to redesign.

## Parent sessions

### T1 — QL-MEF K/AW + Bimba→M′

Parent integrates **3 development subagents**:

1. K8–K10/L5 implementation backcheck: exact M→M′ binding, current Profiles/Scenes/Actions, no kernel reopen.
2. AW/Ta-Onta continuity: S′ application contracts + Vāk C′ runtime + existing constitutional/specialist roster.
3. Bimba Expression production: source-bound coordinate/branch Profile/artifact generation into the QL namespace of Point-Cloud-Demo.

### T2 — Point-Cloud production sandbox

Parent integrates **2 development subagents**:

1. durable output convention + only the smallest repeatable generation/import/export helper actually needed;
2. shared editor/engine quality + common authoring defects + occurrence/provenance indexing.

QL and Antykathera own their content fan-out. No new runtime/service architecture.

### T3 — O:I desktop Expression world

Parent integrates **3 development subagents**:

1. generic native carriers/Profile/Edition/Portal/front-verso over the already-integrated engine;
2. Wiki/graph/constellation/Expression shared-ref state and source/deixis/portal behaviour;
3. desktop lifecycle/UX/SharedField plus consumer hooks for canonical Agents, including Nara when generic speech Agency lands.

### T4 — generic speech Agency + Nara consumer

Parent integrates **4 native-owner development subagents**:

1. AIKit #317 — generic speech/audio ModelSurface + HarnessComposition resolution;
2. Actuation #94 + #91 — model-bearing speech constitution, Nara Agency/authority/delegation;
3. QL-MEF #201 — Nara/Ta-Onta/M4 semantic context and exact deictic/Epii relations;
4. O:I #336 — desktop microphone/playback/transcript/deixis/Expression-act integration.

Joined acceptance happens in the O:I desktop cradle, not Point-Cloud-Demo.

#### T4 return — native identity and personal desktop, 21 September 2026

**Authored ground:** `docs/positions/FOUNDING-POSITIONS.md`. This increment meets the existing Central user field and original Day/Flow/Journal documents. It does not replace them with a biography schema, separate diary, model-generated identity or new renderer. Source writing, disclosure, native acceptance and publication remain different acts. The six identity layers are optional; missing birth time, chart, assessment or interpretation remains unavailable, not guessed.

**Review stack, not a merge/installation receipt:** O:I #469 on `agent/nara-personal-completion-20260921`, above #460 (`14f62167bbaa1310be00e3105b5040099bf18fff`) and #442 (`0cd377905efe55ba7b677ced223aeee6fa5eb722`). Production identity consumer `2f30d4d77de75173d045b505fdd85e766e150388`; source-pinned read-only CI `e0d3ac3aaf4da1156aec76e6c83e344caf4bc765`; receiving-centre accessibility repair `ca87bab8ab82dcd8ae3e81fc1ffd1ee8001d85a9`. The local reconciliation lead must recover the actual #65/#220 integrated cut and newer shared-file successors before combining these dependent changes. Remote main is not substituted for that receipt.

**Native dependency:** QL-MEF #236 `deb332d42fa7e9925414b0b70eab18bf1ba3439a`, above #235 `0807b827b63732a38c58660a36ca7e5988e09164` and the existing personal owner `f84fb896c295fb75d365e558f60e8e4f50fd366d`. #235 preserves unique source links across distinct practice hold/review/close receipts: the same actual Flow may support several acts. The old duplicate-link refusal is original failure evidence, not a requirement to manufacture new files. Its capability parser also rejects unknown private fields.

**Current identity contract supersedes the renderer-fingerprint description in `docs/contracts/NARA-SPEECH-EXPERIENCE-V1.md` §10.** The desktop no longer computes `sha256-identity-source-basis/v1`. It consumes the native `ql.nara-identity-material/v1` / `sha256-native-identity-basis/v1` over canonically ordered accepted identity evidence and provenance. This is a protected exact-basis digest, not a similarity metric, person identifier, natal inference or archetypal-quintessence compression law. Native M3 address, identity quaternion reference and current reception orientation remain separate. Empty or unprovided identity is not turned into a demo hash.

The human path is: continue the existing private occasion; select or write ordinary Central sources; optionally review six source-backed layers; read native identity material without mutation; reject or explicitly accept its exact revision; choose an existing Expressions window and authored chakral form; show privately; return to the unchanged authored Expression. Preview, seal and presentation recheck the native record and selected Central source revisions. A changed/withheld source refuses; no replacement source is searched. Native seal records its actual receipt and identity-source replacement invalidates stale derivative references. The minimal `oi.private-identity-presentation/v2` packet contains only the accepted digest, current quaternion and chosen form. It carries no prose, source addresses, personal subject refs, transcript or receipts into the hosted renderer.

The existing Expressions engine and chakra/yantra/cymatic form factories remain the body. Seven receiving centres are not resonator stations. Private rendering does not enter DocumentStore, public editions or the scene library. Capture, checkpoint and generic engine diagnostics are blocked while private. Exit restores ordinary scene targets and uses the existing native reset to remove residual private particles **before** unlocking capture; a failed reset stays private. This reseeds the ordinary field rather than claiming a rewind of GPU, audio or provider history. It does not steal an active native material lease. A continuous personal overlay on that live M4 lease remains the shared-field owner's distinct join.

**Executed evidence and original failures:** the recovered-container suite at the production consumer passes 157 tests, zero failed/skipped; the hosted Expressions build passes. Run35607054441 proves complete Cradle/Expressions builds and a freshly compiled pinned QL #236 consumer join, but its personal browser still failed the centre label assertion. Merely waiting for the heading was insufficient: the selector lacked a stable explicit accessible name. `ca87bab8` repairs the production label and strengthens the browser to select native ordinal six and wait for every actual branch view, without removing capture/privacy checks. The final-head browser result belongs to #469's exact CI receipt; it is not inferred from these earlier passes. Earlier #460 failure35551344485 remains intact. Source-recovery omissions and unavailable container browser downloads are not product passes or reasons to stub production.

Reproduce on the selected complete source checkout:

```bash
cd desktop/cradle
npm ci --no-audit --no-fund
npm ci --prefix expressions-app --no-audit --no-fund
node --experimental-strip-types --import ./tests/ts-register.mjs --test \
  tests/nara-personal-contracts.test.mjs tests/nara-personal-telemetry.test.mjs \
  tests/nara-personal-identity.test.mjs tests/nara-speech-conformance.mjs \
  tests/dictation-conformance.mjs tests/nara-runtime.test.mjs \
  tests/nara-audio-contract.test.mjs tests/nara-local-acceptance.test.mjs
npm run build
npm --prefix expressions-app run build
npx playwright install chromium
node --experimental-strip-types --import ./tests/ts-register.mjs tests/nara-native-browser.mjs
node tests/nara-personal-browser.mjs
# QL_NARA_TEST_BIN must name a built, pinned QL #236 or verified successor.
# This test uses its own disposable private World, not the person's root.
QL_NARA_TEST_BIN="$QL_TEST_EXECUTABLE" \
  QL_NARA_TEST_REVISION="$QL_TEST_REVISION" node tests/nara-personal-native.mjs
```

**Bounded local integration/acceptance:** retain the existing three-track/worktree arrangement and one computer-use driver. Preserve dirty sources, Central Control, Day/NOW, original Flow HTML and active native sessions. Integrate the dependent QL and O:I cuts with their current successors rather than resetting to these old parents. Record source/build/install/running cuts separately. Run the repository and native gates; then use the real existing user sources and actual selected speech body. First prove source writing and Day/Flow access without QL/microphone opt-in; then optional identity linkage, rejection, native acceptance, private form display, stale-source refusal and ordinary-scene restoration. Exercise each centre independently, oracle originality, repeated practice with the same real source, branch-exact perspectives, and attributed response return without rewriting human Journal/notes/media. Use the opt-in executable speech packet in the Nara contract §9 for readiness, saved-WAV/provider round trip, exact-delivery recovery and reconnect. Physical OS microphone/denial, audible playback/barge-in, native window interactions and human judgement need separately labelled evidence, never synthetic promotion. Keep all private recordings, transcripts and detailed source receipts local; only non-sensitive revision/test summaries belong on #65/#220.

**Still open under T4, not hidden as local testing:** complete ordinary native body/catalogue setup, provider-declared realtime/delivery-scoped cancellation where supported, restart/detached personal encounter continuity, and broader non-focus Epii/native proposals remain the existing native-owner joins described by #336 and the speech contract. This PR does not certify them or redefine a manual HTTP cascade as duplex realtime. Shared M1–M3/physics/stage and generic session composition keep their respective writers. G4/G5 are not closed by the identity slice. Both installable/self-inhabiting technology and complete published corpus remain required; private identity/audio/dialogue is not publication material.

### T5 — Return-of-Zero corpus

Parent owns E0 census, source packet production, artifact inventory and continuity. Use **6 content-production subagents**:

1. essay + section rooms;
2. A + A′;
3. C + A/C + S + Concepts/other Episteme;
4. Matheme;
5. Symbolon;
6. Whole Mythemes.

Each writes disjoint source-bound artifacts into the Return-of-Zero Point-Cloud collection. No common engine edits and no automatic worktrees for ordinary Profiles/HTML/artifacts.

## Runtime agent law

Development subagents above are implementation workers only.

Actual Epi runtime composition remains the accepted Ta-Onta system:

```text
Khora → Hen → Pleroma → Chronos → Anima → Aletheia
```

Anima's constitutional field is `Anima · Nous · Logos · Eros · Mythos · Psyche · Sophia`. Existing specialists `Anansi · Janus · Moirai · Mercurius · Agora · Zeithoven` are dispatched when the C′/CF/CFP/CS composition warrants them. Techne remains under Pleroma.

## Convergence gates

**G0** — correction docs landed; old Atelier architecture removed.  
**G1** — Point-Cloud output convention stable enough for both corpora; no new runtime required.  
**G2** — QL M→M′ bindings + Bimba production and Return-of-Zero production proceed in parallel over real engine.  
**G3** — O:I desktop loads/inhabits generated artifacts through existing integrated engine.  
**G4** — AIKit/Actuation generic speech model surfaces accepted; Nara consumer stack joins in O:I.  
**G5** — full corpus/SharedField/desktop acceptance and returned praxis through existing AW/Aletheia/Epii paths.

## Handoff minimum

Every parent session returns:

```text
native repo + exact revision
claimed lanes
landed PRs/commits
actual tests/evidence
source/artifact revisions
cross-product interface changes
unresolved native-owner defects
next convergence gate
```

Do not return a new conceptual architecture unless observed implementation reality genuinely forces a source revision.