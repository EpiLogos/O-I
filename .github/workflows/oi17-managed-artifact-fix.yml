name: OI17 managed artifact fix

on:
  push:
    branches:
      - converge/oi-017-first-suite
    paths:
      - scripts/oi17_managed_artifact_fix.py
      - .github/workflows/oi17-managed-artifact-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: converge/oi-017-first-suite
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: python scripts/oi17_managed_artifact_fix.py
      - run: cargo fmt --manifest-path cli/Cargo.toml
      - run: cargo clippy --manifest-path cli/Cargo.toml --all-targets --locked -- -D warnings
      - run: cargo test --manifest-path cli/Cargo.toml --locked
      - name: Commit repair
        run: |
          git config user.name oi17-convergence-bot
          git config user.email actions@users.noreply.github.com
          git add cli/src/verification.rs surfaces.json
          git diff --cached --check
          git commit -m 'fix(verify): consume managed artifact evidence for installed components'
          git push origin HEAD:converge/oi-017-first-suite
