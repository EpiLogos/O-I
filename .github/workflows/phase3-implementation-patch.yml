name: Apply Phase 3 exchange-authority patch

on:
  push:
    branches: [agent/oi-017-exchange-authority]
    paths: ['.github/workflows/phase3-implementation-patch.yml']

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/oi-017-exchange-authority
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Patch Phase 3 authority boundary
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          def read(path): return Path(path).read_text()
          def write(path, text): Path(path).write_text(text)
          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected exactly one anchor, found {count}')
              return text.replace(old, new, 1)

          # ------------------------------------------------------------------
          # SpaceTimeDB: private request -> grant -> bounded use authority state.
          # ------------------------------------------------------------------
          path = 'shared-field/spacetimedb/src/index.ts'
          s = read(path)

          s = replace_once(s,
          "const CONTACT_MAX_TTL_SECONDS = 7 * 24 * 60 * 60;\n",
          "const CONTACT_MAX_TTL_SECONDS = 7 * 24 * 60 * 60;\n"
          "const EXCHANGE_MAX_TTL_SECONDS = 24 * 60 * 60;\n"
          "const EXCHANGE_MAX_USES = 64;\n"
          "const EXCHANGE_MAX_SCOPE_BYTES = 8 * 1024;\n"
          "const EXCHANGE_MAX_OUTSTANDING_REQUESTS = 8;\n"
          "const EXCHANGE_PROTOCOLS = new Set(['a2a', 'mcp', 'http']);\n"
          "const EXCHANGE_MODES = new Set(['message:send', 'task', 'data']);\n",
          'exchange constants')

          tables = r'''
const exchangeRequest = table(
  { name: 'exchange_request', public: false },
  {
    requestRef: t.string().primaryKey(),
    fieldRef: t.string().index('btree'),
    initiatorParticipantRef: t.string().index('btree'),
    initiatorActorIdentity: t.identity().index('btree'),
    counterpartyParticipantRef: t.string().index('btree'),
    purpose: t.string(),
    scopeJson: t.string(),
    protocol: t.string().index('btree'),
    bindingRef: t.string(),
    bindingRevision: t.u32(),
    modesJson: t.string(),
    requestedMaxUses: t.u32(),
    requestedTtlSeconds: t.u32(),
    state: t.string().index('btree'),
    createdAtMicros: t.u64(),
    requestExpiresAtMicros: t.u64(),
    decisionRef: t.string(),
  }
);

const exchangeGrant = table(
  { name: 'exchange_grant', public: false },
  {
    grantRef: t.string().primaryKey(),
    requestRef: t.string().unique(),
    fieldRef: t.string().index('btree'),
    initiatorParticipantRef: t.string().index('btree'),
    initiatorActorIdentity: t.identity().index('btree'),
    counterpartyParticipantRef: t.string().index('btree'),
    purpose: t.string(),
    scopeJson: t.string(),
    protocol: t.string().index('btree'),
    bindingRef: t.string(),
    bindingRevision: t.u32(),
    modesJson: t.string(),
    maxUses: t.u32(),
    usedUses: t.u32(),
    state: t.string().index('btree'),
    grantedAtMicros: t.u64(),
    expiresAtMicros: t.u64(),
    endedAtMicros: t.u64(),
    grantorIdentity: t.identity().index('btree'),
    decisionRef: t.string(),
    provenanceJson: t.string(),
  }
);

const exchangeUse = table(
  { name: 'exchange_use', public: false },
  {
    useKey: t.string().primaryKey(),
    grantRef: t.string().index('btree'),
    operationId: t.string(),
    fieldRef: t.string().index('btree'),
    actorIdentity: t.identity().index('btree'),
    counterpartyParticipantRef: t.string(),
    protocol: t.string(),
    bindingRef: t.string(),
    bindingRevision: t.u32(),
    mode: t.string(),
    purpose: t.string(),
    scopeJson: t.string(),
    usedAtMicros: t.u64(),
    demandFingerprint: t.string(),
  }
);

'''
          s = replace_once(s, "const spacetimedb = schema({\n", tables + "const spacetimedb = schema({\n", 'exchange tables')
          s = replace_once(s,
          "  contactRate,\n});\n",
          "  contactRate,\n  exchangeRequest,\n  exchangeGrant,\n  exchangeUse,\n});\n",
          'exchange schema registration')

          s = replace_once(s,
          "function contributionRateKey(fieldRef: string, originRef: string): string {\n  return fingerprintString(`${fieldRef}\\u001f${originRef}`);\n}\n",
          "function contributionRateKey(fieldRef: string, originRef: string): string {\n  return fingerprintString(`${fieldRef}\\u001f${originRef}`);\n}\n\n"
          "function exchangeUseKey(grantRef: string, operationId: string): string {\n  return `exchange-use:${fingerprintString(`${grantRef}\\u001f${operationId}`)}`;\n}\n",
          'exchange use key')

          exchange_helpers = r'''
function requireLiveParticipantAuthority(ctx: any, fieldRef: string, participantRef: string, roles?: string[]): any {
  const grant = ctx.db.fieldAuthority.authorityKey.find(authorityKey(fieldRef, participantRef));
  if (!grant || grant.revoked) fail(`Participant ${participantRef} has no live field authority`);
  if (grant.expiresAtMicros !== 0n && nowMicros(ctx) >= grant.expiresAtMicros) {
    fail(`Authority for Participant ${participantRef} is expired`);
  }
  if (roles && !roles.includes(grant.role)) fail(`Participant ${participantRef} lacks required role`);
  return grant;
}

function identityStillOwnsParticipant(ctx: any, fieldRef: string, participantRef: string, identity: any): boolean {
  const key = authorityKey(fieldRef, participantRef);
  for (const row of ctx.db.fieldAuthority.actorIdentity.filter(identity)) {
    if (row.authorityKey === key && !row.revoked && (row.expiresAtMicros === 0n || nowMicros(ctx) < row.expiresAtMicros)) return true;
  }
  return false;
}

function callerIsLiveParticipant(ctx: any, fieldRef: string, participantRef: string): boolean {
  const key = authorityKey(fieldRef, participantRef);
  for (const row of ctx.db.fieldAuthority.actorIdentity.filter(ctx.sender)) {
    if (row.authorityKey === key && !row.revoked && (row.expiresAtMicros === 0n || nowMicros(ctx) < row.expiresAtMicros)) return true;
  }
  return false;
}

function validateExchangeScope(scopeJson: string): void {
  if (utf8Bytes(scopeJson) > EXCHANGE_MAX_SCOPE_BYTES) fail(`Exchange scope exceeds ${EXCHANGE_MAX_SCOPE_BYTES} byte limit`);
  const scope = requireJsonObject(scopeJson, 'Exchange scope', EXCHANGE_MAX_SCOPE_BYTES);
  enforceJsonBounds(scope, 'Exchange scope');
}

function validateExchangeModes(modesJson: string): string[] {
  const modes = requireJsonStringArray(modesJson, 'Exchange modes', 8);
  if (modes.length === 0) fail('Exchange modes must contain at least one mode');
  for (const mode of modes) if (!EXCHANGE_MODES.has(mode)) fail(`Unsupported Exchange mode: ${mode}`);
  return modes;
}

function requireExchangeProtocol(protocol: string): void {
  if (!EXCHANGE_PROTOCOLS.has(protocol)) fail(`Unsupported Exchange protocol: ${protocol}`);
}

function requireExchangePolicyClear(ctx: any, fieldRef: string, initiatorRef: string, counterpartyRef: string): void {
  for (const [blocker, blocked] of [[initiatorRef, counterpartyRef], [counterpartyRef, initiatorRef]]) {
    const row = ctx.db.contactPolicy.policyKey.find(contactPolicyKey(fieldRef, blocker, blocked));
    if (row && (row.mode === 'blocked' || row.mode === 'muted')) {
      fail(`Exchange relation is ${row.mode} by ${blocker}`);
    }
  }
}

function endExchangeGrant(ctx: any, grant: any, state: string): void {
  if (grant.state !== 'active') return;
  ctx.db.exchangeGrant.grantRef.update({ ...grant, state, endedAtMicros: nowMicros(ctx) });
}

function terminateExchangeForParticipant(ctx: any, fieldRef: string, participantRef: string, state = 'revoked'): void {
  const seen = new Set<string>();
  for (const row of ctx.db.exchangeGrant.initiatorParticipantRef.filter(participantRef)) {
    if (row.fieldRef === fieldRef && !seen.has(row.grantRef)) { endExchangeGrant(ctx, row, state); seen.add(row.grantRef); }
  }
  for (const row of ctx.db.exchangeGrant.counterpartyParticipantRef.filter(participantRef)) {
    if (row.fieldRef === fieldRef && !seen.has(row.grantRef)) { endExchangeGrant(ctx, row, state); seen.add(row.grantRef); }
  }
}

function terminateExchangeBetween(ctx: any, fieldRef: string, a: string, b: string): void {
  for (const row of ctx.db.exchangeGrant.initiatorParticipantRef.filter(a)) {
    if (row.fieldRef === fieldRef && row.counterpartyParticipantRef === b) endExchangeGrant(ctx, row, 'revoked');
  }
  for (const row of ctx.db.exchangeGrant.initiatorParticipantRef.filter(b)) {
    if (row.fieldRef === fieldRef && row.counterpartyParticipantRef === a) endExchangeGrant(ctx, row, 'revoked');
  }
}

'''
          s = replace_once(s, "function validateHostedFieldVisibility(visibility: string): void {\n", exchange_helpers + "function validateHostedFieldVisibility(visibility: string): void {\n", 'exchange helpers')

          # Add optional exchange ancestry to generic Phase-2 ingress provenance.
          s = replace_once(s,
          "  transportMessageId: string;\n  contractJson: string;\n}): void {\n",
          "  transportMessageId: string;\n  contractJson: string;\n  exchangeGrantRef?: string;\n  exchangeOperationId?: string;\n}): void {\n",
          'ingress exchange ancestry type')
          s = replace_once(s,
          "      contributor_participant_ref: input.contributorParticipantRef,\n      received_at_micros: String(now),\n",
          "      contributor_participant_ref: input.contributorParticipantRef,\n      ...(input.exchangeGrantRef ? { exchange_grant_ref: input.exchangeGrantRef } : {}),\n      ...(input.exchangeOperationId ? { exchange_operation_id: input.exchangeOperationId } : {}),\n      received_at_micros: String(now),\n",
          'ingress exchange provenance')

          # Participant authority revocation also terminates outstanding Exchange authority.
          s = replace_once(s,
          "    if (!existing) fail(`No authority grant for Participant ${args.participantRef}`);\n    ctx.db.fieldAuthority.authorityKey.delete(key);\n",
          "    if (!existing) fail(`No authority grant for Participant ${args.participantRef}`);\n    terminateExchangeForParticipant(ctx, args.fieldRef, args.participantRef, 'revoked');\n    ctx.db.fieldAuthority.authorityKey.delete(key);\n",
          'authority revocation terminates exchange')

          reducers = r'''

export const request_exchange = spacetimedb.reducer(
  {
    requestRef: t.string(),
    fieldRef: t.string(),
    initiatorParticipantRef: t.string(),
    counterpartyParticipantRef: t.string(),
    purpose: t.string(),
    scopeJson: t.string(),
    protocol: t.string(),
    bindingRef: t.string(),
    bindingRevision: t.u32(),
    modesJson: t.string(),
    maxUses: t.u32(),
    ttlSeconds: t.u32(),
  },
  (ctx, args) => {
    requireString(args.requestRef, 'Exchange requestRef', 512);
    requireString(args.purpose, 'Exchange purpose', 500);
    requireString(args.bindingRef, 'Exchange bindingRef', 512);
    if (args.bindingRevision < 1) fail('Exchange bindingRevision must be positive');
    if (args.maxUses < 1 || args.maxUses > EXCHANGE_MAX_USES) fail(`Exchange maxUses must be between 1 and ${EXCHANGE_MAX_USES}`);
    if (args.ttlSeconds < 1 || args.ttlSeconds > EXCHANGE_MAX_TTL_SECONDS) fail(`Exchange ttlSeconds must be between 1 and ${EXCHANGE_MAX_TTL_SECONDS}`);
    requireExchangeProtocol(args.protocol);
    validateExchangeScope(args.scopeJson);
    validateExchangeModes(args.modesJson);
    if (args.initiatorParticipantRef === args.counterpartyParticipantRef) fail('Exchange requires distinct Participants');
    requireParticipantInField(ctx, args.initiatorParticipantRef, args.fieldRef);
    requireParticipantInField(ctx, args.counterpartyParticipantRef, args.fieldRef);
    requireParticipantAuthority(ctx, args.fieldRef, args.initiatorParticipantRef, ['contact', 'contributor']);
    requireExchangePolicyClear(ctx, args.fieldRef, args.initiatorParticipantRef, args.counterpartyParticipantRef);

    const existing = ctx.db.exchangeRequest.requestRef.find(args.requestRef);
    if (existing) {
      const exact = existing.fieldRef === args.fieldRef
        && existing.initiatorParticipantRef === args.initiatorParticipantRef
        && existing.counterpartyParticipantRef === args.counterpartyParticipantRef
        && existing.purpose === args.purpose
        && existing.scopeJson === args.scopeJson
        && existing.protocol === args.protocol
        && existing.bindingRef === args.bindingRef
        && existing.bindingRevision === args.bindingRevision
        && existing.modesJson === args.modesJson
        && existing.requestedMaxUses === args.maxUses
        && existing.requestedTtlSeconds === args.ttlSeconds;
      if (exact) return;
      fail('Exchange request ref conflicts with a different request');
    }

    let outstanding = 0;
    for (const row of ctx.db.exchangeRequest.initiatorParticipantRef.filter(args.initiatorParticipantRef)) {
      if (row.fieldRef === args.fieldRef && row.state === 'pending' && nowMicros(ctx) < row.requestExpiresAtMicros) outstanding += 1;
    }
    if (outstanding >= EXCHANGE_MAX_OUTSTANDING_REQUESTS) fail('Exchange outstanding request limit exceeded');

    const now = nowMicros(ctx);
    ctx.db.exchangeRequest.insert({
      requestRef: args.requestRef,
      fieldRef: args.fieldRef,
      initiatorParticipantRef: args.initiatorParticipantRef,
      initiatorActorIdentity: ctx.sender,
      counterpartyParticipantRef: args.counterpartyParticipantRef,
      purpose: args.purpose,
      scopeJson: args.scopeJson,
      protocol: args.protocol,
      bindingRef: args.bindingRef,
      bindingRevision: args.bindingRevision,
      modesJson: args.modesJson,
      requestedMaxUses: args.maxUses,
      requestedTtlSeconds: args.ttlSeconds,
      state: 'pending',
      createdAtMicros: now,
      requestExpiresAtMicros: now + BigInt(args.ttlSeconds) * 1_000_000n,
      decisionRef: '',
    });
  }
);

export const grant_exchange = spacetimedb.reducer(
  { requestRef: t.string(), grantRef: t.string(), reason: t.string(), evidenceJson: t.string() },
  (ctx, args) => {
    requireString(args.grantRef, 'Exchange grantRef', 512);
    requireString(args.reason, 'Exchange grant reason', 1_000);
    requireJsonObject(args.evidenceJson, 'Exchange grant evidence', 8_192);
    const request = ctx.db.exchangeRequest.requestRef.find(args.requestRef);
    if (!request) fail(`Unknown Exchange request: ${args.requestRef}`);
    requireFieldOwner(ctx, request.fieldRef);
    if (request.state !== 'pending') fail(`Exchange request is already ${request.state}`);
    if (nowMicros(ctx) >= request.requestExpiresAtMicros) fail('Exchange request has expired');
    requireExchangePolicyClear(ctx, request.fieldRef, request.initiatorParticipantRef, request.counterpartyParticipantRef);
    requireParticipantInField(ctx, request.counterpartyParticipantRef, request.fieldRef);
    const initiatorAuthority = requireLiveParticipantAuthority(ctx, request.fieldRef, request.initiatorParticipantRef, ['contact', 'contributor']);
    if (!identityStillOwnsParticipant(ctx, request.fieldRef, request.initiatorParticipantRef, request.initiatorActorIdentity)) {
      fail('Exchange request actor no longer owns the initiating Participant');
    }
    if (ctx.db.exchangeGrant.grantRef.find(args.grantRef)) fail(`Exchange grant ref already exists: ${args.grantRef}`);
    if (ctx.db.exchangeGrant.requestRef.find(args.requestRef)) fail('Exchange request already has a grant');
    const now = nowMicros(ctx);
    const decisionRef = `exchange-decision:${fingerprintString(`${request.requestRef}\u001f${args.grantRef}\u001f${String(now)}`)}`;
    ctx.db.exchangeGrant.insert({
      grantRef: args.grantRef,
      requestRef: request.requestRef,
      fieldRef: request.fieldRef,
      initiatorParticipantRef: request.initiatorParticipantRef,
      initiatorActorIdentity: initiatorAuthority.actorIdentity,
      counterpartyParticipantRef: request.counterpartyParticipantRef,
      purpose: request.purpose,
      scopeJson: request.scopeJson,
      protocol: request.protocol,
      bindingRef: request.bindingRef,
      bindingRevision: request.bindingRevision,
      modesJson: request.modesJson,
      maxUses: request.requestedMaxUses,
      usedUses: 0,
      state: 'active',
      grantedAtMicros: now,
      expiresAtMicros: now + BigInt(request.requestedTtlSeconds) * 1_000_000n,
      endedAtMicros: 0n,
      grantorIdentity: ctx.sender,
      decisionRef,
      provenanceJson: JSON.stringify({ schema: 'oi.exchange-decision/v1', decision_ref: decisionRef, request_ref: request.requestRef, grant_ref: args.grantRef, decided_at_micros: String(now), reason: args.reason, evidence: JSON.parse(args.evidenceJson) }),
    });
    ctx.db.exchangeRequest.requestRef.update({ ...request, state: 'granted', decisionRef });
  }
);

export const consume_exchange = spacetimedb.reducer(
  {
    grantRef: t.string(), operationId: t.string(), fieldRef: t.string(),
    initiatorParticipantRef: t.string(), counterpartyParticipantRef: t.string(),
    protocol: t.string(), bindingRef: t.string(), bindingRevision: t.u32(),
    mode: t.string(), purpose: t.string(), scopeJson: t.string(),
  },
  (ctx, args) => {
    requireString(args.operationId, 'Exchange operationId', 256);
    requireString(args.bindingRef, 'Exchange bindingRef', 512);
    requireString(args.purpose, 'Exchange purpose', 500);
    requireExchangeProtocol(args.protocol);
    validateExchangeScope(args.scopeJson);
    if (!EXCHANGE_MODES.has(args.mode)) fail(`Unsupported Exchange mode: ${args.mode}`);
    const grant = ctx.db.exchangeGrant.grantRef.find(args.grantRef);
    if (!grant) fail(`Unknown Exchange grant: ${args.grantRef}`);
    if (grant.fieldRef !== args.fieldRef) fail('Exchange grant crosses SharedField boundary');
    if (grant.initiatorParticipantRef !== args.initiatorParticipantRef) fail('Exchange initiating Participant does not match grant');
    if (grant.counterpartyParticipantRef !== args.counterpartyParticipantRef) fail('Exchange counterparty does not match grant');
    if (grant.protocol !== args.protocol) fail('Exchange protocol does not match grant');
    if (grant.bindingRef !== args.bindingRef || grant.bindingRevision !== args.bindingRevision) fail('Exchange endpoint/binding lineage does not match grant');
    if (grant.purpose !== args.purpose) fail('Exchange purpose does not match grant');
    if (grant.scopeJson !== args.scopeJson) fail('Exchange scope does not match grant');
    if (!validateExchangeModes(grant.modesJson).includes(args.mode)) fail('Exchange mode is outside grant scope');
    if (grant.state === 'revoked' || grant.state === 'completed') fail(`Exchange grant is ${grant.state}`);
    if (nowMicros(ctx) >= grant.expiresAtMicros) fail('Exchange grant has expired');
    requireExchangePolicyClear(ctx, grant.fieldRef, grant.initiatorParticipantRef, grant.counterpartyParticipantRef);
    requireParticipantAuthority(ctx, grant.fieldRef, grant.initiatorParticipantRef, ['contact', 'contributor']);
    let boundToCaller = false;
    for (const row of ctx.db.exchangeGrant.initiatorActorIdentity.filter(ctx.sender)) {
      if (row.grantRef === grant.grantRef) { boundToCaller = true; break; }
    }
    if (!boundToCaller) fail('Runtime caller is not the actor bound by the Exchange grant');

    const useKey = exchangeUseKey(grant.grantRef, args.operationId);
    const demandFingerprint = fingerprintString(JSON.stringify({
      fieldRef: args.fieldRef, initiator: args.initiatorParticipantRef, counterparty: args.counterpartyParticipantRef,
      protocol: args.protocol, bindingRef: args.bindingRef, bindingRevision: args.bindingRevision,
      mode: args.mode, purpose: args.purpose, scopeJson: args.scopeJson,
    }));
    const existingUse = ctx.db.exchangeUse.useKey.find(useKey);
    if (existingUse) {
      if (existingUse.demandFingerprint !== demandFingerprint) fail('Exchange operation replay key conflicts with a different demand');
      return;
    }
    if (grant.state !== 'active') fail(`Exchange grant is ${grant.state}`);
    if (grant.usedUses >= grant.maxUses) fail('Exchange grant use budget is exhausted');
    const now = nowMicros(ctx);
    ctx.db.exchangeUse.insert({
      useKey, grantRef: grant.grantRef, operationId: args.operationId, fieldRef: grant.fieldRef,
      actorIdentity: ctx.sender, counterpartyParticipantRef: grant.counterpartyParticipantRef,
      protocol: args.protocol, bindingRef: args.bindingRef, bindingRevision: args.bindingRevision,
      mode: args.mode, purpose: args.purpose, scopeJson: args.scopeJson,
      usedAtMicros: now, demandFingerprint,
    });
    const usedUses = grant.usedUses + 1;
    ctx.db.exchangeGrant.grantRef.update({
      ...grant,
      usedUses,
      state: usedUses >= grant.maxUses ? 'exhausted' : 'active',
      endedAtMicros: usedUses >= grant.maxUses ? now : grant.endedAtMicros,
    });
  }
);

export const revoke_exchange = spacetimedb.reducer(
  { grantRef: t.string(), reason: t.string() },
  (ctx, args) => {
    requireString(args.reason, 'Exchange revocation reason', 1_000);
    const grant = ctx.db.exchangeGrant.grantRef.find(args.grantRef);
    if (!grant) fail(`Unknown Exchange grant: ${args.grantRef}`);
    if (!isFieldOwner(ctx, grant.fieldRef)
        && !callerIsLiveParticipant(ctx, grant.fieldRef, grant.initiatorParticipantRef)
        && !callerIsLiveParticipant(ctx, grant.fieldRef, grant.counterpartyParticipantRef)) {
      fail('Caller cannot revoke this Exchange grant');
    }
    if (grant.state === 'revoked') return;
    if (grant.state === 'completed') fail('Completed Exchange grant cannot be revoked');
    ctx.db.exchangeGrant.grantRef.update({ ...grant, state: 'revoked', endedAtMicros: nowMicros(ctx) });
  }
);

export const complete_exchange = spacetimedb.reducer(
  { grantRef: t.string() },
  (ctx, args) => {
    const grant = ctx.db.exchangeGrant.grantRef.find(args.grantRef);
    if (!grant) fail(`Unknown Exchange grant: ${args.grantRef}`);
    if (!isFieldOwner(ctx, grant.fieldRef)
        && !callerIsLiveParticipant(ctx, grant.fieldRef, grant.initiatorParticipantRef)
        && !callerIsLiveParticipant(ctx, grant.fieldRef, grant.counterpartyParticipantRef)) {
      fail('Caller cannot complete this Exchange grant');
    }
    if (grant.state === 'completed') return;
    if (grant.state === 'revoked') fail('Revoked Exchange grant cannot be completed');
    ctx.db.exchangeGrant.grantRef.update({ ...grant, state: 'completed', endedAtMicros: nowMicros(ctx) });
  }
);

export const ingest_authorized_exchange_contribution = spacetimedb.reducer(
  {
    grantRef: t.string(), operationId: t.string(), fieldRef: t.string(),
    contributorParticipantRef: t.string(), sourceKind: t.string(), transportProvider: t.string(),
    transportMessageId: t.string(), contractJson: t.string(),
  },
  (ctx, args) => {
    requireFieldOwner(ctx, args.fieldRef);
    const grant = ctx.db.exchangeGrant.grantRef.find(args.grantRef);
    if (!grant || grant.fieldRef !== args.fieldRef) fail('Returned material does not reference a valid Exchange grant in this SharedField');
    if (grant.counterpartyParticipantRef !== args.contributorParticipantRef) fail('Returned material contributor does not match Exchange counterparty');
    if (grant.protocol !== args.sourceKind) fail('Returned material source kind does not match Exchange protocol');
    const use = ctx.db.exchangeUse.useKey.find(exchangeUseKey(args.grantRef, args.operationId));
    if (!use || use.grantRef !== args.grantRef || use.operationId !== args.operationId) fail('Returned material has no matching consumed Exchange operation');
    if (!CONTRIBUTION_TRANSPORTS.has(args.sourceKind)) fail(`Unsupported mediated Contribution source kind: ${args.sourceKind}`);
    ingressContribution(ctx, {
      fieldRef: args.fieldRef,
      contributorParticipantRef: args.contributorParticipantRef,
      sourceKind: args.sourceKind,
      transportProvider: args.transportProvider,
      transportMessageId: args.transportMessageId,
      contractJson: args.contractJson,
      exchangeGrantRef: args.grantRef,
      exchangeOperationId: args.operationId,
    });
  }
);
'''
          s = replace_once(s, "\nexport const admit_contribution = spacetimedb.reducer(\n", reducers + "\nexport const admit_contribution = spacetimedb.reducer(\n", 'exchange reducers')

          # Contact block/mute immediately terminates already-active exchange grants.
          s = replace_once(s,
          "    if (existing) ctx.db.contactPolicy.policyKey.update(row);\n    else ctx.db.contactPolicy.insert(row);\n  }\n);\n",
          "    if (existing) ctx.db.contactPolicy.policyKey.update(row);\n    else ctx.db.contactPolicy.insert(row);\n    terminateExchangeBetween(ctx, args.fieldRef, args.blockerParticipantRef, args.blockedParticipantRef);\n  }\n);\n",
          'contact policy terminates exchange')

          write(path, s)

          # ------------------------------------------------------------------
          # A2A privileged controller: authority consumption occurs before *any* fetch.
          # ------------------------------------------------------------------
          path = 'shared-field/a2a.mjs'
          s = read(path)
          s = replace_once(s,
          "const AVAILABILITY_STATES = new Set(['online', 'degraded', 'offline', 'withdrawn']);\n",
          "const AVAILABILITY_STATES = new Set(['online', 'degraded', 'offline', 'withdrawn']);\n"
          "const A2A_AGENT_CARD_MAX_BYTES = 64 * 1024;\n"
          "const A2A_RESPONSE_MAX_BYTES = 1024 * 1024;\n"
          "const A2A_MESSAGE_MAX_BYTES = 32 * 1024;\n"
          "const A2A_FETCH_TIMEOUT_MS = 15_000;\n"
          "const A2A_MAX_JSON_DEPTH = 12;\n"
          "const A2A_MAX_JSON_COLLECTION = 256;\n",
          'A2A bounds constants')

          a2a_helpers = r'''
function utf8Bytes(value) {
  return new TextEncoder().encode(value).byteLength;
}

function boundedJsonShape(value, name, depth = 0) {
  if (depth > A2A_MAX_JSON_DEPTH) throw new TypeError(`${name} exceeds maximum JSON depth ${A2A_MAX_JSON_DEPTH}`);
  if (value === null || typeof value === 'boolean' || typeof value === 'number') return;
  if (typeof value === 'string') {
    if (utf8Bytes(value) > A2A_RESPONSE_MAX_BYTES) throw new TypeError(`${name} contains an oversized string`);
    return;
  }
  if (Array.isArray(value)) {
    if (value.length > A2A_MAX_JSON_COLLECTION) throw new TypeError(`${name} exceeds ${A2A_MAX_JSON_COLLECTION} array entries`);
    value.forEach((entry, index) => boundedJsonShape(entry, `${name}[${index}]`, depth + 1));
    return;
  }
  if (typeof value !== 'object') throw new TypeError(`${name} contains an unsupported JSON value`);
  const entries = Object.entries(value);
  if (entries.length > A2A_MAX_JSON_COLLECTION) throw new TypeError(`${name} exceeds ${A2A_MAX_JSON_COLLECTION} object properties`);
  entries.forEach(([key, entry]) => boundedJsonShape(entry, `${name}.${key}`, depth + 1));
}

async function boundedJsonResponse(response, name, maxBytes) {
  if (!response?.ok) throw new Error(`${name} failed: ${response?.status ?? 'unknown'}`);
  const declared = Number(response.headers?.get?.('content-length') ?? 0);
  if (Number.isFinite(declared) && declared > maxBytes) throw new Error(`${name} exceeds ${maxBytes} byte limit`);
  let text;
  if (typeof response.text === 'function') text = await response.text();
  else if (typeof response.json === 'function') text = JSON.stringify(await response.json());
  else throw new Error(`${name} did not expose a readable body`);
  if (utf8Bytes(text) > maxBytes) throw new Error(`${name} exceeds ${maxBytes} byte limit`);
  let parsed;
  try { parsed = JSON.parse(text); } catch { throw new Error(`${name} returned invalid JSON`); }
  boundedJsonShape(parsed, name);
  return parsed;
}

async function boundedFetch(fetchImpl, url, options, name, maxBytes) {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), A2A_FETCH_TIMEOUT_MS);
  try {
    const response = await fetchImpl(url, { ...options, redirect: 'error', signal: controller.signal });
    return await boundedJsonResponse(response, name, maxBytes);
  } finally {
    clearTimeout(timer);
  }
}

'''
          s = replace_once(s, "function publicUrl(value, name) {\n", a2a_helpers + "function publicUrl(value, name) {\n", 'A2A bounds helpers')

          start = s.index('export async function performA2aExchange(')
          end = s.index('\nexport function validateA2aDifference', start)
          new_perform = r'''export async function performA2aExchange({
  binding,
  presence,
  initiator_participant_ref,
  message,
  authorize_exchange,
  fetch_impl = globalThis.fetch,
  authorization_headers = {},
}) {
  const currentBinding = validateA2aBinding(binding);
  const currentPresence = validateA2aPresence(presence);
  string(initiator_participant_ref, 'initiator_participant_ref');
  record(message, 'A2A message');
  string(message.message_id, 'A2A message.message_id');
  string(message.text, 'A2A message.text');
  if (utf8Bytes(message.text) > A2A_MESSAGE_MAX_BYTES) throw new TypeError(`A2A message.text exceeds ${A2A_MESSAGE_MAX_BYTES} byte limit`);
  if (typeof fetch_impl !== 'function') throw new TypeError('fetch_impl must be a function');
  if (typeof authorize_exchange !== 'function') throw new TypeError('explicit authorize_exchange resolver is required before A2A network I/O');
  if (currentBinding.state !== 'published') throw new TypeError('A2A exchange requires an explicitly published binding');
  if (currentPresence.binding_ref !== currentBinding.binding_ref || currentPresence.participant_ref !== currentBinding.participant_ref) {
    throw new TypeError('A2A presence must belong to the selected binding/Participant');
  }
  if (!['online', 'degraded'].includes(currentPresence.availability)) throw new TypeError(`A2A endpoint is not currently reachable: ${currentPresence.availability}`);

  const operationId = string(message.exchange_operation_id ?? message.message_id, 'A2A exchange operation id');
  const scopeJson = JSON.stringify(message.scope ?? { kind: 'message' });
  const authority = await authorize_exchange({
    field_ref: currentBinding.field_ref,
    initiator_participant_ref,
    counterparty_participant_ref: currentBinding.participant_ref,
    protocol: 'a2a',
    protocol_version: currentBinding.protocol_version,
    protocol_binding: currentBinding.protocol_binding,
    mode: 'message:send',
    binding_ref: currentBinding.binding_ref,
    binding_revision: currentBinding.binding_revision,
    operation_id: operationId,
    purpose: message.purpose ?? 'a2a-message-exchange',
    scope_json: scopeJson,
  });
  record(authority, 'A2A exchange authority result');
  if (authority.allowed !== true) throw new Error('A2A exchange denied by explicit Exchange authority');
  const grantRef = string(authority.grant_ref, 'A2A exchange authority grant_ref');

  const card = await boundedFetch(fetch_impl, currentBinding.agent_card_url,
    { headers: { Accept: 'application/json', ...authorization_headers } },
    'A2A Agent Card fetch', A2A_AGENT_CARD_MAX_BYTES);
  const selectedInterface = assertA2aCard(card, currentBinding);
  const request = {
    ...(selectedInterface.tenant ? { tenant: selectedInterface.tenant } : {}),
    message: { messageId: message.message_id, role: 'ROLE_USER', parts: [{ text: message.text }] },
  };
  const payload = await boundedFetch(fetch_impl, a2aSendUrl(selectedInterface.url, selectedInterface.tenant), {
    method: 'POST',
    headers: { 'Content-Type': 'application/a2a+json', Accept: 'application/a2a+json', 'A2A-Version': currentBinding.protocol_version, ...authorization_headers },
    body: JSON.stringify(request),
  }, 'A2A message exchange', A2A_RESPONSE_MAX_BYTES);
  const result = transportRef(payload);

  return {
    schema: A2A_DIFFERENCE_SCHEMA,
    exchange_ref: message.exchange_ref ?? `a2a-exchange:${message.message_id}`,
    field_ref: currentBinding.field_ref,
    initiator_participant_ref,
    recipient_participant_ref: currentBinding.participant_ref,
    agent_ref: currentBinding.agent_ref,
    binding_ref: currentBinding.binding_ref,
    binding_revision: currentBinding.binding_revision,
    request_message_id: message.message_id,
    exchange_authority: { grant_ref: grantRef, operation_id: operationId },
    transport_result: { ...result, payload: clone(payload) },
    admission: 'pending',
    transport_provenance: {
      protocol: 'A2A', protocol_version: currentBinding.protocol_version, protocol_binding: currentBinding.protocol_binding,
      agent_card: { name: card.name, version: card.version, url: currentBinding.agent_card_url },
    },
  };
}
'''
          s = s[:start] + new_perform + s[end:]
          s = replace_once(s,
          "  string(value.request_message_id, 'A2A difference.request_message_id');\n  record(value.transport_result, 'A2A difference.transport_result');\n",
          "  string(value.request_message_id, 'A2A difference.request_message_id');\n"
          "  record(value.exchange_authority, 'A2A difference.exchange_authority');\n"
          "  string(value.exchange_authority.grant_ref, 'A2A difference.exchange_authority.grant_ref');\n"
          "  string(value.exchange_authority.operation_id, 'A2A difference.exchange_authority.operation_id');\n"
          "  record(value.transport_result, 'A2A difference.transport_result');\n",
          'A2A difference exchange lineage')
          s = replace_once(s,
          "      binding_ref: value.binding_ref,\n      transport_kind: value.transport_result.kind,\n",
          "      binding_ref: value.binding_ref,\n      exchange_grant_ref: value.exchange_authority.grant_ref,\n      exchange_operation_id: value.exchange_authority.operation_id,\n      transport_kind: value.transport_result.kind,\n",
          'A2A encounter exchange lineage')
          s = replace_once(s,
          "      exchange_ref: value.exchange_ref,\n      binding_ref: value.binding_ref,\n",
          "      exchange_ref: value.exchange_ref,\n      exchange_grant_ref: value.exchange_authority.grant_ref,\n      exchange_operation_id: value.exchange_authority.operation_id,\n      binding_ref: value.binding_ref,\n",
          'A2A ingress exchange lineage')
          write(path, s)

          # Existing #37 tests must now pass explicit grant consumption; the new dedicated
          # Phase-3 tests prove denial/lineage/bounds in more detail.
          path = 'shared-field/a2a.test.mjs'
          s = read(path)
          s = replace_once(s,
          "const LOCAL_PARTICIPANT = 'participant:local:canonical';\n",
          "const LOCAL_PARTICIPANT = 'participant:local:canonical';\n"
          "const ALLOW_EXCHANGE = async (demand) => ({ allowed: true, grant_ref: `exchange-grant:test:${demand.operation_id}` });\n",
          'A2A test authority helper')
          pattern = re.compile(r'(?P<indent>\s*)initiator_participant_ref: LOCAL_PARTICIPANT,\n(?P=indent)message:')
          s, n = pattern.subn(lambda m: f"{m.group('indent')}initiator_participant_ref: LOCAL_PARTICIPANT,\n{m.group('indent')}authorize_exchange: ALLOW_EXCHANGE,\n{m.group('indent')}message:", s)
          if n < 3: raise SystemExit(f'A2A test authority injection: expected >=3 calls, found {n}')
          s = replace_once(s,
          "    request_message_id: 'a2a-message:encounter-1',\n    transport_result:",
          "    request_message_id: 'a2a-message:encounter-1',\n    exchange_authority: { grant_ref: 'exchange-grant:encounter', operation_id: 'operation:encounter' },\n    transport_result:",
          'manual A2A difference authority')
          write(path, s)

          # Dedicated portable Phase-3 test expected demand now includes exact scope.
          path = 'shared-field/exchange-authority.test.mjs'
          s = read(path)
          s = replace_once(s,
          "    operation_id: 'operation:phase3:portable', purpose: 'portable-phase3-proof',\n",
          "    operation_id: 'operation:phase3:portable', purpose: 'portable-phase3-proof', scope_json: JSON.stringify({ kind: 'message' }),\n",
          'portable A2A scope demand')
          write(path, s)

          # ------------------------------------------------------------------
          # Live A2A adapter: real SpaceTimeDB grant consumption before network, then
          # generic Phase-2 quarantine linked to that consumed operation.
          # ------------------------------------------------------------------
          path = 'shared-field/spacetimedb/a2a-phase2-ingress-live-acceptance.ts'
          s = read(path)
          s = replace_once(s,
          "const BINDING_REF = 'a2a-binding:p2-remote';\n",
          "const BINDING_REF = 'a2a-binding:p2-remote';\n"
          "const EXCHANGE_REQUEST_REF = 'exchange-request:a2a:p3-live';\n"
          "const EXCHANGE_GRANT_REF = 'exchange-grant:a2a:p3-live';\n"
          "const EXCHANGE_OPERATION_ID = 'exchange-operation:a2a:p3-live';\n",
          'live A2A exchange refs')
          s = replace_once(s,
          "      'SELECT * FROM my_field_authority',\n",
          "      'SELECT * FROM my_field_authority',\n      'SELECT * FROM my_contribution_receipt',\n      'SELECT * FROM contribution',\n",
          'live A2A receipt subscriptions')
          marker = "  const difference = await performA2aExchange({\n"
          grant_setup = r'''  await owner.conn.reducers.requestExchange({
    requestRef: EXCHANGE_REQUEST_REF,
    fieldRef: FIELD_REF,
    initiatorParticipantRef: OWNER_PARTICIPANT_REF,
    counterpartyParticipantRef: AGENT_PARTICIPANT_REF,
    purpose: 'a2a-message-exchange',
    scopeJson: JSON.stringify({ kind: 'message' }),
    protocol: 'a2a',
    bindingRef: BINDING_REF,
    bindingRevision: 1,
    modesJson: JSON.stringify(['message:send']),
    maxUses: 1,
    ttlSeconds: 300,
  });
  await owner.conn.reducers.grantExchange({
    requestRef: EXCHANGE_REQUEST_REF,
    grantRef: EXCHANGE_GRANT_REF,
    reason: 'Phase 3 live A2A exchange grant',
    evidenceJson: JSON.stringify({ fixture: 'a2a-phase3-live' }),
  });

'''
          s = replace_once(s, marker, grant_setup + marker, 'live A2A grant setup')
          s = replace_once(s,
          "    initiator_participant_ref: OWNER_PARTICIPANT_REF,\n    message: { exchange_ref: 'a2a-exchange:p2', message_id: 'a2a-message:p2', text: 'Return untrusted material.' },\n",
          "    initiator_participant_ref: OWNER_PARTICIPANT_REF,\n"
          "    authorize_exchange: async (demand: any) => {\n"
          "      await owner.conn.reducers.consumeExchange({\n"
          "        grantRef: EXCHANGE_GRANT_REF, operationId: demand.operation_id, fieldRef: demand.field_ref,\n"
          "        initiatorParticipantRef: demand.initiator_participant_ref, counterpartyParticipantRef: demand.counterparty_participant_ref,\n"
          "        protocol: demand.protocol, bindingRef: demand.binding_ref, bindingRevision: demand.binding_revision,\n"
          "        mode: demand.mode, purpose: demand.purpose, scopeJson: demand.scope_json,\n"
          "      });\n"
          "      return { allowed: true, grant_ref: EXCHANGE_GRANT_REF };\n"
          "    },\n"
          "    message: { exchange_ref: 'a2a-exchange:p2', exchange_operation_id: EXCHANGE_OPERATION_ID, message_id: 'a2a-message:p2', text: 'Return untrusted material.' },\n",
          'live A2A authority resolver')

          old_tail = "  assert.equal([...owner.conn.db.projection.iter()].length, projectionsBefore, 'transport/ingress preparation must not mint Projection');\n  assert.equal(fixture.requests.length, 1);\n"
          new_tail = r'''  assert.equal([...owner.conn.db.projection.iter()].length, projectionsBefore, 'transport/ingress preparation must not mint Projection');
  assert.equal(fixture.requests.length, 1);
  assert.equal(difference.exchange_authority.grant_ref, EXCHANGE_GRANT_REF);
  assert.equal(ingress.transport_provenance.exchange_operation_id, EXCHANGE_OPERATION_ID);

  await owner.conn.reducers.ingestAuthorizedExchangeContribution({
    grantRef: EXCHANGE_GRANT_REF,
    operationId: EXCHANGE_OPERATION_ID,
    fieldRef: FIELD_REF,
    contributorParticipantRef: AGENT_PARTICIPANT_REF,
    sourceKind: ingress.source_kind,
    transportProvider: ingress.transport_provider,
    transportMessageId: ingress.transport_message_id,
    contractJson: JSON.stringify(ingress.contribution),
  });
  await waitUntil(() => [...owner.conn.db.myContributionReceipt.iter()].some((row: any) => row.contributionRef === ingress.contribution.contribution_ref && row.state === 'quarantined'), 'A2A returned material quarantine');
  assert.equal([...owner.conn.db.contribution.iter()].some((row: any) => row.contributionRef === ingress.contribution.contribution_ref), false, 'authorised A2A return must still require Admission');
'''
          s = replace_once(s, old_tail, new_tail, 'live A2A quarantine')
          s = s.replace("proof: 'oi-a2a-phase2-ingress-adapter/v1'", "proof: 'oi-a2a-phase3-exchange-authority/v1'")
          s = s.replace("legacy_a2a_admission_bridge: 'disabled',", "exchange_grant_ref: EXCHANGE_GRANT_REF,\n    exchange_operation_id: EXCHANGE_OPERATION_ID,\n    legacy_a2a_admission_bridge: 'disabled',")
          write(path, s)

          # ------------------------------------------------------------------
          # Acceptance chain and shared exports/workflow convergence.
          # ------------------------------------------------------------------
          path = 'shared-field/spacetimedb/security-live-acceptance-v4-bootstrap.ts'
          s = read(path)
          if "security-live-acceptance-v6.ts" not in s:
              s += "await import('./security-live-acceptance-v6.ts');\n"
          write(path, s)

          path = 'shared-field/spacetimedb/package.json'
          s = read(path)
          # Phase-2 branch has the desired v4->v5 bootstrap; A2A is the converged #51 test now upgraded to Phase 3.
          data = __import__('json').loads(s)
          data['scripts']['acceptance'] = 'tsx security-live-acceptance-v4-bootstrap.ts'
          data['scripts']['acceptance:a2a'] = 'tsx a2a-phase2-ingress-live-acceptance.ts'
          write(path, __import__('json').dumps(data, indent=2) + '\n')

          path = 'shared-field/api.mjs'
          s = read(path)
          for export in ["export * from './a2a.mjs';", "export * from './a2a-lifecycle.mjs';", "export * from './a2a-explore.mjs';", "export * from './spacetimedb-a2a.mjs';"]:
              if export not in s:
                  s += export + '\n'
          write(path, s)

          path = '.github/workflows/shared-field.yml'
          s = read(path)
          if 'agent/oi-017-exchange-authority' not in s:
              s = s.replace('      - agent/oi-017-encounter-security\n', '      - agent/oi-017-encounter-security\n      - agent/oi-017-exchange-authority\n')
          s = s.replace('Run shared-field contract, Explore and security tests', 'Run shared-field contract, Explore, A2A and security tests')
          s = s.replace('Prove Phase 1 privacy and Phase 2 Contribution Admission conformance', 'Prove Phase 1 privacy, Phase 2 Admission and Phase 3 Exchange conformance')
          a2a_steps = r'''      - name: Publish isolated live A2A SharedField database
        run: spacetime publish oi-shared-field-a2a-ci --server local --module-path shared-field/spacetimedb --yes=all
      - name: Prove grant-gated A2A exchange returns only to generic quarantine
        env:
          SPACETIMEDB_URI: ws://127.0.0.1:3000
          SPACETIMEDB_A2A_DATABASE: oi-shared-field-a2a-ci
        run: npm --prefix shared-field/spacetimedb run acceptance:a2a
'''
          if 'acceptance:a2a' not in s:
              s = s.replace('      - name: Show SpaceTimeDB log on failure\n', a2a_steps + '      - name: Show SpaceTimeDB log on failure\n')
          write(path, s)

          # Remove this one-shot patch workflow in the implementation commit.
          Path('.github/workflows/phase3-implementation-patch.yml').unlink()
          PY
      - name: Sanity-check patched JavaScript
        run: |
          node --check shared-field/a2a.mjs
          node --check shared-field/a2a.test.mjs
          node --check shared-field/exchange-authority.test.mjs
      - name: Commit Phase 3 implementation
        shell: bash
        run: |
          git config user.name 'Epii'
          git config user.email 'frank.g.taylor97@gmail.com'
          git add -A
          git diff --cached --check
          git commit -m 'Enforce explicit bounded Exchange authority'
          git push origin HEAD:agent/oi-017-exchange-authority
