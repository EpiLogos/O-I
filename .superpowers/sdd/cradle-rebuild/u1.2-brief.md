# U1.2 — Editor surface

Relation: S can open and safely write sources because Central owns source refs,
read/write authority and revisions (S→S0).

Law: “Surface presentation != semantic ownership” — APP-SPEC §5.
Law: “No component copies selection state independently.” — cradle/02 §7.
Law: “Never derive semantic state from presentation state.” — cradle/02 §6.
Law: “forced conflict shows both sides, no data loss” — map §5 U1.3;
preserve that existing behavior while extending the source editor.

Branch cradle-p1 at b1ff50d. Central pinned to 832f2e5, isolated archived-source
build already prepared. U1.1 pre-change suite 123/123 and native walk complete.
No design edits. User requests continued sequential execution through the map.

Files: kernel lib.rs (buffer project route), source_cas.rs (real-owner tests);
source surface and frame bindings/provider (source routing, dirty isolation,
ref-scoped errors); navigator source rows; Rest/Cradle (retain writing while
opening a source); walk editor scenario + provisioned real Central test ground.

Walk: open ten distinct documents through actual Central source operations,
each <1s; match owner refs/content; switch dirty files with no cross-contamination;
save to the originating project even after another project is selected; reopen
retains ref and canonical revision; owner refusal is visible; write nothing on
open; initial writing remains when source surfaces close. Native walk reads and
saves a real O-I source, restores original bytes through Central, and records
screenshots. Real CLI and filesystem throughout, no mocked owner executable.
