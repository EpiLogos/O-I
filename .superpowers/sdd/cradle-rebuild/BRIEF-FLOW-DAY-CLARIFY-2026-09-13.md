# BRIEF — Flow/Day logics clarified for the owner; blank flow placeholders stop — 2026-09-13

Queue cell A (NEXT-SESSION-PROMPT-2026-09-12-2, as amended by #257). The owner
ruled on the PREMISE: `ProjectCentral/now/flows/` placeholders are the fault —
an agent preempted flow files with a structure no design chose. Flows live in
the user's Day through the two supplied carriers. The former
gitignore/commit-law/relocation options are WITHDRAWN.

## Owner contract (authored ground; cite, never re-derive)

> "0/1 `ql-dialogue-flow_1.html` — Flow and Dialogue are views of the same
> document entries; Journal is supported inside this same file." — wayfinder §2.
> "4+2 `ql-daily-die_2.html` — When used as a Day, consume Central's
> source/temporal reading; opening a tab does not create another Day." — §2.
> "Flow opens an entry as a full writing page; Dialogue reads the same entry
> set conversationally … Do not maintain independent editable source copies
> per view." — §3.2.

Design meaning moves by proposal: this cell carries the one-page clarified
position (PROPOSAL-FLOW-DAY-LOGICS-2026-09-13.md, same directory) for the
owner's ratification. Nothing in the owner's ground moves before that.

## Change (the one reversible desktop half — build NOW)

The blank-mint chain is `createFlow` (Cradle.tsx) → kernel `flow_create` →
Central `projectcentral_flow.rs` `DEFAULT_FLOW_DIR` (`now/flows/<stamp>.md`,
registry `.central/flows.json`). Three UI entries mint blanks today:
Start writing (`startWriting`), the navigator's New flow (same route), and the
fresh tab's Write (`freshChoice` kind "flow"). Saving an empty draft places a
blank too (`placeDraft` mints before checking content).

- `desktop/cradle/src/Cradle.tsx` — `startWriting` always opens the retained
  local draft (kind "draft"), carrying the register named before opening as
  the draft's default (the synchronous-carry law, new form). `freshChoice`'s
  flow branch opens a draft in place of the blank surface (no register
  refusal — blank writing is valid; the picker names the register at Save).
  `placeDraft` refuses empty content (nothing to place). `createFlow` survives
  only as the explicit-Save path with real content through Central's own
  installed operations — no kernel changes, no new desktop store.
- `desktop/cradle/src/flow/DraftSurface.tsx` — Save disabled while the
  writing is empty; docstring states the deferral law.
- `desktop/cradle/src/Rest.tsx` — docstring states the new law (no comment
  claims a behaviour the surface no longer has).
- Walks re-contracted: `flow-canvas` (New flow → draft → owner list empty →
  git-clean proof → Save → the Flow exists with the exact content → existing
  assertions continue), `navigator` (draft carries the just-chosen register;
  no ground write; Save places; downstream focus/race assertions unchanged),
  `contemplate` (place first, then the cognition section), `leave-reenter`
  (place before the re-entry phases). `bootstrap`/`ground` already walk the
  draft path — they become the mainline, unchanged.

## Walk contract (metrics)

1. New flow / Start writing / Write open a draft; `projectcentral.flow.list`
   stays empty; `ProjectCentral/now/flows/` holds no files; the scratch
   project is a git repo and `git status --porcelain` is empty — the owner's
   actual collision, reproduced and proven closed.
2. The draft carries the register named before opening (picker value).
3. Save with real content places the writing through Central's own operation;
   the owner's listing holds exactly that Flow with the exact bytes.
4. Save is unreachable while the writing is empty; an empty draft never
   reaches the ground.
5. Full regression floor green (all 20 suites on the final bundle).

## Standing

Branch `agent/oi-flow-day-clarify`, cut from origin/main `9cb5f073` (#257).
Claim: brief + proposal pushed, draft PR opened before the build commit.
