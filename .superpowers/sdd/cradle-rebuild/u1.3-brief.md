# U1.3 — Conflict + history

S can reconcile competing source edits and inspect recorded source changes
because Central owns CAS, revisions, and its source-change horizon (S→S0).

Law: “forced conflict shows both sides, no data loss, resolve continues from
resulting revision; history row count increments per save.” — map §5 U1.3.
Law: “Surface presentation != semantic ownership” — APP-SPEC §5.
Law: “No component copies selection state independently.” — cradle/02 §7.

Ground: cradle-p1 d2f5c99, baseline 155/155, native U1.2 verified. Central
832f2e5 built from archive; active product checkouts untouched. Its real
projectcentral.change.horizon already discloses source-scoped attributed
changes after writes. Use those records without inventing desktop history.

Files: kernel history.rs + lib seam; TS wire and SourceHistory presentation;
SourceSurface summoned History; real-owner conflict/history walk.

Walk: edit in UI, a second real Central writer advances canonical; Cmd+S
shows both exact sides and leaves source unchanged. Reconcile buffer explicitly,
reread then save from resulting revision; history gains exactly one row per
changed save, none for failure/no-op. Rows retain owner change/source refs and
attribution. Reopen the source and reread durable history. Native conflict and
history walk with original source bytes restored afterward. Run all checks
before and after; record screenshots, receipts and ledger; no design edits.
