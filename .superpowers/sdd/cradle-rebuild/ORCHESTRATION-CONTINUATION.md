# Full-programme continuation — current desktop ownership and execution

**Current owner direction: 2026-09-10.** The owner reports Wave 5 pushed and a strong current desktop. Continue that work through its coordinated thread. This is a planning/execution handoff, not an assertion that a remote session has inspected the local build or its acceptance receipts.

The [Waves 6–8 continuous-work amendment](../../../docs/OI-DESKTOP-CONTINUOUS-WORK-WAYFINDER.md) is the current next-work map. It supersedes the former foundation-first dispatch in this file and [BUILD-O-I-NEXT.md](BUILD-O-I-NEXT.md). Do not restart foundation, recreate `cradle-p1`/`cradle-rebuild`, or revive a dated pause/availability assumption merely because it remains in historical provenance.

The amendment updates the existing programme, not the shell or product ownership. Earlier unit identities, accepted work, outstanding gates and reference/design requirements survive wherever not explicitly changed. This documentation update does not itself launch agents, restart providers or alter the user's machine.

## Read the smallest sufficient current ground

1. [BUILD-O-I-NEXT.md](BUILD-O-I-NEXT.md) and the [current Waves 6–8 amendment](../../../docs/OI-DESKTOP-CONTINUOUS-WORK-WAYFINDER.md).
2. Actual local branch/worktrees/dirty files/active processes and the latest `progress.md`; identify occupied UI, bridge and provider files before assigning work.
3. The current [CAW entry/owner-operation ledger](https://github.com/EpiLogos/O-I/issues/220), [desktop #190](https://github.com/EpiLogos/O-I/issues/190) and [proving #201/#205](https://github.com/EpiLogos/O-I/issues/201), including current amendments.
4. Relevant parts of the [existing Wayfinder](../../../docs/OI-DESKTOP-CRADLE-REBUILD-WAYFINDER.md), [programme](IMPLEMENTATION-PROGRAMME-2026-09-06.md), [Self/Other UX](SELF-OTHER-FIELD-UX-2026-09-08.md), native design/Action/Surface contracts and exact supplied document specifications.

Do not reload the whole historical corpus for every unit. Old SHAs, process names and test counts are dated evidence. Consume accepted current public contracts and exact installed bindings; do not reinstall an old artifact to match a prompt.

## What the work is for

Make the desktop a faithful entrance into:

```text
human Day / Flow / Dialogue / Journal
→ explicit selected and scoped Agent work
→ Workcell-hosted NOW
→ Direct OR Factory execution
→ attributable result and pending Return
→ review / inclusion, with Recognition distinct
→ continuation / archive / re-entry
```

The blank tab makes three existing file-backed forms easy to enter: 0/1 (Dialogue/Flow), 4+2 (the supplied Day form) and the raw single-person Journal. Opening is ordinary owner-backed source/Surface navigation, not a template generator, duplicate document store, model invocation or task allocation. The owner supplies the final HTML elsewhere; exact payload/fidelity gates wait for those files, independent host/owner work does not.

Preserve all other programme scope: durable sources; operative Wiki/Search; actual Agency; living knowledge; System/contributions; source-faithful Factory and material surfaces; Shared Field/Explore; rich formats; complete native and human acceptance.

## Existing desktop structure remains binding

The right sidebar remains the optional accompanying Agent. In encounter context it contains Agent details and Conversation / Activity / Context / Inspect with the actual current subject. Shared Field detail uses ordinary canvas panes/full views/popouts, returning as tabs. No generic inspector replaces the four planes and no second encounter/composer/store is created for another presentation mode.

Keep the existing primary writing canvas, pane-local tabs, tiling, full/return, detach/re-dock, focus and restoration. Do not introduce a persistent panel-management toolbar, diagnostic footer or duplicate web app top bar. Native menus/keyboard/contextual actions carry the established grammar. Keep workspace presentation autosave distinct from native document persistence.

Use the current `packages/oi-design-system` and its token/accent/loading/expression contracts. No new design framework, fourth mockup or sample operational backend. The supplied document templates govern their own content and interaction; the host provides the established navigation/chrome and authorised native integration.

The reference set remains complete: Seed, WorkspaceStudy chat and tiled modes, plus the loading reference for loading behaviour. Do not repeat every reference walk for an unchanged component; do compare affected interactions against the right reference and carry unresolved required fidelity rows. Reference screenshots alone do not prove native window behaviour.

## Shared semantics and native-owner responsibility

A component consumes a stable subject/ref, owner state, disclosed operations and authority-at-commit. The host composes and presents; products keep source, Agency, session, development, material and intelligence semantics.

Use the current `oi` / S route for native operations and the desktop M′ composition. Bind the real active suite or explicit developer-source candidate. No private PATH-selected product executable or renderer-owned operation is a substitute for the public route.

Preserve source revision and authorship, selected-text scope, draft content, native identity/authority, real queue/delivery/permission states and pending Returns. No passive open/select/save/link/view change sends content or invokes a model. Imported document content receives no privileged bridge.

Native product gaps remain real owner work. Route them into current owner tracks, including the existing single-active Actuation migration queue, rather than inventing desktop state or duplicating an active hardening session. Source/public-contract availability and actual installed availability are recorded separately.

## Execution allocation

Subagents remain authorised where available; use them only for concrete independent work with explicit file and effect boundaries. They are not required for the programme to execute. Keep one UI lead for shared document/panel/layout work and one integration owner for each shared native seam.

Current useful parallel lanes are:

- document/blank-tab and native projection fidelity;
- disjoint native owner consumers and public contract tests;
- Factory trajectory/material presentation against published inputs, with UI-lead file agreement;
- independent read-only review and disjoint operational/accessibility/performance tests;
- QL/Wiki consumer integration once its accepted upstream contract is available.

Do not start competing Central/AIKit/Factory hardening programmes from the desktop. Existing owner teams supply the public operations. Central and AIKit consumer changes may proceed against a published contract before unrelated migrations finish. Actuation stays in its existing serial migration queue.

Each bounded BRIEF is at most 60 lines and gives: intended human result; existing unit and CAW proof ID; exact owning repo/files; source and owner operation/revision; permitted effects; forbidden overlaps; required real tests/readback; return boundary. An implementer does not spawn another unbounded orchestration hierarchy, mutate phase branches, replace the candidate, touch private user data or choose arbitrary executables.

Review every return. Model confidence is not acceptance. The lead remains responsible for useful implementation and integration, not endless supervision.

## Integration sequence

Begin from the current affected-state/source lock, not another suite archaeology exercise. Develop the 6A blank-tab/file opening and native document work. In parallel consume ready participant, source-scoped dispatch, receiving and material/development contracts on disjoint files.

Reach one full public path early:

```text
open document → select passage → choose actual Agent
→ preview exact source → explicit Send → actual response
→ durable pending Return → reviewed revision-checked inclusion
```

Then develop the full 6A–6F depth and the Wave 7/8 gates from the current amendment. Do not make all owner repos finish before the first useful native operation can be tested.

Serialize where realities join: document/message source basis; Agency/authority/dispatch; Return/source inclusion; shared bridges and schemas; contribution mounting; native build; exact executable binding; provider replacement; installed walk; matrix/evidence promotion; phase landing. One computer-use/native-walk operator and one candidate build owner. Never rebuild into an app being inspected.

Incoming owner revisions define the next candidate. Preserve exact source/contract/executable evidence for the candidate under test. Test the same `oi` route the app actually calls.

## Proof and returned state

Every required control has public operation → actual effect → owner readback → consumer integration proof → required native interaction evidence. A rendered absent state is useful but does not close a required unavailable capability.

Run focused deterministic/native-owner/browser checks between checkpoints. Native detach/re-dock/restart, installed provider/material behaviour and human writing/Return assessment retain their proper evidence requirements. Repeated unchanged AX inspection or full-suite runs for a cosmetic edit are not the default method.

Keep D/C/P/M/H distinct. A locally run fixture is not automatically material proof; an H author label in a document is not H-grade human acceptance. Final CAW P24 is a fresh non-implementer's full-path verification with consequential recovery and missing-connection regression, plus required human evidence.

Maintain one compact active section in `progress.md`: lane/owner, unit/UX/capability refs, deliverable, state, exact dependency, candidate, operation/readback evidence and next join. Existing accepted receipts stay; only affected relations need renewed proof. Matrix/account regeneration is a serial steward operation from accepted merges, not a file every parallel worker edits. Merge is an implementation update, not UX verification.

No blanket staging, destructive reset, force push, pulling over occupied dirty checkouts, unauthorised machine migration, or provider replacement during another operator's walk. Preserve unique work and evidence before retiring any development environment.

At handoff return exact changed files/owner operations, actual verification and candidate receipts, the next native walk, and specific remaining payload/provider/material/human gates. Keep independent work moving; do not turn the pending curated HTML or deeper QL work into a blanket stop for the desktop.
