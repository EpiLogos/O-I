# Full-programme continuation and orchestration mandate

> **Final foundation placement correction (September 8):** Read [the pane and resource contract](SELF-OTHER-FIELD-UX-2026-09-08.md#one-pane-system-an-optional-accompanying-agent). The right sidebar stays the optional accompanying agent; Shared Field details use ordinary canvas panes/full views/popouts, returning as tabs. A NOW starts from intention + one or more worlds/projections; selected outputs may survive closure. First rounds establish UI fidelity, window/focus/restore correctness and measured bounded resource use. This overrides earlier generic right-inspector wording for shared subjects.

> **Latest owner field correction:** [Self/Other UX and making/sharing journeys](SELF-OTHER-FIELD-UX-2026-09-08.md) governs shared NOW, VAK constellation navigation, rich canvas publication and point-cloud travel. Carry its contracts into FND/wiki work; dependent shared-owner gates remain explicit.

> Read the [September 8 product-foundation refresh](PRODUCT-FOUNDATION-REFRESH-2026-09-08.md) before dated owner allocations. It distinguishes current mainlines from separate candidates, incorporates landed Workcell detection and maps Factory full-page harmonisation. This planning review does not itself restart providers or implementation.


Standing: owner's subsequent direction, 2026-09-06. This is an execution handover,
not a new design exercise. It carries the full session's objective and delegated
decision authority. It supersedes prior wording that merely permits independent
work without authorising subagents. The user explicitly wants orchestration.

## Current foundation dispatch — owner clarification,2026-09-06

The minimal execution prompt is [BUILD-O-I-NEXT.md](BUILD-O-I-NEXT.md).
Detailed next work is FND-00–06 and GW-01–06 in
[the programme](IMPLEMENTATION-PROGRAMME-2026-09-06.md#foundation-execution-gate--owner-direction-2026-09-06),
with the [wayfinder](../../../docs/OI-DESKTOP-CRADLE-REBUILD-WAYFINDER.md#foundation-and-gateway-execution-map--current-binding-direction)
as the map of record. Read these current sections before historical allocations.
The full reference set requires real running inspection; the previously missed
right-hand structure justifies a fresh bounded pass. The top-bar exception is
narrow; the agent-panel and keyboard corrections remain binding.

Gateway product work is being developed separately by the owner/product team.
Desktop responsibility is its specified authorised consumer/awareness/bootstrap/
continuity seam; do not duplicate that lane or invent a desktop owner. Preserve
and coordinate other independent active work. Native endpoint progress must not
outrun the foundation's visual/interaction gate. Whole slices0–8 remain the scope
once that foundation is established. This specification/prompt update itself does
not restart paused implementation or providers.

## Your responsibility

Act as the accountable lead for the complete O-I desktop programme. The latest
file, failing check, or owner endpoint is not the whole task. Continue from the
actual current state; do not restart discovery, discard working native semantics,
or finish after one isolated improvement when other authorised work can proceed.

Read in this order, without repeatedly reloading the corpus:
1. This mandate and REORIENTATION-HANDOFF-2026-09-06.md, including later addenda.
2. IMPLEMENTATION-PROGRAMME-2026-09-06.md: all slices0–8 and their gates.
3. The latest progress.md entries and the current working tree/active work.
4. The design set and three studies required by BUILD-O-I-NEXT.md; consult the
  166-capability audit and bootstrap/token contracts for the affected obligations.
Historical handoffs do not override subsequent owner corrections. Exact hashes,
processes and test counts in older entries are dated evidence: verify current
state rather than reinstalling old artifacts or overwriting ongoing work.

The full destination includes: rolling native development; faithful spatial
shell and Central source safety; operative wiki/search; canonical ACP agency;
Flow/Return/living knowledge; System and contribution composition; Factory and
material surfaces; Shared Field/Explore; complete integration acceptance. Every
capability gets its owner operation and acceptance or an explicit alternate-native/
non-UI disposition. Do not reduce this to a chat app or filesystem browser.

## Autonomy transferred

You may make routine implementation and scheduling decisions, refactor unsuitable
presentation, fix missing operations in their owner repositories, delegate bounded
work, integrate verified changes and proceed through authorised gates without
asking the owner to repeat the brief. Existing commit/push/phase-merge authority
continues subject to ownership and passing gates. No blanket staging, worktrees,
force pushes, destructive resets, pulling occupied dirty owner checkouts, or
replacing an active provider. No claimed phase completion from partial tests.

Ask only when a consequential choice truly lacks authority or necessary owner
information. Report the exact blocked operation and keep independent work moving.
Missing native functionality is generally implementation work, not a permission
request and not permission to invent desktop business logic.

## Start orchestration now

Inventory active work before assigning files. Reuse existing agents where visible;
do not duplicate a lane already owned by another session. Use available subagent
capacity for concrete independent tasks while doing useful integration/UI work
locally. Delegation here is explicitly authorised; separate user-visible tasks are
not required. If subagent tools are unavailable, state that once and preserve the
same work breakdown while executing locally.

Initial allocation, adjusted for current ownership and available slots:
- Lead: own production shell fidelity and overall integration. Implement native
  app chrome, tab-bound workspace arrangements, regional colours/layers, compact
  project modes and responsive geometry from the accepted studies. Do not spend
  the whole turn supervising while leaving this primary work undone.
- AIKit lane: owner-level generic ACP encounter operation, thinking content
  passthrough and long-lived streams without an arbitrary event-count ceiling;
  bound memory through retention/backpressure. Prove real ACP continuity,
  explicit cancellation and tool/permission authority. No Pi-specific bypass.
- Central lane: remaining ordinary-file edit/CAS/history owner contract and real
  native consumer evidence. Preserve authored-source authority and dirty drafts.
- Acceptance lane when capacity permits: independently compare actual production
  presentation with all three studies and track programme/capability omissions.
  Read-only review ownership avoids colliding with lead's UI changes. Queue this
  lane if all slots are occupied; do not oversubscribe for appearances.

Each assignment carries: concrete result; owning repo/files; relevant design and
owner contract; forbidden overlap; real tests/acceptance; required return evidence.
Agree refs, operations, event/read-model shapes and availability behaviour before
connecting code. Workers report exact diffs/revisions, commands/results and gaps;
lead reviews and integrates. A worker's confident summary is not acceptance.

## Control the whole programme without another planning round

Maintain one compact live section in progress.md: lane, owner/agent, deliverable,
state, dependency, evidence, next integration gate. Update at meaningful changes.
Track both visual fidelity and functional acceptance; neither implies the other.
Preserve latest routing corrections (including oi/S and desktop/M′) rather than
recreating direct private product routes from an older handoff snapshot.

UI and native work can proceed independently against explicit contracts. The
production shell's structure comes from the accepted design, not whatever owner
endpoint landed first. Keep existing real integrations attached; no new demo
backend, transcript store, ranking database or fake operational controls.

Coordinate shared resources: one preview/native build owner at a time; no builds
into an app under inspection; one computer-use operator. Inspect study references
once, compare at the coherent visual checkpoint, then exercise changed native
flows. Use appropriate automated checks between checkpoints, not repeated broad
UI inspection or full suites for every small edit. Preserve substantive CAS,
identity, authority, focus and persistence tests.

Communicate meaningful progress: what changed in the app, which lanes are active,
what evidence passed, what remains at the current gate. Do not present a local
check count as programme completion. At handoff, carry the complete objective,
accepted corrections, active ownership, current artifacts and next concrete
steps so the successor can orchestrate rather than follow the last code path.


## Owner correction and pause — 2026-09-06

Standing: explicit owner review during the rate-limit pause. This corrects the
current implementation and execution acceptance; it does not endorse the current
shell as a foundation or waive the remaining programme.

- The dynamic right-hand layer must implement the accepted agent-panel shape.
  In encounter context it carries agent details and the complete
  Conversation / Activity / Context / Inspect chain and its associated content.
  Remove the duplicated encounter header/plane section currently occupying the
  canvas; move that section to the right panel. A generic Context / History /
  System inspector is not an adequate substitute for the subject's actual planes.
  Preserve one native encounter/transcript/composer across side, full, tab and
  detached presentations; do not create another chat state to repair layout.
- Panel and window manipulation is keyboard-first using the existing professional
  grammar. Remove the wasteful persistent panel-management control bar identified
  by the owner. Native menus and contextual disclosure support discovery and
  accessibility; do not replace the removed row with another cluster of buttons.
  Preserve useful pane-local tab grammar and deliberate task controls.
- The canvas is a general material/surface host, not a bounded-text viewer with
  other formats treated as afterthoughts. Broad file-format handling, especially
  actual rendered HTML, is a programme obligation. Track native format/type,
  renderer availability, actual content, editing/read-only state, provenance and
  failure/fallback separately. HTML needs an actual contained rendering surface;
  showing markup in a textarea does not establish HTML support. Unsupported
  formats require an explicit native opening/fallback disposition, never a claim
  that every format renders. File viewing does not grant execution or source-write
  authority.

Execution correction: native-owner progress must no longer outrun validation of
where its presentation belongs. Before integrating the next body, trace the
accepted reference's region, active-subject relation, planes, keyboard transitions
and full/collapsed behavior into the actual shell. Verify those relationships as
well as pixels; passing current-layout tests does not establish the right design.
Do not restart discovery or produce another mockup. Resume with the right-panel
structure and removal of redundant chrome, then integrate proven owner work in
programme dependency order. All slices0–8 remain in scope.

Implementation is paused at the owner's request for reflection. Preserve all
uncommitted work, candidate receipts and active native providers; no new builds,
provider launches, promotion or installation during this pause. Later continuation
must distinguish verified candidates from unfinished rate-limited agent work.

