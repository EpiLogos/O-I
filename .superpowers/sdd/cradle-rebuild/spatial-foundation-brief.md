# Spatial/workspace foundation — owner priority correction, 2026-09-06

S hosts native subjects in a professional three-region desktop workspace;
products retain semantic identity and mutation authority. Supersedes filling
individual surfaces until D16–D19 and the U1.4/U1.5 foundation are real.

Law: “The shell must support resizable/collapsible regions, keyboard-first
movement, tabs and useful split views.” — APP-SPEC §3.
Law: “Events never move the layout.” — map D18.
Law: “Autosave is crucial” — map D19.
Law: “No component copies selection state independently.” — cradle/02 §7.

Ground: cradle-p1 2fe8a7e; Central 832f2e5 archive. Baseline 172 checks.
No design changes. Keep actual source operations and owner change records;
replace shipped test-surface entry points with real surface navigation.

Build: explicit left/centre/right host regions; side depth/width state;
keyboard and pointer resizing/collapse/full; movable/resizable centre splits;
root/project/freeform workspace records including layout, contextual owner
query and working drafts. Restore is a request to native owners, not imported
semantic truth. One kernel focus feeds both the canvas and right region.

Walk: three real regions; source opened from left is the right subject;
resize/collapse/full return preserves layout and source identity; two projects
and a named workspace restore distinct tabs/depths/drafts after reload and
native relaunch; narrow dimensions degrade sides before the canvas; no test
surface can be opened in the production bundle. Replace synthetic frame
checks with real source walks while retaining their behavioral coverage.
