# EX2–EX5 integration review — 2026-09-15

Implements O-I #306 EX2–EX5 against `docs/contracts/EXPRESSION-APPLICATION-V1.md`
and `shared-field/WORLD-PRESENTATION.md`. The four implementation lanes ran with
Sol at low effort; the integrating agent reviewed their changes, returned defects
for repair, and exercised the combined application through its native shell.
EX6 / PR #321 is included in the convergence base.

## Result and evidence

- **Nara:** seven independent K10 centres and distinct EarthBody remain in one
  retained engine field across M1–M4 focus and close/re-entry. Bimba and Epii are
  summonable. Epii receives an explicit allowlist of safe refs/cues; live Personal
  values never enter that portable document. The QL producer merged in
  [QL-MEF #199](https://github.com/EpiLogos/QL-MEF/pull/199), commit
  `2848f7e39b0ab6c7a45a9bbf086aa6d3672537b5`. See `EX2-NARA-RECEIPT.md` and
  `instrument-native-host.json` for the actual QL host/C++ worker/kernel walk.
- **Knowledge:** the native owner-backed walk passes 18/18, including real search,
  Wiki/ProjectMap, stable refs and revisions, local-whole projection, source/page
  return, pin/follow and back/forward. The native shell additionally proves Wiki
  popout, re-dock and return to the same graph selection. See
  `knowledge-expression.json` and `expression-native.json`.
- **Human/Agent composition:** actual native socket proposals, human correction,
  rejection and continuation preserve identity and decisions. Structured pedagogy
  compiles scenes, source-bearing objects, movement, methods and evidence through
  the same kernel. Joined Chrome verification includes stale-basis refusal and
  delayed owner mutation while selecting another document. See
  `expression-native.json`; the real-owner expression integration suite passes
  13/13, including the normally owner-gated cases.
- **Pages:** the owner-backed walk passes 18/18. Native WebKit proves exact page
  admission, moving the same canvas into focus, capture followed immediately by
  return, standalone fallback, and invalidation on unsaved page edits without
  writing the owner file. Rereading the owner readmits the same Expression.
  See `expression-ex5/RECEIPT.md`, `expression-page.json` and
  `expression-native.json`.

## Repairs made during joined review

The integration repaired dropped asynchronous captures on immediate return,
unreadable engine ink on dark surfaces, a closed-instrument reopening fault,
stale composition selection, overbroad portable cue copying, and page admission
surviving an unsaved edit. A real context-loss walk also found that pausing left
an animation frame queued: it could render against the lost context and dispose
the retained canvas. The recovery receipt records the repair and repeated real
loss/restoration verification.

## Executed gates

Commands run from the O-I root unless noted:

```sh
node --test shared-field/*.test.mjs
OI_CENTRAL_CTRL_BIN=/Users/admin/.local/bin/ctrl CARGO_INCREMENTAL=0 \
  cargo test --manifest-path desktop/cradle/kernel/Cargo.toml --test expression -- --include-ignored
CARGO_INCREMENTAL=0 cargo test --manifest-path desktop/cradle/kernel/Cargo.toml --lib
```

Commands run from `desktop/cradle`:

```sh
npm run build
node tests/knowledge-expression.mjs
node tests/expression-stage-placement.mjs
node tests/search-overlay.mjs
npm run test:personal-web
```

The combined shared-field contract suite passes 220 tests. Knowledge projection
passes 13 assertions. Personal Web passes 34 contract tests and 41 browser
assertions. CI also executes the locked production build, native-shell build,
kernel floor and clippy, search in Chromium/WebKit, and the existing repository
checks. The first joined CI kernel attempt had an isolated census-warning test
failure; the unchanged isolated test and complete 36-test local unit suite pass.
CI results are attached to [O-I #320](https://github.com/EpiLogos/O-I/pull/320).

## Scope of the observations

Runtime proofs used isolated source files and controlled QL reception. They do
not claim human sensory or material acceptance. Undisclosed QL grammar, cymatic
stations, Agent personality, authority and native Activity remain explicitly
unavailable. Geometry introduces no semantic relations. Caller-provided Activity
correlation remains distinguishable from observed owner Activity.

The native Wiki review encountered an unavailable SharedField module binding in
that local fixture; local Wiki projection and native popout still completed.
Hosted publication has the separate EX6 acceptance receipt; this review does not
relabel the local fixture as a hosted-field proof. No new renderer, audio engine,
Personal/knowledge store, Agent loop or CMS was introduced.
