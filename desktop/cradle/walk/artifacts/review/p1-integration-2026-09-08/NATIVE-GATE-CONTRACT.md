# P1 native acceptance contract — 2026-09-08

Agent-authored preparation; no native acceptance claimed. Ground: programme FND-00–07, Self/Other resource contract, D22 and the latest resize-only owner correction.

## Serialized inputs

Freeze source content and locks; build one native app with WALK=0; record native/owner executable absolute paths and SHA-256. Use isolated Central and AIKit acceptance homes and a unique native data-store identifier. Preserve existing user windows/data and resident PID 79665. UI lead owns visual comparison, under an explicit resource handoff after the functional walk.

## Functional walk

Walk cold bootstrap and explicit absent/degraded owner exits; ordinary and authored source open/write/CAS/history; HTML relative assets/modules, Markdown, image, PDF or explicit native disposition, unsupported binary; native menu/keyboard focus/split/move/full/restore/close/reopen/detach/redock/relaunch; same owner refs/drafts/caret/scroll across view changes. Two real encounter subjects must drive the accompanying agent and its four planes without stale subject data. Required unavailable evidence remains open.

## Resource cycles

Record target machine/OS/display scale and exact small/large dataset byte counts. Before cycles collect 60 seconds of warmed idle RSS/CPU and representative peak loads. Declare observed noise plus explained bounded cache allowance before acceptance. Do not reuse Chromium heap thresholds as native budgets.

For each dataset: three warm-up and twenty sampled cycles, no forced reload or restart to hide growth. Four supported live surfaces: source/file, existing knowledge reading where available, temporary native browser, terminal. Switch/split/full/restore, then sequentially detach/redock supported surfaces, returning to identical baseline. Check drafts/caret/scroll/camera, browser form/counter and PTY PID/output continuity. Close through actual UI; require native window disappearance, temporary browser disposal, PTY disposal and unchanged independent provider lifetime. Test relaunch separately.

Capture timestamps, PID/PPID/start time/executable and RSS/CPU for attributable native processes. WebKit helpers can be parented separately/shared: correlate births/exits with surface operations and report uncertain attribution separately. No summing every machine WebKit process. RSS includes shared pages. Report native heap, GPU/subscription counters unavailable unless reliably observed, never zero. Compare settled first/last five-cycle bands and slope/uncertainty against predeclared envelope.

## Expression

One overlay per host window; actual resize intent holds one emitter and drains within two seconds after release; other motion intents remain reserved by owner correction. Exercise 200 supported intents with four surfaces; record drawing cap <=30/s and bounded emitter/point storage. Hidden/paused clears transient work and schedules no frames; reduced motion refuses gestures and preserves a still form. Theme checks retain desktop/light/dark/warm-dark/contrast. Agent morphs consume real session events, not simulated progress. Native window attachment must not duplicate overlay work.

## Failure conditions and limits

Any lost state, failed cycle, duplicate live consumer, orphan window/PTY, provider cancellation, invented availability, unexplained resource accumulation or missing required attribution keeps FND-06 open. Browser child.hide() alone does not prove media/JavaScript suspension; use a real observable lifecycle page.

Existing browser evidence: resources-measurements.json has three warm-up/twenty sampled Chromium cycles, zero step failures and CDP-collected heap slope 20537.83 B/cycle; this is not native resource proof. resources-summary.md retains the older rejected eighty-failure run; current resources.json is the passing wrapper. Individual native browser/PTY receipts establish bounded operations, not repeated lifetime acceptance.

## Review and landing

Review integrated diff and exact capability IDs. UI lead closes or leaves open every required reference structural row with same-candidate native evidence. Append actual results and limitations to progress.md. Land/retire cradle-p1 and open cradle-p2 only after the stated gate passes; downstream findings retain existing U/GW/SF units.
