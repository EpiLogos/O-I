# Native material runtime observations · 8 September 2026

Isolated real Central project: the earlier `oi-cradle-editor-RQgvZ1` fixture, with `walk/fixtures/rendering-quality` copied beneath Work/Material. Platform: macOS WKWebView in the actual debug O:I.app. No fake bridge or provider supplied native results.

Before change: authored CSS/grid/SVG rendered; Classic JavaScript Ready; ES module import Waiting; Relative data request Unavailable; Native bridge Not exposed. This is a real runtime deficit, separate from the old minimal study.html's intentionally unstyled content.

After shared MIME adapter and opaque-origin CORS response: Classic JavaScript Ready; ES module import Ready (entry.mjs imports module.mjs); Relative data request Ready (data.json); Native bridge Not exposed. Add one changed12→13. Reload preview reset13→12 and returned all three runtime results Ready. Native fullscreen-canvas screenshot is embedded in the task record. Authored responsive content is legible and styled with the same CSS as the standalone page.

The native app still bootstraps and reads owner files after application commands became manifest-controlled and capabilities switched from containing windows to trusted shell webview labels. This establishes shell operation with the ACL in place; absence of a JavaScript global alone is not a full adversarial IPC acceptance test. No general browser webview or remote-computer provider was introduced.

Browser walk screenshots and tests are separate evidence and do not stand in for native profiles/downloads/permissions, process/GPU lifetime or hidden-session state continuity.
