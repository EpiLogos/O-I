# Git review retention — 2026-09-14

Moving a Git result between panes or native windows previously fetched current working changes again. The Surface now carries the exact bounded AIKit reading already under review. Explicit Refresh replaces it. An invalid or oversized saved reading preserves the Surface and unrelated tabs, discloses the recovery condition, and requires explicit Refresh instead of silently showing a different patch.

The native transport accepts only view state for the held Project/worktree/comparison identity. Detached reload and redock consume the latest validated view. Requested worktree identity remains distinct from the owner's observed worktree; a mismatched patch stays withheld.

## Executed evidence

- Production TypeScript/Vite build passes. Surface composition: 24 checks.
- Real AIKit snapshot validation: 17 checks, using the current owner response and corrupt derivatives for refusal cases.
- Production conversation placement: 18 checks. Factory workspace/reload/leave: 18 checks.
- Expanded Git browser walk: 34 checks, including complete snapshot equality, no extra owner reads, unrelated tab identity, corrupted-snapshot recovery and explicit Refresh. Result: `../git-return-ui.json`.
- Native Omarchy CUA: the displayed diff section is byte-identical before/after detach. Explicit Refresh in the detached window returns a different owner-observed patch; re-dock retains that refreshed patch. The sibling JSON records SHA-256 values, windows and exact source-file hashes. These native hashes cover the displayed section; the browser walk covers the complete persisted owner snapshot.
- Leaving Factory restores the original ordinary terminal view, with the existing persisted tmux marker and no new terminal input.

Screenshots: [Factory result desk](right.png), [detached](detached.png), [after explicit Refresh](refreshed.png), [re-docked](redocked.png), [ordinary workspace restored](ordinary.png).

The view snapshot is limited to 64 KiB. A reading beyond that bound remains viewable where mounted, with an explicit retention/recovery notice; it is not silently truncated or reread on relocation. Source/build/owner evidence here does not establish Factory execution, populated Factory material, all-harness acceptance or human acceptance.

The existing context checker passed its file and native-command portions, then reported its legacy primary-worktree restriction. The owner's explicit instruction to continue this existing Omarchy worktree governs that restriction; no checkout was reset or replaced.
