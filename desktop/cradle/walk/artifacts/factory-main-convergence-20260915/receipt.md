# Factory current-main convergence — 2026-09-15

Continues O-I #289 through draft PR #292 in the existing Omarchy checkout. The accepted Factory Agency design, owner correction `35d2aa0`, arrangement wayfinder, Ta-Onta amendment and #220 lane U allocation govern this increment. It does not redefine those sources.

## Result

Current main `dd0e5d5ab2cbb8abdba257f1bb8dd3e1c29da05c` is incorporated through merge `cf4e21b748eb77e446be17f9769d2c4470f3e4b7`. The merge keeps the Factory Run/Build composition, returned material, Handoff, Git review, Project NOW, Agent/session readers, exact terminal attachment and presentation persistence while adding main's Explore/WorldPresentation, Nara/Expression and configuration work.

Direct inspection of the merged Omarchy desktop found one product-lock regression: ordinary work exposed a fifth persistent `Composition` tab beside Conversation, Activity, Context and Inspect. Expression composition now opens through the existing summon event inside Inspect. Selecting Inspect again returns to the exact native session inspection. The ordinary and centrally promoted encounter headers both retain the accepted four planes, with one canonical transcript and composer.

The app entered the authorized queued Run, promoted the existing conversation, then returned to `Working Surface`. The terminal still showed binding `working-surface/oi-factory-demo-20260914-shell` and marker `OI_DESKTOP_PERSISTED_SURFACE_20260914`. No prompt or terminal input was sent. The right result region remained honestly empty because this Run has no returned material.

## Executed evidence

| Check | Result |
|---|---|
| `npm run build` | passed after merge and after the four-plane correction; 329 production modules |
| `cargo test --locked --all-targets` in `desktop/cradle/kernel` | 37 library + 12 Expression + 4 SharedField graph tests passed; one Expression and owner-gated integration cases remained ignored |
| `cargo build --manifest-path src-tauri/Cargo.toml` | passed after merge and after the correction |
| `node walk/factory-workbench.mjs` | passed the current-main Factory composition/return walk |
| `node walk/factory-live-updates.mjs` | passed 24 real-owner update, refusal, focus and review-retention checks |
| `node walk/conversation-placement.mjs` | passed 19 checks, including the exact AgentSession/SessionSpace, four-plane header, Factory promotion and ordinary return |
| `node walk/workspace-persistence.mjs` against production preview | passed pagehide/reload restoration; the Vite development server separately exhausted Chromium resources |
| Expression summon browser check | passed: summon opens in Inspect; Inspect returns to native session details without a fifth plane |
| Native AT-SPI + compositor walk | passed: queued Factory → canonical conversation → exact persisted terminal → ordinary four-plane right region |

The post-merge controlled `instrument-host` scenario could not run from the current Omarchy QL checkout. Its current `HEAD` does not contain `adapters/retained-field/focused-instrument-session.mjs`, and O:I still has no production registration callsite beyond the walk client. The diagnostic receipt is retained as `controlled-instrument-unavailable.json`; this increment does not claim ordinary → Epi/Nara → Factory parity.

Screenshots and machine-readable readbacks in this directory show the actual merged application. `native-factory-entered.png` shows the queued Run and empty result region; `native-factory-conversation.png` shows the one central conversation with four planes; `native-factory-return.png` shows the exact persisted terminal with the ordinary four-plane Agent. Browser walk results are copied here before restoring the pre-existing artifact ground.

## Owner reality and standing

The authorized state `desktop/cradle/walk/material/header-continuity.state.json` remains Run revision 2 / Build revision 1, queued, with one workflow unit and zero executions, Candidates, Evidence or retained tasks. The live terminal and resident session are not this Run's execution.

Public installed owner reality remains:

1. AIKit exposes effectful `factory start-work`, but no published shared Start resolver admits a selected Agent/Profile/AgentSet or formation into a Direct/Factory session with effective context, praxis and authority readiness. AIKit #311 and #312 remain open.
2. Actuation #85 implements local authority in source, but the installed Omarchy CLI has no authority route. No real Factory attempt/Execution exists for `attempt owner-action`.
3. Factory exposes the harmless Run, Build, workflow-unit and attempt-task reads used here. It exposes no typed public artifact body/location/Surface resolver.
4. Routine catalogue/occurrence/enable/pause operations remain unavailable. The requested-worktree Git read still requires its owner correction; the O:I consumer keeps its mismatch refusal and explicit Refresh.
5. The generic Expression/Nara consumers are present, but no production QL/Epi focused-instrument source is registered. Separate controlled evidence does not establish the required continuous product path.

The consumer implementation and regression evidence support D/C, plus limited P/M for the existing Codex SessionSpace and terminal. They do not establish commissioned Factory execution, populated Candidate/Evidence/Return handling, all-harness acceptance or human Recognition. PR #292 remains draft.

## Preservation and next join

Before merging, 33 modified/untracked artifact files were archived under `/home/frank/.cache/codex/factory-campaign-20260915-pre-dd0e5d5`. After the walks, every archived file passed `sha256sum -c`; tracked walk outputs were restored before the new evidence directory was added. The frozen Factory runtime remains `/home/frank/Central/Work/Factory/.worktrees/codex-developmental-build-read/target/debug/factory`, SHA-256 `a48c87ecb43244faef73e8c231f70594a738c7469e943cd0c1e7356cfe6dc133`. The resident AIKit service PID 589249 and its exact SessionSpace remained in place.

Next executable join: lane A publishes and installs the shared Start/admission and scoped authority → real Factory Execution operations, and Factory or its artifact owner publishes the typed material resolver. U can then drive the accepted actual execution, repair/evidence action, Return, ordinary-work survival and re-entry walk on this PR. Instrument, Routine and Git owners return their exact operations under the current allocation.
