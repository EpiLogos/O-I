# Collection/source acceptance — PR #428

This is the bounded executable packet for the existing Technè Wayfinder's W7 collection continuation, not another programme or a complete-corpus verdict.

## Source and evidence already returned

Production repair: `d5f298e3f3a0686d7ba784378dc01c7877ca5806`, incorporating `53d66d482377b411a811102f6e628750882fe3f3` and the native test-route correction `60ef2a8873cc294c95439ac9840fde661c07b47c`. Documentation-only child `880d352c37967063cde811314cc7d9356592203f` appends the return to `.wayfinder/maps/techne-expression-mode.md` without replacing its original bytes or C1–C12 requirements. The temporary append workflow removed itself; no continuing write automation remains.

[Collection workflow 35505819058](https://github.com/EpiLogos/O-I/actions/runs/35505819058) passed on synthetic merge `86d6ceea6007b7cfa78f2b62e5a41e536b63bafc`: complete embedded Expressions/Cradle build, 23 controlled regressions, 10 production-Library browser checks, existing native Expression suites and nine new source-built native checks. Native producer pin: Central `12ee31313e710378845baae190efc1a4b3e7ebdb`. The native receipt records the actual O:I, ctrl and kernel executable hashes. [OI Verify 35505818934](https://github.com/EpiLogos/O-I/actions/runs/35505818934) passed its Rust and suite-data jobs; its path-gated Central-bootstrap job was skipped, not executed.

Consumer artifact `10603796158` contains `browser.json`, `controlled.log`, `library-membership.png` and `library-shared-withdrawal.png`. Native artifact `10603399450` contains `native.json` and `native-bridge.log`. These are controlled browser and source-built disposable-ground results, not installed Mac, real-model, microphone or human acceptance.

## Run from an already reviewed checkout

Do not reset, pull, install or deploy onto personal machines as part of this packet. Use the chosen existing O:I and Central source checkouts. Prepare locked dependencies and source-built executables using their normal build commands:

```sh
npm ci --prefix desktop/cradle --no-audit --no-fund
npm ci --prefix desktop/cradle/expressions-app --no-audit --no-fund
npm --prefix desktop/cradle/expressions-app run build:journeys
npm --prefix desktop/cradle run build
cargo build --locked --manifest-path cli/Cargo.toml --bin oi
cargo build --locked --manifest-path desktop/cradle/kernel/Cargo.toml --bin walk-bridge
cargo build --locked --manifest-path /chosen/Central/Cargo.toml -p ctrl
```

From `desktop/cradle`:

```sh
node --experimental-strip-types --import ./tests/ts-register.mjs --test tests/collection-source-identity.test.mjs tests/collection-consumers.test.mjs
npx playwright install chromium
node tests/collection-browser.mjs
OI_BIN=/absolute/source-built/oi OI_CENTRAL_CTRL_BIN=/absolute/source-built/ctrl OI_COLLECTION_BRIDGE_BIN=/absolute/source-built/walk-bridge node --experimental-strip-types --import ./tests/ts-register.mjs tests/collection-native-acceptance.mjs
```

The three native executable paths are required. The kernel calls the actual O:I CLI, which calls Central; do not substitute a wrapper that bypasses this route. The native packet creates and removes only its own uniquely named temporary Central/home. It never loads the operator's existing Central, contacts a model, changes an installation or connects to another machine. Capture actual source revisions, dirty-file status and platform outside the disposable ground. Evidence defaults to `tests/artifacts/collection-source`; copy any earlier result elsewhere before repeating. `OI_COLLECTION_EVIDENCE` may direct native evidence to a new external directory. Browser evidence stays in its named test-artifact directory.

## What the executable checks prove

The controlled tests exercise exact identity and per-member errors, duplicate/unsafe/malformed input refusal, four-way read concurrency, a 193-member paged reading with all memberships retained, metadata-only discovery, late-member search, cancellation, same-source multiple memberships, source-revision drift, cross-world isolation, root work without child Projects, denied/private exclusion, and native CAS requests/readback.

The browser mounts the production `LibraryBrowser` under its real `KernelProvider`, with test-only owner HTTP responses and actual committed composition bytes. It proves first-open provider discovery without pre-registration, source-first opening of the second member rather than its first sibling, keyboard-triggered membership save exactly once, unchanged member body, retired-source refusal, immediate private-item withdrawal and cancelled opening on shared-horizon change. This is not the installed shell's complete page/graph/Expression/Technè return path.

The native packet uses source-built Central/O:I/kernel and all 49 committed member documents unchanged. It exercises actual manifest CAS/readback; native Expression creation/binding, collection edits and index; explicit source save; stale Expression/file revision refusal; process restart; reopening the saved document/selection and exact source return. Initial test Expression file placement is disclosed setup, not UI first-file-creation proof. No particle checkpoint is claimed.

## Original failures retained

`tests/evidence/collection-source-baseline.log` records Seven Centres incorrectly resolving to Field Studies. The repaired reader no longer caches a selected leaf under its directory key.

`tests/evidence/collection-browser-environment-failure.log` preserves the local Chromium `ERR_BLOCKED_BY_ADMINISTRATOR` result. No policy bypass was attempted; GitHub-hosted Chromium is a separate environment.

Run `35505037158` retained the missing-O:I-executable native setup failure and the initial empty-Library browser timeout. Run `35505412427` passed native checks after supplying the actual O:I executable, but its browser diagnostics showed no provider operations on first mount. The production `useSyncExternalStore` repair corrected the missed initial provider registration; the later browser passed without test preloading.

## Installed interaction remains a separate evidence grade

The local integrator should enter the reviewed installed candidate's Library, search a real eligible subject, open its exact source, return to the same selected collection occurrence, change a membership without changing the subject, reject a concurrent stale save, and reopen the saved native Expression after restart. Record source/build/install/running revisions and the actual Mac interaction. Keep existing unrelated writing and private content untouched. Real-model and microphone/audio checks remain the W3/W5/W6 native facilities' own packets; none is simulated by this collection test.

## Full commission is not closed

The native material tested is 40 legacy compositions plus one Return-of-Zero essay composition and eight section rooms. It is not the complete current Return-of-Zero canonical census, live Bimba/Atlas collections, O:I plus all six product corpora, or the distinct Epii M5-1/Antichrist corpus. Those remain actual source/knowledge/production obligations, not presumed absences.

The shared embedded-app Journey/native-Expression opening and save relay remains with W5's active writer; the generic Wiki/index/whole/member/role operations remain with their current native owners. All six professional instruments, the complete same-position graph/page/Expression/Technè/source traversal, and admitted public readings/assets for lane I #417 remain required. This PR supplies the collection consumer repair and native proof, not fake publication inputs or another renderer/semantic store.
