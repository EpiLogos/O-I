/** The registry-era flow route is retired (Central #177): the human writing
 * path is the flow-instance module (`instance.ts`) through Central's
 * `central.files.*`, and commissions ride the kernel's
 * `instance_commission` op. No typed flow-action route remains. */
export {};
