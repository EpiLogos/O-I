{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "O:I root shell window. No shell, filesystem, process, network or secret plugins are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
